{
  "id": "cc.0.coding-agent-survey-aider-swe-agent.20260704.134125308076",
  "emitter": "cc.0",
  "namespace": [
    "coding-agent-survey-aider-swe-agent"
  ],
  "category": "notes",
  "kind": "note",
  "emitted_at": "2026-07-04T13:41:25.308076",
  "links": {
    "decisions": [],
    "tickets": [],
    "commits": [],
    "whys": []
  },
  "body": {
    "title": "Survey: how aider/SWE-agent get (small) models to EDIT vs our AgenticLoop",
    "text": "# Survey: how aider / SWE-agent / peers get (small) models to EDIT — and what our AgenticLoop does differently\n\n**Why this note exists:** three sessions were spent tuning our loop from the *inside* (num_ctx → context-discipline eviction → honest-halt) trying to get devstral-small-2:24b@Hex to stop read-wandering and actually emit edits. Two live DS.0 observe-runs produced **0 Write/Edit tool calls** — and the log proves those were **0 edit *attempts*, not 0 successful edits** (num_ctx run: 47 Read / 8 Bash / 0 edit; earlier run: 102 Read / 12 Bash / 0 edit). The model never *tried* to edit. That's a signal the *loop architecture* is the bottleneck, not a parameter. aider et al. get small/local models to edit reliably today. This surveys what they do differently. **This is research to inform a design review — not a decision, not a ticket batch yet.**\n\n> **Diagnosis ranking (from the log evidence):** zero *attempts* means the model died in **orientation/planning**, never reaching edit-serialization. So the evidence-backed suspects are the repo-map (#1), architect/editor split (#3/#4), and edit-centric turn structure (#5). The edit-format contract + lint gate (#2) is aider's demonstrated problem but **speculative for us** — we can't confirm it bites until orientation is fixed and devstral actually attempts a diff. **Most robust conclusion: the architect/editor split (#4)** — it helps whether devstral *won't* attempt edits (a strong planner drives it) *or* attempts and fails at serialization (it only has to emit a diff from an explicit plan).\n\n## The landscape (open-source, 2026)\n\n| Tool | Stars/license | What it is | Relevant idea |\n|---|---|---|---|\n| **aider** | 41k, Apache-2 | Git-native terminal pair-programmer | **Repo map** + **edit-format discipline** + **architect/editor split** |\n| **SWE-agent** (Princeton) | research | The ACI paper: \"Agent-Computer Interfaces Enable SWE LMs\" | **Interface > model**: linter-gated edits, 100-line windows, concise feedback |\n| **OpenHands** (was OpenDevin) | 68k, MIT | Framework: agent controller + sandboxed exec | universal controller, multi-session, sandbox |\n| **Cline** | 58k, Apache-2 | Editor-first: plan → review → execute+iterate | explicit plan/act split with human review gate |\n| **Goose** (Block) | 32k, Apache-2 | MCP-native CLI; **runs local models well** | MCP-from-day-one, extensible |\n| **Plandex** | — | Very large multi-step tasks, 2M ctx, diff-review sandbox | staged diff review before apply |\n\n## The five structural differences (why they edit and we don't)\n\n### 1. Repo map instead of full-file dumps — the big one\naider auto-builds a **~1k-token ranked signature map of the WHOLE repo** (tree-sitter: file names, class/method/function signatures, import graph), selected by a **graph-ranking algorithm** (files = nodes, dependencies = edges) against the current task. The model sees *structure* without reading file bodies.\n\n**We do the opposite.** Our AgenticLoop hands the model generic `Read`, and the model discovers structure by reading whole files one at a time — each result (≤3000 chars) appended to `messages` and **re-sent whole every turn**. That IS the read-wander + context-bloat we've been fighting. Our context-discipline eviction fix treats the *symptom* (bloat); aider *never creates the problem* because it never dumps full files to discover structure.\n\n### 2. Edit-format discipline + a validation gate — *(speculative for us; see ranking above)*\n- **aider**: edits must be **SEARCH/REPLACE blocks** (git-conflict syntax). aider *measures edit-format conformance* as a first-class metric. Crucially: **quantized/small local models \"are not capable enough to follow aider's system prompts\" → aider drops them to the `whole` format** (return the entire file). Weak model ⇒ simpler edit contract, not \"try harder.\"\n- **SWE-agent**: **a linter runs on every edit; syntactically-invalid edits are DISCARDED and the model is re-prompted to try again.** Errors are caught early instead of compounding.\n\n**We have neither** — no edit-format contract, no syntax gate, no discard-and-retry, no `whole`-format fallback for the weak tier. A 24B model needs the *simplest reliable* edit contract; we gave it free-form tool calls. **Caveat (honesty):** our runs show 0 edit attempts, so we have *no evidence yet* that our edit path itself fails — this is aider's demonstrated failure mode that we predict will surface once orientation is fixed, not one we've observed. (Open follow-up: confirm what our `Edit` tool actually returns on a non-matching edit — silent no-op vs structured error — before assuming this bites.)\n\n### 3. A constrained LM-centric interface beats a generic tool loop (the ACI thesis)\nSWE-agent's core finding: **\"good ACI design fundamentally outweighs naive tool-call approaches\"** — a baseline agent with generic tools does *much* worse than the same model behind a tuned ACI. Concretely: **100-line file windows** with scroll/search (not `cat` the whole file), concise feedback (\"Your command ran successfully and did not produce any output\"), search that **lists matching files, not every match's context**.\n\n**Our loop is precisely the \"naive tool-call loop\"** the ACI paper says underperforms: generic `Read`/`Bash`, whole-file dumps, verbose results. We've been tuning the *model's* context window; the ACI thesis says the *interface we hand it* is the lever.\n\n### 4. Architect / editor separation — directly relevant to DS.0 leveling\naider's **architect mode**: a strong model \"resolves the coding task and provides plain-text instructions about which file changes need to be made\"; a separate **editor model \"interprets those instructions to produce the syntactically correct diff.\"** Planning and edit-serialization are *different jobs* given to *different models*.\n\nThis reframes our whole DS.0 problem. We're asking one 24B local model to do orient + plan + serialize-correct-edits in one ReAct stream. The architect/editor split says: let a capable planner emit prose intent, and use devstral **only** as the cheap editor that turns intent → diff. That may be the actual shape of \"leveling DS.0\" — not making devstral do everything, but giving it the one narrow job it *can* do.\n\n### 5. Edit-centric turn structure, not exploration-centric\naider's loop *shape*: map → (files added to chat) → **model emits edits** → apply + lint → git commit. Every turn is oriented toward *producing an edit*. Our loop is oriented toward *tool exploration*, with editing as merely one tool the model rarely chooses. Reading is the path of least resistance, so a weak model takes it forever.\n\n## The meta-thesis\n**The small model isn't the bottleneck — the interface is.** That's SWE-agent's entire result, and it explains why three sessions of context tuning didn't move the needle: we were optimizing the wrong layer. num_ctx/compaction/honest-halt are real fixes to real bugs, but they're *plumbing* around a loop whose *shape* is what stops a 24B model from editing.\n\n## Design-review questions this teed up (NOT decisions) — ordered by evidence\n1. **(lead / most robust) architect(strong)/editor(devstral) split** — since devstral never *plans* an edit, does a capable planner emit intent and devstral serve only as the cheap diff-editor? Helps both failure branches. This may be the real shape of \"leveling DS.0.\"\n2. **Repo map** (tree-sitter signature map + graph rank) as the orientation surface, replacing read-to-discover — kills the read-wander the logs show (47–102 Reads, 0 edits).\n3. **Edit-centric turn structure** — reshape the loop so each turn drives toward *producing an edit*, not exploring tools.\n4. **ACI for the minion tier** — is our generic `Read/Bash/Edit` surface (whole-file dumps, verbose feedback) the wrong interface for weak models? Windowed viewer + concise feedback for minions; keep rich tools for strong models.\n5. **(speculative) Edit-format contract + syntax/lint gate + discard-and-retry + `whole`-fallback** — earn this *after* orientation is fixed and we can observe whether devstral's diffs actually fail.\n6. **What we KEEP:** the bus/shim/device rack, escalation walk, and cost-router are orthogonal and sound. This is about the *inner loop + tool interface*, not the substrate.\n\n## Sources\n- aider edit formats — https://aider.chat/docs/more/edit-formats.html\n- aider repo map — https://aider.chat/docs/repomap.html ; tree-sitter repomap — https://aider.chat/2023/10/22/repomap.html\n- aider edit-errors (local/quantized → whole) — https://aider.chat/docs/troubleshooting/edit-errors.html\n- aider code-editing leaderboard (format conformance) — https://aider.chat/docs/leaderboards/edit.html\n- SWE-agent ACI — https://swe-agent.com/1.0/background/aci/ ; paper — https://arxiv.org/pdf/2405.15793\n- landscape — https://github.com/bradAGI/awesome-cli-coding-agents ; https://stoneforge.ai/blog/open-source-ai-coding-agents/\n"
  }
}
