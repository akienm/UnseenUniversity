{
  "id": "cc.0.T-reorg-repo-root-paths-broken.20260628.164026230067",
  "emitter": "cc.0",
  "namespace": [
    "T-reorg-repo-root-paths-broken"
  ],
  "category": "tickets",
  "kind": "ticket",
  "emitted_at": "2026-06-28T22:40:26.230067+00:00",
  "links": {
    "decisions": [
      "D-single-package-reorg-2026-06-28"
    ],
    "tickets": [
      "T-reorg-repo-root-paths-broken"
    ],
    "commits": [],
    "whys": []
  },
  "body": {
    "id": "T-reorg-repo-root-paths-broken",
    "title": "[triage] Fix repo-root paths broken by collapse: granny cc_queue, auditor devlab",
    "size": "M",
    "tags": [
      "Reorg",
      "Bug"
    ],
    "status": "triage",
    "priority": 0.55,
    "decision_id": "D-single-package-reorg-2026-06-28",
    "description": "**⛔ FAIL — KICKED BACK TO DESIGN 2026-07-13 (Akien: 'those should get sent back to the design step as FAIL for learning from and fixing').**\n\n**THIS TICKET ENTERED SPRINT WITH NO INTENTION.** Under D-intentions-are-the-root-2026-07-13 and proof-on-close, THE INTENTION IS THE PROPERTY MUTATION-RED BREAKS. With no intention there is **no property to break**, so this ticket **cannot be proven as it stands** — it could only ever close hollow, or `shipped-unproven` for a reason that is not the real one.\n\n**IT IS NOT DEFECTIVE WORK — IT IS INCOMPLETE DESIGN.** So the honest disposition is BACK TO DESIGN, not a backfilled intention. CC drafting the missing intention would have **papered over the real defect**: 150 tickets cleared the design step without an intention, which is a **gate hole**, not 150 individual oversights. Per CLAUDE.md, the nuisance stays VISIBLE so it drives finding the fix — never papered over, which sucks silently forever (CP4).\n\n**CP2 — FAIL = FURTHER ADVANCE IN LEARNING.** This kickback IS the learning signal. The fix is upstream: the triage→sprint transition must reject a missing, empty, or degenerate (`TBD`/`unknown`) intention (T-sprint-tickets-with-no-intention-cannot-be-proven).\n\n**TO RE-ENTER SPRINT:** state the intention as a present-tense contract — *'I intend that X, because Y'* — where X names a property a hollow build would violate. Akien declares it (`provenance_class: declared`); a machine-extracted one is a RATCHET, not a SPEC (D-provenance-is-the-safety-property-2026-07-13).\n\n---\n\n**Problem:** The single-import-root collapse moved devices INTO the package, so `Path(__file__).resolve().parents[2]` from a `unseen_university/devices/<dev>/x.py` file now resolves to the PACKAGE root (`unseen_university/`), not the REPO root. Files that named that anchor `_UU_ROOT` / `REPO_ROOT` and used it for REPO-root paths (`devlab/`, which lives at the true repo root, NOT in the package) are now silently broken. Verified 2026-06-28: `granny/daemon.py` `_CC_QUEUE = _UU_ROOT / 'devlab' / 'claudecode' / 'cc_queue.py'` -> `unseen_university/devlab/...` which does NOT exist (granny dispatches tickets via cc_queue.py — this is its core path); `auditor/device.py` `CHECKS_PATH = REPO_ROOT / 'devlab' / 'claudecode' / 'audit_checks.json'` -> nonexistent. (auditor's `REPO_ROOT / 'devices' / 'igor'` is coincidentally CORRECT now because devices moved into the package — do NOT 'fix' that one.) Config paths in these files were already migrated to uu_config_dir() under T-canonical-config-dir-resolver; this ticket is the REPO-root half.\n**Proposed shape:** Replace the repo-root `parents[N]` anchors with `uu_root()` from `unseen_university._uu_root` (the canonical repo-root resolver). granny: `_CC_QUEUE = Path(uu_root()) / 'devlab' / 'claudecode' / 'cc_queue.py'`; drop/repoint `_UU_ROOT`. auditor: `CHECKS_PATH` + any other `REPO_ROOT / 'devlab' / ...` -> `uu_root()`; KEEP the in-package `devices/igor` resolution correct (anchor it on the package, not repo root, so it stays right). Audit each `_UU_ROOT` / `REPO_ROOT` use in both files and classify per-use: repo-root path -> uu_root(); in-package path -> package anchor. This is the per-call-site discipline that the config-resolver ticket used.\n**Affected files:** unseen_university/devices/granny/daemon.py, unseen_university/devices/auditor/device.py (audit for any other files with the same conflated `parents[2]`-as-repo-root pattern — grep `parents\\[` for `devlab`/`devices`/`tests` uses).\n**Design rules:** single canonical resolver (uu_root() for repo root, uu_config_dir() for bundled config); per-call-site migration (don't blanket-swap — package-internal anchors must NOT become uu_root()); CP1 (verify each resolved path exists before/after).\n**Scope boundary:** IN — repo-root path anchors broken by the collapse in granny + auditor (+ any sibling found by grep). OUT — config paths (done in T-canonical-config-dir-resolver); the pre-existing granny rule-engine logic failures (separate, T-igor-shim sibling class); behavior changes to dispatch/audit logic.\n**Test plan:** (1) `python3 -c` asserting granny `_CC_QUEUE` and auditor `CHECKS_PATH` both `.exists()` after the fix. (2) A behavioral proof: a test asserting the resolved cc_queue path exists / is the real repo file (red with the broken parents[2] anchor -> green with uu_root()). (3) Re-run granny + auditor module tests; confirm no NEW failures vs the pre-existing baseline (granny rule-engine logic failures are pre-existing and out of scope).\n\n## Pre-computed constraints (auto-stamped at add time)\n[hard_block] ⛔ NO SQLITE. EVER. POSTGRES OR FLAT-FILE ONLY. See banner at top of this file. — applies: all (source: CLAUDE.md#hard-rules)\n[hard_block] Log every state change and every interface crossing. State changes: ticket status — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] No TheIgors imports. UnseenUniversity must be portable without TheIgors present. — applies: all (source: CLAUDE.md#hard-rules)\n[error] No live keys or passwords in source. `.env` is gitignored. — applies: all (source: CLAUDE.md#hard-rules)\n[error] `pip install -e .` must succeed at all times (even with empty stubs). — applies: all (source: CLAUDE.md#hard-rules)\n[error] Canonical dev-process memory — one home, no exceptions. Every dev-process artifact — — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] device.py and shim.py are the design center. Every component inherits from — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] bus/ owns comms:// routing. Nothing outside bus/ speaks to IMAP directly. — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] skeleton/ owns the MCP aggregator and flat-file registry. No Postgres dependency. — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] devices/ contains one subdirectory per device; each is independently deployable. — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] `~/.unseen_university/logs/<device>/<stream>/` is the canonical log hierarchy — — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n[error] Every ticket closes proven, or declares itself unproven — no discriminator. A ticket that — applies: tags: architecture (source: CLAUDE.md#structural-rules)\n…and 39 more (lower-severity or capped) — query constraints_get for the full set",
    "created_at": "2026-06-28T22:40:24.099172+00:00",
    "worker": null,
    "created_by": null,
    "result": null,
    "dispatched_at": null,
    "completed_at": null,
    "required_files": [],
    "related_to": null,
    "github_issue": null,
    "gate": null,
    "intention": null,
    "target_difficulty": 1,
    "role": "apprentice",
    "scraps_validated": "2026-06-28T22:40:24.148282+00:00",
    "inferred_intention": "unknown",
    "inferred_intention_id": "a844dfc9-db5f-41cf-a879-91acf22653b0",
    "fail_reason": "entered sprint with no intention — cannot be proven; design incomplete",
    "kicked_back_from": "sprint",
    "kicked_back_at": "2026-07-13"
  }
}
