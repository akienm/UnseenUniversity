# Slate — 2026-07-14

## Rewound? (read this if you were rewound, not compacted)
A rewind rolls your CONVERSATION back but NOT git/this slate — so your memory is now BEHIND reality. Read this slate + `git log --oneline -10`, trust slate+git over your memory, and `git status` for working-tree divergence (a code-rewind can delete committed files — `git restore` fixes it) before acting.

## Summary
(empty — session just started)

## Notes

## In-flight
NONE

## Planned

## Ad hoc

## Done today

## Session close → Next (2026-07-14) — ⚠️ THE LAST UU SLATE. READ THIS BLOCK, IGNORE EVERYTHING BELOW IT.
Done: THE PIVOT. Akien ratified a full restart: UnseenUniversity is RETIRED to read-only quarry
(like TheIgors). Successor: **Cairn** — ~/dev/src/cairn (MAP.md = the authoritative map, 12 commits
today) + ~/dev/src/CairnCommons (knowledge repo: Telos declared, first intention
I-cairn-claude-md declared). Ratified today: Telos (Akien's six) + sub-intentions v1 (T2 = the
demo domain for T1.5); 8 Laws; Form (device contract); 3 roots (repo/commons/~/.cairn);
graph-tree storage (trees-as-rows, truth never calves/caches may); workflow v0 (question nexus =
the primitive; 10-skill roster incl. /loadslate + /saveslate renames); CLAUDE.md discipline
(prose rule = IOU for enforcement, no negative-history rules); trouble tickets (base reflex);
journeys (forensic-first debugging); build spine (kernel chokepoints BEFORE first client);
crossing rule (nothing leaves the quarry without ticket + intention + proof).
In-flight: NONE. All UU work is permanently halted — do NOT resume UU tickets, do NOT trust UU
greens (the day's finding: both proxies bypassed at scale, records blind). The background test
run's result is moot.
Next: read ~/dev/src/cairn/MAP.md WHOLE, then build cairn/CLAUDE.md against the spec in
~/dev/src/CairnCommons/intentions/I-cairn-claude-md.md. Work happens in the cairn repos, never here.

## In-flight (STALE — superseded by the block above): CHOKEPOINT MEASUREMENT (the day's live finding, not yet ticketed): the db_proxy and the inference proxy are BOTH bypassed at scale. MEASURED: unseen_university/db_proxy.py (make_home_proxy/make_local_proxy/make_infra_proxy/make_dc_proxy — logs sql+elapsed_ms+owner, slow-query log, alarms) is used by only 20 shipped files, nearly all in the legacy devices/igor/ tree; 40 live-rack files call psycopg2.connect DIRECTLY (bus/pg_bus.py, inference/budget_ledger.py, intent/store.py, vault/store.py, web_server/server.py, channel.py, action_log.py, librarian/*, classifier, evaluator, scraps, nanny, archivist, hubert, build_digester, installer, auditor). Inference has TWO bypass levels: L1 = 6 live-rack files opening RAW HTTP to Hex, never entering InferenceDevice at all (aider/shim.py, summarizer/device.py, classifier/annotator.py, librarian/research.py, vetinari/device.py, librarian/tools/budget_tools.py) — NOT TICKETED, and this is why the io corpus and the 804 RunRecords are blind to the 187-364 kernel-counted Hex connections; L2 = 10 files that reach the proxy but skip the DOMAIN layer — already ticketed as T-route-all-inference-through-a-domain (0.8, status=dependency; one of its two gates, T-inference-escalation-policy-object, closed proven this morning). Akien's verdict: 'EVERYONE IS SUPPOSED TO USE THE DATABASE PROXY FOR EVERYTHING, JUST LIKE THE INFERENCE PROXY IS SUPPOSED TO BE THE ONLY PIPE FOR INFERENCE' — FAIL, CC--. Candidate lever (unratified): the tester now OWNS the network, so DENY on 5432/11434 inside the netns makes the chokepoint a KERNEL property, not a rule people remember — a bypasser cannot open the socket. Migration is drudge; enforcement is the ticket. ALSO IN FLIGHT: tester --uid 0 drop + Postgres FORWARD fix committed 88f839aa (both policy rules held: Hex REFUSED kernel-counted, PG CONNECTED via host-side forwarder); a full-suite run under the router is STILL RUNNING in background to land the pass/fail counts that confirm the 716 peer-auth failures were mine and are gone — the earlier run returned counts:{} (regex parsed nothing), so the number is NOT yet known and must not be reported until measured. NEXT (Akien's direction): start at the BOTTOM of the stack — devlab/runtime/memory. Notes go in /home/akien/.unseen_university/akien/inbox/20260709.CCReviewOfIntentionsOutline.txt (the markup master; one writer per file). Akien is feeding input — do not self-direct.
