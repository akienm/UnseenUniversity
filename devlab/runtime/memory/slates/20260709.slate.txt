# Slate 2026-07-09

## Notes
STANDING DIRECTIVE (Akien, 2026-07-08): "Everything else stops until inference works right every time."
Read D-domains-general-with-device-owned-specializations-2026-07-08 (+2 amendments) before touching inference.

Two hard-won rules from yesterday, both now memories:
- Run the consumer PAST the first assertion. An early assertion on the return ENVELOPE can hide that the consumer
  never did the work. Yesterday a model claimed "wrote smoke file" and wrote nothing; the test died on
  `.startswith("DONE:")` before ever reaching `smoke_file.exists()`.
- NEVER build a live device inside a proof node. A proof whose ctor probes real providers passes on the weather.
  Smell test: would this proof give the same verdict with the network unplugged?

## In-flight
NONE — 2026-07-08 closed clean. Tree clean, all pushed (HEAD 1b8eee39).

## Planned — inference, in dependency order (nothing else until these land)
1. ⛔ T-inference-tier-ladder-real (0.95, sprint) — BLOCKS EVERYTHING. The rungs are FAKE, verified:
     ticket_tier=builder -> devstral-small-2:24b@ollama
     ticket_tier=creator -> devstral-small-2:24b@ollama   (IDENTICAL)
     ticket_tier=master  -> None
   Five role rungs collapse into 3 difficulty buckets (builder+creator both -> 'code'); NO model is creator-tier;
   the registry vocabulary (minion/worker/analyst/designer) shares not one word with the role ladder
   (apprentice/builder/creator/master/guru). A model must declare WHICH RUNG'S STACK it can stand in. Do NOT paper
   over by re-adding curated triples — that curation is exactly the compensation we folded back unproven.
2. T-inference-typed-no-path-result (0.9, sprint) — resolve() returns NoCapableModel vs NoAvailableProvider.
   Today both collapse to None, so the domain GUESSES: it re-reads a capability ceiling as an availability outage,
   retries, then halts announcing "no live source". Fixes the CP3 bug + the triple-alarm at the root.
3. T-inference-escalation-policy-object (dep) — escalation becomes DATA: ladder + bump rule + terminal.
   DEFAULT lives on the GENERAL DOMAIN (not the proxy). escalation_allowed -> NoEscalation; harvest_mode -> Harvest.
   Proxy REPORTS, domain DECIDES.

Structure track (parallel-safe):
4. T-domains-global-package (0.8) — unseen_university/domains/; rename BaseDomain -> GeneralDomain (it is CONCRETE
   and IS the default). It owns: default EscalationPolicy + the COMPLETION CONTRACT + _run_attempt = ONE DISPATCH.
   Retire resolve_domain() (capabilities/base.py is its only caller left).
5. T-agentic-primitives-package (0.75) — unseen_university/agentic/. NOTE from audit: clone_commit.py and
   verdict_gate.py are imported ONLY by domains/architect_editor.py — they must follow the loop.
6. T-ds-coding-domain-specialization / T-aider-coding-domain-specialization (dep)
7. T-route-all-inference-through-a-domain (dep) — ~10 devices call InferenceDevice().dispatch directly today and
   therefore get ZERO escalation. Invariant: nothing reaches the proxy except through a domain.
8. T-compensation-record-and-graduation (dep) — AUDIT FINDING: the graduation apparatus is DEAD CODE.
   prereg.py and eval_slice.py have ZERO importers; corpus_verdict.py is imported only by orphaned prereg.py.
   That is the mechanical reason fold-back never happened automatically. Do not assume they work — they have no
   callers and therefore no evidence.

Also open: T-inference-single-device-instance-no-thread-per-call (0.85) — every InferenceDevice() spawns a probing
HealthMonitor thread; 18 construction sites; agentic_loop + architect_editor construct one PER CALL (5 -> 5 threads).
This is also how yesterday's proof passed on environment luck.

## Ad hoc

## Done today
- T-inference-escalation-eval-corpus — Escalation eval corpus — graduated queries with ground-truth verifiers; measures each model's real rung (12 graduated queries in 4 structural bands, each with a deterministic verifier and the plausible confabulation it must r)
- T-general-domain-chat-prototype — GeneralDomain — the prototype chatbot: concrete default domain, one dispatch per attempt (GeneralDomain is the concrete default domain ('' registered, not a fallback special-case); one attempt = ONE dispatch, n)
- T-escalation-corpus-false-negatives — Corpus verifiers reject CORRECT answers that carry a unit or natural phrasing (EvalQuery.accepts declares the phrasings a correct model actually emits; verify() compares against the accepted-forms SE)
- T-escalation-handoff-transmits-the-confabulation — The hop-1 handoff feeds the weak model's raw reasoning to the strong model, which adopts its wrong answer (Only the failed CLAIM crosses a rung — never the argument for it. _summarize_attempt hands over extract_answer() marked )

## Done today
- T-inference-escalation-eval-corpus — CLOSED PROVEN. 12 queries / 4 structural bands, deterministic
  verifiers, each carrying the confabulation it must reject. No LLM judge.
- T-general-domain-chat-prototype — CLOSED PROVEN. GeneralDomain is the concrete default domain
  (registered at '', not a fallback special-case). One attempt = ONE dispatch, no tools. Escalation
  walk INHERITED from BaseDomain (single money-safety owner).
- T-escalation-corpus-false-negatives — CLOSED PROVEN. Verifier rejected CORRECT answers
  ('ANSWER: 100 litres' vs truth '100'). First capability matrix was VOID.
- T-escalation-handoff-transmits-the-confabulation — CLOSED PROVEN + LIVE-VERIFIED.
  Only the failed CLAIM crosses a rung, never the argument for it.

- T-escalation-corpus-frontier-band — Corpus saturates at deepseek-r1:32b — add a frontier band, or capability rank has no evidence above the local box (b5_frontier added: 4 queries, every ground truth brute-forced before entering the corpus. Proven: proofs/cc.0.frontier-b)
- T-inference-cost-first-sort-strands-cloud-fleet — Cost-first sort makes 19/22 models unreachable — escalation can never leave the local box (Escalation can leave the local box. 'frontier' is the capability rung the box does not hold; claiming it requires capabi)
- T-repin-coding-prompt-sha256 — Repin coding-prompt SHA256 (stale since coding-prompt path fix 124553ee) (Re-pinned _CODING_PROMPT_SHA256 to 0e044ff7 (the intended post-124553ee text). Verified BEFORE re-pinning: 124553ee (Aki)
- T-preexisting-domain-prompt-sha — test_coding_prompt_byte_identical red — coding prompt drifted from pinned SHA256 (DUPLICATE of T-repin-coding-prompt-sha256 — same failing node (test_domain_prompts.py, the coding-prompt SHA pin), same )
- T-preexisting-repo-map-second-symbol — test_signature_map_surfaces_multiple_symbols red — repo-map drops a file's 2nd symbol (PROVEN, and the original red was a dead fixture rather than a regression. The old node patched psycopg2.connect and asse)
- T-graph-map-ingests-venvs — Repo graph map walked virtualenvs as first-party source — vendored code outranked the task's files (PROVEN. Virtualenvs detected by mechanism (pyvenv.cfg), not by name; site-packages/dist-packages skipped wherever an env)
- T-preexisting-dicksimnel-done-prefix-close — test_done_prefix_closes_ticket red — DS close now appends --shipped-unproven args (Test was stale, implementation was right. DS closes shipped-unproven with a named lever because it is a branch-builder: )
- T-inference-run-record — An inference request is its own JSON ticket — the whole escalation, readable at a glance (PROVEN (spine). Every inference request now leaves one JSON run ticket under uu_home()/inference_corpus/runs/<yyyymmdd>/)
- T-links-intentions-edge-kind — Restore the intention edge: add `intentions` to LINK_KINDS / _LINK_KEYS (PROVEN. links.goals was an EDGE into an entity; T-skills-goals-to-intentions removed it 2026-06-26 and never added a suc)
## ⭐ LIVE ESCALATION WORKS (first time, unpinned, on the box)
    hop 0 -> deepseek-r1:14b   check FAILED -> capability -> escalate
    hop 1 -> deepseek-r1:32b   check PASSED -> done
    VERIFIED ANSWER: "Both apples and oranges"   (b4-boxes, ground truth 'both')
  Evidence: notes/cc.0.escalation-live-verified-20260709.*.json

## ⛔ STILL BLOCKING (the reason "simple case first" was the right call)
  19 of 22 models are UNREACHABLE by dimensional routing (all 450 dimension combos).
  Only deepseek-r1:14b, deepseek-r1:32b, devstral-small-2:24b are ever selected — all local.
  cost_class_rank sorts FIRST and owned_local dominates every bucket it claims; difficulty_meets
  is a >= filter, so a cheap model claiming the top bucket wins every bucket beneath it.
  The selector REWARDS overclaiming and nothing verifies the claim.
  => T-inference-cost-first-sort-strands-cloud-fleet (0.95) BLOCKS T-inference-tier-ladder-real.

  Measured proof the label is a wish: llama3.2:3b claims 'classify' (the FLOOR) and clears
  b1 3/3 + b2 3/3. deepseek-r1:32b claims 'design'; gemini-2.0-flash also claims 'design'.

## Next
1. T-inference-cost-first-sort-strands-cloud-fleet (0.95) — separate CAPABILITY RANK (measured,
   ordered) from COST PREFERENCE (the tiebreak WITHIN a rung). Escalation walks capability; cost
   only chooses among stacks that clear it. Rank comes from escalation_matrix, not a hand-typed label.
   Do NOT re-add curated triples.
2. T-escalation-truncation-misclassified-as-capability (0.85) — a max_tokens truncation is
   classified CAPABILITY and spends up a tier. The bump cannot help: the stronger model is also a
   reasoning model. Needs a BUDGET class + bounded same-rung retry at a larger budget.
3. T-inference-tier-ladder-real (0.95, blocked by #1).
4. CodingDomain may need its own _summarize_attempt: its "answer" is a diff/test result, not a
   final claim. The rule still holds — only the claim crosses.

## Tools built (data, never assertions — they reach live providers)
  devlab/claudecode/escalation_matrix.py  — pin each model, run the corpus, emit the capability matrix
                                            (cost-gated: local by default, --include-subscription, --include-paid)
  devlab/claudecode/escalation_demo.py    — run ONE query live through the walk, unpinned


════════════════════════════════════════════════════════════════════════════
## Session close — 2026-07-09 (savestate before compact; ~420K ctx)
════════════════════════════════════════════════════════════════════════════

STANDING DIRECTIVE (Akien, 2026-07-08): "Everything else stops until inference works
right every time."

EPISTEMOLOGY (Akien, 2026-07-09, now in CC memory as `epistemology-experiment-only`):
  "This project starts with maximum uncertainty. NOTHING IN MY HEAD IS CERTAIN EXCEPT
   EXPERIMENTAL RESULTS. AND THEY ARE ONLY CERTAIN FOR THEIR EXACT CIRCUMSTANCES...
   even then it's a certainty of 90% conditional on the conditions at the time."
  => A result measured at max_tokens=4096 is a result about the model AT 4096.
  => Record conditions with every result. One sample at temp 0 is one sample.

## Done today (all pushed, all proven, tree clean)
- T-inference-escalation-eval-corpus        CLOSED PROVEN
- T-general-domain-chat-prototype           CLOSED PROVEN
- T-escalation-corpus-false-negatives       CLOSED PROVEN
- T-escalation-handoff-transmits-the-confabulation  CLOSED PROVEN + live-verified
- T-escalation-corpus-frontier-band         CLOSED PROVEN
- T-inference-cost-first-sort-strands-cloud-fleet   CLOSED PROVEN

## ⭐ LIVE RESULTS (unpinned, on the box)
  b4-boxes:  hop0 deepseek-r1:14b FAILED -> hop1 deepseek-r1:32b PASSED
             VERIFIED "Both apples and oranges"   <- first working escalation
  Resolver:  code -> deepseek-r1:14b @ollama
             design -> deepseek-r1:32b @ollama
             frontier -> deepseek-v3.1:671b-cloud @ollama_cloud   <- leaves the box
  Real-registry sweep: 4 distinct models / 2 sources (was 3 models / 1 source).

## ⚠️ FOUR INSTRUMENT BUGS FOUND — ALL by reading the RAW REPLY behind a verdict
  1. Verifier exact-equality rejected CORRECT answers ("ANSWER: 100 litres" vs "100").
     First capability matrix (32b < 14b) was VOID.
  2. The hop-1 handoff TRANSMITTED THE CONFABULATION upward. prior_attempt=text[:400] is
     the opening of a <think> block; injected as "**What was tried:**". Measured, one
     variable isolated: 32b answers b4-boxes correctly alone, adopts the 14b's wrong answer
     when handed it. Stripping <think> was NOT enough — deepseek-r1 restates its derivation
     in prose outside the block, and the 32b followed it even when told not to.
     RULE: only the failed CLAIM crosses a rung, never the argument for it.
  3. GeneralDomain timeout 120s < a 32b's 110-190s latency => every stronger-rung attempt
     read as AVAILABILITY, retried in place, exhausted. Raised to 300s.
  4. Matrix missed Gemini's truncation spelling (finish_reason="max_tokens", not "length")
     and scored truncated replies as WRONG ANSWERS.

## ⛔ OPEN CORRECTION — READ THIS BEFORE TRUSTING THE CAPABILITY RANKS
  T-capability-measured-at-a-budget-ceiling (0.95, sprint) — CAPABILITY and BUDGET are two
  axes and escalation_matrix conflated them. All b5 numbers were taken at max_tokens=4096
  (32b, 671b) or 16384 (gemini). Then deepseek-r1:14b PASSED b5-frobenius live at 8192 —
  the query the 32b "failed" at 4096. Different budgets => not a comparison.
    SURVIVES: 671b cleared b5 4/4 at the same budget where 32b failed one query.
              The resolver proof is structural and independent of this.
    DOES NOT SURVIVE: "deepseek-r1:32b is not frontier-capable" unconditionally, and the
              implied total order 14b < 32b < 671b.
  The evidence note is annotated in place (stale_flags: conditions-uncontrolled).
  Akien's design: measure capability at a SILLY-HIGH ceiling (nothing truncates), then walk
  max_tokens DOWN to find each model's token floor. Two phases, both recorded with conditions.

## Next (dependency-ordered)
1. T-capability-measured-at-a-budget-ceiling (0.95) — the measurement half. Sweep max_tokens
   as an axis; ceiling pass for capability, budget walk for the token floor; >1 sample; record
   conditions in every evidence string; re-derive capability_evidence; annotate what changes.
2. T-escalation-truncation-misclassified-as-capability (0.85) — the RUNTIME half of the same
   confusion: the walk classifies a truncation as CAPABILITY and spends up a tier for want of
   tokens. The bump cannot help (the stronger model is also a reasoning model). Needs a BUDGET
   class + bounded same-rung retry at a larger budget.
3. T-inference-activate-coding-tools-policy — coding escalation is STILL ONE RUNG
   (coding-needs-tools requires features=['tools']; no design/frontier coding model declares
   it). Pinned by test_the_coding_ladder_still_tops_out_and_that_is_not_hidden. Do NOT set the
   tools flag on a hunch — measure the model's tool-calling first.
4. T-inference-tier-ladder-real (0.95) — reconcile the two tier vocabularies (ROLE_TIERS vs
   registry TIERS). Partly relieved by the `frontier` rung but not closed.
5. CodingDomain needs its own `_summarize_attempt`: its "answer" is a diff/test result, not a
   final claim. Today the shared default tells the next rung that "Tests still fail:
   AssertionError in test_foo" was a wrong ANSWER not to repeat. Pinned by a test.

## Tools (DATA, never assertions — they reach live providers)
  devlab/claudecode/escalation_matrix.py  pin each model, run the corpus, emit the matrix
                                          (--bands, --models, --max-tokens; cost-gated:
                                          local default, --include-subscription, --include-paid)
  devlab/claudecode/escalation_demo.py    run ONE query live through the walk, unpinned

## Key files
  unseen_university/devices/inference/domains/general.py           GeneralDomain (the default)
  unseen_university/devices/inference/domains/escalation_corpus.py 16 queries / 5 bands
  unseen_university/devices/inference/domains/reply_text.py        strip_reasoning/extract_answer
  unseen_university/devices/inference/domains/base.py              _summarize_attempt (the seam)
  unseen_university/devices/inference/routing_buckets.py           DIFFICULTY_BUCKETS + frontier
  tests/inference/test_capability_evidence.py                      top rung requires MEASURED
  tests/inference/test_escalation_leaves_the_local_box.py          the cost-first proof

## Suite state
  tests/inference/ + test_capability_mixin: 488 passed, 4 FAILED (all pre-existing & ticketed):
    test_dicksimnel::test_done_prefix_closes_ticket
    test_dicksimnel_live_smoke::test_dicksimnel_builds_on_hex
    test_domain_prompts::test_coding_prompt_byte_identical_to_prior_ds_prompt
    test_repo_map_orientation::test_signature_map_surfaces_multiple_symbols_per_file

## Background job left running (safe to kill)
  escalation_matrix --bands b5_frontier --models deepseek-r1:14b deepseek-r1:32b
    --max-tokens 8192   -> /tmp/.../scratchpad/b5fair.txt
  This is the FAIR-BUDGET comparison. Its result feeds ticket #1. If lost, just re-run.
  FINAL RESULT (fair budget, both at max_tokens=8192, band b5_frontier):
    deepseek-r1:14b  knights PASS  modular PASS  frobenius TRUNC  digitcount PASS   (no verdict)
    deepseek-r1:32b  knights PASS  modular PASS  frobenius fail   digitcount PASS   (finish=stop)
  STRENGTHENS the shipped claim: the 32b fails b5-frobenius at BOTH 4096 and 8192, terminating
  cleanly each time => a genuine capability failure, not a budget artifact. 671b remains the only
  model measured to clear the band. The registry entry is better-founded than when it was written.
  STILL UNSUPPORTED: the ordering 14b < 32b — the 14b has NO VERDICT on the separating query.
  ...yet the SAME model at the SAME 8192 budget PASSED b5-frobenius in the live demo minutes
  earlier. So at 8192 the 14b is ON THE TRUNCATION BOUNDARY for that query and its verdict is
  not reproducible. A capability label read off a budget-bound run is noise. This is exactly
  Akien's point: measure capability at a ceiling where nothing truncates, THEN walk the budget
  down. It also means the b5-frobenius "failures" must all be re-taken at the ceiling.

## Session close: 2026-07-09 (afternoon) — capability is not a ladder; the corpus does not discriminate

### Done (all committed + pushed)

TEST DEBT — the four standing reds are gone. Two were REAL BUGS, not stale tests.
- T-graph-map-ingests-venvs (NEW, filed+closed PROVEN, 5fc62692): `repo_graph_map._gather_py_files`
  skipped dirs by NAME while its docstring promised it skipped "hidden/venv/site dirs". This repo's
  gitignored venv is `test_env/` — 2173 .py files — so EVERY DickSimnel orientation packet led with
  pip's vendored typing_extensions, ranked above the task's own files. Fix: detect a venv by the
  `pyvenv.cfg` the interpreter writes, not by guessing its name; skip site-packages/dist-packages;
  match relative to repo_root (the old skip matched the ABSOLUTE path, so a checkout under any dir
  named build/dist/venv would have blanked its whole map). Live map now leads with bus/envelope.py.
- T-preexisting-repo-map-second-symbol (closed PROVEN, same commit): the old proof node COULD NOT be
  made green honestly. It patched psycopg2 and asserted a fabricated symbol `helper_fn` came back
  from the symbols table, but build_signature_map now calls build_graph_map FIRST and returns early;
  the fake cursor was never consulted. Its red meant "the fixture no longer reaches the code".
- T-repin-coding-prompt-sha256 + duplicate T-preexisting-domain-prompt-sha (closed shipped-unproven,
  e2eb667f): stale SHA pin. Verified BEFORE re-pinning — 124553ee (Akien's own path-strip fix) is the
  only content change; ea74a6a0 was a 0-insertion rename. Added the property test the hash cannot
  make: the prompt must never hand DS an absolute path (measured: 6 hits on pre-fix text, 0 on HEAD).
- T-preexisting-dicksimnel-done-prefix-close (closed shipped-unproven, 9fdfad27): test was stale, impl
  right (DS closes shipped-unproven because a branch-builder cannot emit a HEAD-valid proof; verified
  emit_merge_proof exists). Removed a HOLLOW assertion: `assert_called_once_with(..., mock_cmd.call_args[0][2])`
  read the expected note out of the actual call and could never fail.
- T-proof-form-for-repinned-tests (NEW, triage): the SAME missing lever named twice in one session —
  a test re-pinned onto intended behaviour has no red form a hollow re-pin would fail. Lever 3
  (property-first pinning) looks strongest.

T-inference-run-record (Akien's ask; closed shipped-unproven — SPINE proven, 14ed9c35 / stub b0f3f33f)
- "an inference request is its own kind of json ticket ... the whole conversation of the escalation
  you can read at a glance." Built: unseen_university/devices/inference/run_record.py; BaseDomain.run
  is now a thin recorder around _escalation_walk, writing in a `finally` so a halt / cost cap / harvest
  wall / unexpected raise ALL leave a record (the failures are the runs worth reading). All six exits
  name their resolution. `_decorate_record` is the specialty hook. LoopResult gained model/source_kind.
  Payloads REFERENCED not duplicated (question excerpted 500ch; hop carries the extracted CLAIM, never
  the reasoning). 30-day expires_at stamped. Location approved by Akien: uu_home()/inference_corpus/runs/.
- Proof: tests/inference/test_run_record.py, red=AssertionError -> green=passed. Discriminators: a HALTED
  walk must also leave a record; an availability failure must never look like a capability failure.
- NAMED GAPS -> T-run-record-io-join (run_id stamped on io records; one hop is NOT one dispatch, so a
  single io_ref per hop is wrong by construction), T-run-record-coding-domain-model-blank (CodingDomain
  hops carry model='' — blind exactly where the background-builder milestone needs sight),
  T-run-record-retention-sweeper (expires_at is a field nothing reads).

THE OBSERVABILITY LEVER (Akien: "the fact it didn't show up when you were looking is an important lever")
- CC claimed "we have no captured truncated replies" and FILED A TICKET on it. FALSE. io_corpus had
  captured every byte of all 1948 calls in ~/.unseen_university/inference_corpus/. CC searched the memory
  store. The capture was never the problem; FINDABILITY was.
- Surfaced T-io-corpus-text-drops-reasoning (0.8): ollama returns deepseek-r1's reasoning in
  `message.thinking`, not `message.content`. `response.text` normalises from content only, so 19216 chars
  of reasoning are invisible to every consumer, and `_unclosed_think(text)` CAN NEVER FIRE — truncation
  detection has rested on finish_reason alone, by luck.
- NOT a 30%-broken-records bug (CC's first read): 1355 ok calls / 186 error calls (raw-only, legitimately
  empty) / 373 NON-CALL side events sharing schema `inference.io.v1` with no `kind` field.

DEGENERACY: MEASURED, NOT BUILT (note: degeneracy-not-observed-20260709)
- Akien asked whether models truly hang; correctly reasoned a hang must live OUTSIDE the NN (the forward
  pass is fixed compute and always halts; non-termination lives in the autoregressive loop).
- 226 reasoning traces, 6-gram tail unique-ratio: truncated (n=21) min 0.965; completed (n=205) min 0.653.
  Repetition does NOT track truncation. The MOST repetitive trace COMPLETED. ZERO degenerate cases.
- So the proposed DEGENERACY outcome class has no positive instance to calibrate against. Narrowed
  T-truncation-vs-degeneracy-third-outcome to INSTRUMENTATION (record the ratio; recognise a positive
  when it appears). Strengthened by temperature 0 being the regime where degeneracy is MOST likely.
- Worked example: 14b on b5-frobenius @8192 spends all 8192 tokens inside `thinking`, content empty,
  tail ratio 0.972, cut off mid-sentence at "c=1:" while enumerating 52, 53... At 32768 it answers 43.


### The measurement (the day's actual result)

CAPABILITY, at a NON-BINDING 32768 ceiling, 2 samples/cell, temp 0, timeout 600s.
Note: notes/capability-ceiling-sweep (REBUILT from io_corpus — see below).

  model                      b1   b2   b3   b4   b5    trunc  err
  deepseek-r1:14b   ($0)    3/3  3/3  3/3  2/3  4/4      0     0    fails b4-boxes
  deepseek-r1:32b   ($0)    3/3  3/3  3/3  3/3  3/4      0     0    fails b5-frobenius
  deepseek-v3.1:671b-cloud  3/3  3/3  3/3  3/3  3/4      0     1    b5-frobenius HUNG
  llama3.2:3b       ($0)    3/3  3/3  1/3  2/3  0/4      0     0

1. CAPABILITY IS NOT A LADDER. The 32b's set is NOT a superset of the 14b's: the 14b passes
   b5-frobenius (2/2) which the 32b fails at 4096, 8192 AND 32768, clean stop each time. One
   counterexample kills the universal `>=` that routing_buckets.difficulty_meets asserts.
   The reverse direction is NOT established (the 32b's only edge is b4-boxes, which a 3b also
   passes — see 2). Memory: feedback_capability_is_not_a_ladder.

2. THE CORPUS DOES NOT DISCRIMINATE. b5 queries are named textbook problems (Chicken McNugget
   6/9/20->43, knights-and-knaves, CRT, stars-and-bars) — a $0 14b clears the whole band.
   b4-boxes leaks its answer: the token "both" appears TWICE in the prompt as the label of the
   box you draw from; answer space is {apples,oranges,both}; 3b PASS, 14b fail, 32b PASS —
   non-monotone in size, i.e. ANTI-correlated with capability. I designed by "what SHAPE of
   reasoning?" and never asked "how could a model be right WITHOUT reasoning?"
   Memory: feedback_eval_queries_must_not_leak. (I then reproduced the same defect in a test
   fixture: REASONING contained "43", so a hop-0 reply answering 41 passed a `"43" in text` check.)

3. THE 671b HANGS ON b5-frobenius, 40% (6/15 calls). ZERO hangs in 30 calls across the other 15
   queries. Query-specific non-termination, at both budgets, consuming the full timeout on a call
   it otherwise completes in ~240s. A timeout captures NOTHING (raw=null) — so the one place a
   degenerate loop could hide is the one place we cannot look.
   Note: notes/671b-hangs-frontier-unearned-20260709.

4. max_tokens does NOT determine the answer. 4096: 43,43,43 (+probe 43) = 4/4. 32768: 43,43,37
   (+probe 37) = 2/4. Suggestive, NOT significant (Fisher p~0.43). A single sample at each budget
   nearly became "max_tokens is semantic at temperature 0". One sample is a coin.

5. TEMPERATURE 0 IS NOT DETERMINISTIC. llama3.2:3b scored PASS then fail on b5-knights, same
   prompt, same budget. GPU float reduction order is not associative; near-ties flip the argmax.
   samples>=2 caught it and _aggregate records UNSTABLE rather than majority-voting a pass.

6. THE TIMEOUT IS A CONDITION. It was never recorded. Two 600s ERRs looked ceiling-induced; I wrote
   that into a code comment as fact and committed it. A controlled probe (timeout held at 1800s,
   only max_tokens varied) refuted it — the model finishes at BOTH 4096 (260s) and 32768 (195s).
   Retracted in 512ca1ba. `timeout_s` now lands in the note's conditions; the guard rejects a
   measured claim from a run where that model ERRORED at the ceiling.

## Next — THE DECISION AKIEN MUST TAKE (do not let CC pick)

  `frontier` HAS NO MEASURED HOLDER. The bucket was added (T-inference-cost-first-sort-strands-
  cloud-fleet) so escalation could leave the local box, ASSUMING the cloud model was more capable.
  Measured: it is not — not on this corpus, and on the one separating query it cannot reliably
  even return. The guard correctly refuses the claim on two independent grounds.

  (a) Drop the 671b to `declared`; accept an EMPTY top bucket. Escalation's last hop then always
      halts. test_the_top_bucket_is_not_empty goes red, correctly.
  (b) Remove `frontier`; revert to 3 buckets. Re-opens "escalation never leaves the box" — but we
      have no evidence that leaving it helps.
  (c) REBUILD THE CORPUS so the top band discriminates: GENERATE queries (parameterise a family,
      brute-force ground truth in code), answer token absent from the prompt, wide answer space.
      `6,9,20->43` is memorisable; `7,11,24->?` is the same reasoning with nothing to recall.
      Only option that produces NEW information rather than re-labelling old.
  (d) Investigate the hang first — a top rung that returns nothing 40% of the time is unusable
      whatever its capability.

  CC's read: (c) then (d). But this is a D-level call.

## Working tree at compact (DELIBERATELY UNCOMMITTED)

  unseen_university/devices/inference/capability_evidence.py   NEW — CapabilityEvidence value object
  unseen_university/devices/inference/models_registry.py       evidence: str -> CapabilityEvidence
  tests/inference/test_capability_evidence.py                  the guard (resolves the note, reads
                                                               numbers back; un-fakeable by design)
  tests/inference/test_escalation_leaves_the_local_box.py      \_ consumers updated
  tests/inference/test_hex_registration.py                     /
  devlab/claudecode/escalation_matrix.py                       committed already (a8e10ae9/512ca1ba)

  Guard state: 2 RED, both naming deepseek-v3.1:671b-cloud — (i) 1 cell errored at the ceiling, so
  no verdict is licensed; (ii) claims 'frontier' but scored 3/4 on b5_frontier. The other three
  models pass their conditions cleanly. THE REDS ARE THE FINDING. Do not make them green by
  editing the registry — that is the hollow-build move this whole ticket exists to prevent.
  Resolve them by taking the decision above.

## Also today — the clones (Akien: "a whole folder of hidden stuff")

  Removed 16 paths, 441MB. ~/.unseen_university 553MB -> 146MB. Nothing lost, VERIFIED: every file
  of every type in every clone was either identical to a current canonical file, or its git blob
  sha1 was found among ALL 16135 blobs ever committed. A filename-only diff (my first check) was
  the WRONG check — slates are .txt. Preserved to ~/TheIgorsProject/runtime/memory/
  aider_unmerged_20260709/ (92KB + MANIFEST): 4 unmerged aider commits.

  IT UNCOVERED A HOLLOW BUILD THE GATE PASSED. T-reorg-parents-path-sweep is CLOSED, "gate PASS,
  tests=green". The builder (qwen3-coder:30b) added three 0-BYTE files at pre-reorg paths that do
  not exist, touched nothing real. Patch line-count: +0/-0. The ticket carried `intention: None` —
  with no "I intend that..." there was nothing for the gate to check the build AGAINST, so "tests
  green" was the only signal, and "tests green" is satisfied by doing nothing.
  -> T-aider-gate-passed-a-hollow-build (0.95)

  THE BUG IT CLAIMED TO FIX IS LIVE AT 7 SITES. parents[2] from unseen_university/devices/X/f.py
  is the PACKAGE dir, not the repo root (that is parents[3]). Two OTHER sites are accidentally
  correct because they append "devices" — which is why a skim misses it.
  -> T-reorg-parents-sweep-still-live (0.8)

  memory_emit derives _REPO from __file__, so a future clone still forks the store.
  -> T-aider-work-clones-hidden-state (0.6)

  Stale: an aider device daemon has run since 2026-07-07 out of a dead CC session's job dir.
  Idle, harmless, unknown to anyone. Left running; flagged.

## Also today

  - T-inference-run-record: SPINE PROVEN (14ed9c35). Every inference request now leaves one JSON
    ticket under uu_home()/inference_corpus/runs/. Written in a `finally` so halts leave records.
    Gaps ticketed: T-run-record-io-join, -coding-domain-model-blank, -retention-sweeper.
  - 4 standing test reds cleared; 2 were REAL bugs (graph map ingested a 2173-file venv and ranked
    pip's typing_extensions above the task's files in EVERY DickSimnel orientation).
  - Sweep killed at 16:09 during its low-value budget-walk phase (671b walk = ~60 calls against a
    provider that hangs 40% on the walk's hardest query). Capability data intact in io_corpus;
    note REBUILT from it. 14b token floors survived: b4-schedule 1024, b5-digitcount 1024,
    b5-modular 2048, b4-pens 4096, b5-knights 8192, b5-frobenius 8192 — an 8x per-query spread.

Next: 1. THE FRONTIER DECISION (above). 2. T-aider-gate-passed-a-hollow-build (0.95).
      3. T-reorg-parents-sweep-still-live (0.8). 4. T-io-corpus-text-drops-reasoning (0.8).
- T-links-intentions-edge-kind: Restore the intention edge (LINK_KINDS lost goals 2026-06-26, never got intentions)
- T-intention-contradiction-gate: intentions compete; contradiction HALTS until reconciled (gated on store+edge)
- Intention chain priority inversion corrected: intentions-store .55->.95, extract-from-chatlogs .5->.95,
  sorted-intention-map .5->.9, audit-ticket-atomicity .45->.85, sorted-decompose .5->.85, intent-extractor .25->.8
- Capture pass 1: akien/inbox/20260709.IntentionsHarvest.txt (121 of Akien's typed statements, 06-01 on)
- Capture pass 2: akien/inbox/20260709.IntentionsByDevice.txt (565 store-held intentions, 34 devices)
- T-links-intentions-edge-kind: PROVEN, closed (bcb28a75) — links.intentions restored
- Intentions outline redraft -> akien/inbox/20260709.CCReviewOfIntentionsOutline.txt (present-tense contract voice; 43 contract holes; 6 conflicts to reconcile; 18 [CC] lines to ratify or cut)
- T-proofs-carry-conditions-and-decay: proofs record conditions; validity_sweep demotes to provisional (retroactive proven/unproven)
- T-mutation-red-retroactive-proof: red from mutation not git history -> late proofs possible + echo-tests detected
- T-rename-lever-to-proof-method: "lever" is a metaphor of force -> it made CC weld "backlog earns removal" onto "corpus earns removal". Define proof/proof-method/proof-corpus in rules/.
- T-claude-md-documents-dead-spawn-path: CLAUDE.md:180 documents _launch_builder (definition gone)
- T-cc-queue-next-is-a-claim-primitive: `never claim` held by prose, not by the tool surface
