# Slate 2026-07-08

## Done today (9 closed proven + 1 design + 1 reframe)
- T-rules-store-materialize @2950bebb — 14 rule artifacts materialized (rules_citation_check.py 15/15)
- T-builder-memory-repo-residency @8ebe84c8 — 146 memories triaged, 72 mirrored to rules/+notes/
- T-filing-audits-independent-examiner — DESIGN HALF @c4deb58f (architecture/examiner-station); build gated on T-audit-run-records-enforced
- T-ungate-status-flip @581ae3a6 — ungate flips dependency->sprint
- T-claude-md-stale-anchors @ec86eda8 — core_values anchor fixed + claude_md_anchor_check.py
- T-consequence-ticket-aging-alarm @698bda08 — day-close overdue-consequence alarm
- T-produced-by-emission-sweep @670b06fd — produced_by at the 3 emission chokepoints
- T-validity-sweep-day-close @ba8165fd (+dedup fix) — day-close validity sweep, annotate-never-delete
- T-audit-run-records-enforced — REFRAMED+GATED: found audit_telemetry on the retired Postgres palace; filed prerequisite T-audit-telemetry-flatfile-repoint
- Filed follow-ups: T-audit-telemetry-flatfile-repoint (M), T-proof-emitter-test-store-isolation (S)
- Day-close: gate sweep cleared 4 consequence gates; validity sweep flagged=2 (probeless facts); path-moves clean; collection clean (4490); critical suites 70 green. Full-suite infra errors = known T-test-suite-hermeticity-infra-deps.

- T-granny-dispatch-observability-gap — Granny dispatch health is invisible — a drained/idle builder + all-defer cycles surface nothing an operator would notice (--reason)
- T-collapse-daemons-to-ground-loop — One daemon structure (Ground Loop) — collapse standalone granny/scraps daemons; no device runs its own __main__ loop (Collapsed granny+scraps standalone daemons onto shim-owned in-process loop threads. No device runs its own __main__+whil)
- T-routing-compiler-rules-engine-test-red — scraps test_routing_compiler::test_rules_engine_add_compiled_rule red (inference rules engine) (Fixed the red test. Root cause was two-fold: (1) RulesEngine.__init__ used 'rules or _DEFAULT_RULES' so an explicit empt)
- T-granny-dispatch-tests-preexisting-failures — Pre-existing granny dispatch/cascade/e2e test failures (env-dependent) (Fixed all 13 env-dependent granny dispatch reds with a shared autouse tests/granny/conftest.py fixture. TICKET ROOT-CAUS)
- T-inference-connections-stack — Inference router: introduce the model<->provider connections stack (additive) (Added the model<->provider connections stack (stack 3 of 4, D-inference-router-stack-decomposition-2026-07-08). Connecti)
- T-inference-dimensions-contract — Inference router: define the caller dimension contract {ticket_tier, builder_tier, domain, urgency, escalation_allowed} (Added the dimensional caller contract: RouteRequest {ticket_tier, builder_tier, domain, urgency, escalation_allowed=defa)
- T-inference-rules-as-policy — Inference router: express rules as dimension->capability-envelope policy (no model/provider literals) (Added the rules-as-policy stack (stack 4 of 4). PolicyRule matches dimension predicates (domain/ticket_tier/builder_tier)
- T-inference-resolver-compose — Inference router: compose the 4 stacks into a dimensional resolver (alongside route()) (CLOSED PROVEN (proof eafe7d79, red=AssertionError->green=passed). resolve(RouteRequest) composes the 4 stacks beside rou)
- T-inference-migrate-consumers-cutover — Inference router: migrate consumers to resolve(); retire (tier->model->provider) triples + ModelSpec.source_name (CLOSED PROVEN (proof cc.0.inference-router-cutover-every-consumer @441076bf, red=AssertionError -> green=passed). Every )
- T-inference-router-decomposition-invariant-proof — Inference router: runnable decomposition invariant — add a connection, zero rule edits, resolve returns it (CLOSED PROVEN (proof cc.0.the-router-decomposition-invariant-addin @240ad99f, red=AssertionError -> green=passed). The r)
## Planned
- Fable judgment block (per 07-07 hand-off): T-rules-store-materialize, T-builder-memory-repo-residency, T-filing-audits-independent-examiner (design half; build gated on T-audit-run-records-enforced)

## Ad hoc
- CLOSED T-rules-store-materialize (PROVEN, red=AssertionError green=passed @2950bebb): 14 rule artifacts in rules/ via memory_emit (statement+why, produced_by, validity_conditions where real); rules_citation_check.py citations=15 unresolved=0; cat rules/*safeguards* now returns the authoritative inertia list; proof pinned in tests/test_rules_citations_resolve.py. Q1 OPEN for Akien: confirm/amend the HIGH-inertia list in the safeguards artifact.

- CLOSED T-builder-memory-repo-residency (PROVEN @8ebe84c8): 146 memories triaged whole-corpus (72 mirrored / 44 already-durable / 30 stay-local); 4 new rules (dispatch, autonomy, git-workflow, design-doctrine) + safeguards Igor-live conditionality + preferred_paths entries + notes/builder-gotchas + notes/device-roster + triage table notes/builder-memory-triage.20260708.md. No local deletions. Follow-on Q (noted, not built): automation of future local->repo sync.

- DESIGN HALF DONE T-filing-audits-independent-examiner -> architecture/cc.0.examiner-station.20260708.063926739676.json: examine verb on the EXISTING evaluator judge-panel (no new device), identity-stamped verdict + run-record (examiner != author, checkable), UNEXAMINED loud fallback (never silent self-audit), refutation-framed single examiner w/ 3-voter panel escalation for HIGH-inertia/L+, G3d constraint curation, 5-step build order. Q1 open on the ticket: proof-review verdict gate-vs-annotate (recommend annotate-first calibration window). Ticket stays DEPENDENCY-gated on T-audit-run-records-enforced — build half untouched.

## Ad hoc (Opus mechanical build-out — same session, role-swapped to builder)
- CLOSED T-ungate-status-flip (PROVEN @581ae3a6): _flip_ungated_to_sprint wired into cmd_ungate (manual) + _ungate_dependents (on close) — clearing a ticket's last dependency flips dependency->sprint so it's claimable in one step; hold + multi-predecessor gates untouched; state change logged. 4 tests, adjacent cc_queue suites green.
- CLOSED T-claude-md-stale-anchors (PROVEN @ec86eda8): new claude_md_anchor_check.py sweeps CLAUDE.md path anchors (6); fixed the one genuine stale one (diagnostic_base/core_values.py -> unseen_university/diagnostic_base/, F3); announce/listener.py verified correct. Checker green anchors=6 stale=0; 3 tests.
- CLOSED T-consequence-ticket-aging-alarm (PROVEN @698bda08): new consequence_aging.py (zero-inference, over gate_logic) lists T-consequence-* whose gate fully cleared but sit non-terminal, aged + >=7d ESCALATE; wired as day-close-audit Step 18.9 (calm signal). LIVE: 4 genuinely overdue (due 2026-07-07) — canonical-memory-consolidation, inference-redesign, skills-two-products, system-alarms-tier-requests. 4 hermetic tests.

## Ad hoc (Opus M-build-out — Akien cleared HIGH-inertia: "no high inertia when Igor isn't running, he's on ice, go go go")
- CLOSED T-produced-by-emission-sweep (PROVEN @670b06fd): produced_by stamped at all 3 chokepoints — memory_emit.emit (param + session:<emitter> default + --produced-by CLI), ticket_store._envelope (derives from decision_id), proof_emitter (proven ticket). Additive; legacy artifacts still read. /sorted gains --produced-by. Proof self-demonstrates (its own produced_by=the ticket). Filed T-proof-emitter-test-store-isolation (test debris found: test_proof_emitter leaks fixtures into real store).
- CLOSED T-validity-sweep-day-close (PROVEN @ba8165fd): new validity_sweep.py — resolves depends-on-path/artifact/fact, annotates stale_flags IN PLACE (never deletes), prints flagged/checked/unresolvable; wired day-close Step 4.7; /sorted prompts conditions at filing. LIVE dry-run flagged=2 checked=7 unresolvable=2.
- REFRAMED+GATED T-audit-run-records-enforced: DISCOVERY — audit_telemetry.py (the run-record emit/read module, 7 skills import it) writes to the RETIRED Postgres memory_palace, not the flat-file store (read_runs returns 0; needs Postgres up; context-load says 'no memory_palace'). Teeth can't gate on a grep-able flat-file record until the module is repointed. Filed prerequisite T-audit-telemetry-flatfile-repoint (M, repoint Postgres->flat-file preserving the API), gated the enforced ticket on it. This is spec-quality data — the ticket under-specified that its substrate points at the dead palace.

## In-flight
NONE — 9 tickets closed proven this session (3 Fable + 6 Opus) + 1 design half + 1 reframe/prerequisite-filed. Tree clean, pushed HEAD.

## Session close 2026-07-08 (Fable judgment block -> Opus build-out)
**Done (Fable judgment):** T-rules-store-materialize (@2950bebb), T-builder-memory-repo-residency (@8ebe84c8), T-filing-audits-independent-examiner design half (architecture/examiner-station).
**Done (Opus, all PROVEN red->green):** T-ungate-status-flip (@581ae3a6), T-claude-md-stale-anchors (@ec86eda8), T-consequence-ticket-aging-alarm (@698bda08), T-produced-by-emission-sweep (@670b06fd), T-validity-sweep-day-close (@ba8165fd).
**Next — FRESH OPUS SESSION:** (1) T-audit-telemetry-flatfile-repoint (M — the newly-found prerequisite; repoint audit_telemetry Postgres->flat-file, preserve API, 7 consuming skills) — this UNGATES T-audit-run-records-enforced which UNGATES the examiner T-filing-audits-independent-examiner (a 3-deep chain). (2) T-escalation-dispatch-producer-review (M). (3) S: T-store-layout-reconcile (move-vs-amend design call), T-unproven-lever-hygiene (cc_queue close-path soft-validation + day-close aggregation), T-proof-emitter-test-store-isolation (test hygiene). NOT buildable: T-aider-conformance-populate-from-replay (gated 2026-07-21).
**Calendar:** /outcome D-fable-window-altitude-agenda at Friday retro (2026-07-10); 3 D-fable consequence tickets fire 2026-07-21. 4 overdue consequence tickets (now surfaced by day-close Step 18.9) want /outcome checks at next day-close.
**HIGH-inertia:** Akien confirmed 2026-07-08 — no HIGH-inertia gate while Igor is down (he's on ice); rules/safeguards conditionality clause is confirmed-correct. Q1 (safeguards list) resolved.

## Post-day-close continuation (compact @~500K — RESUME HERE next session)
**Day-close 2026-07-08 DONE:** slate closed, Discussion #24 + slate comment posted, roll-up note written, docs committed+pushed (@e5b3b268), gate sweep (4 consequence gates cleared), validity sweep applied, path-moves clean, collection clean (4490), critical suites 70 green. Wednesday — no Friday steps. Tree clean, HEAD pushed.

**Granny/Aider health check (this box = akiendelllinux, I'm cc_RESCUE):**
- Granny daemon ALIVE (pid 3348294, ~16h), cycling — but 217/217 recent dispatch attempts target=CC.0, ALL defer reason=unavailable (CC.0=me, flag .false). Aider.0 flagged available but IDLE since 2026-07-07 17:26 (last built+escalated T-memory-emit-frozen-root-post-import). 120 ungated apprentice sprint tickets exist but aren't builder-TARGETED so Granny never routes them to Aider -> offload loop effectively stalled, nothing signals it.
- Akien's point: Granny logs inadequately — this would've shown immediately. FILED T-granny-dispatch-observability-gap (M, pri 0.6): health line + fix the STALE granny_weatherwax.log (last write Jun 3; live daemon logs only to the tmux pane).
- ⛔ CORRECTION 2026-07-08 (post-compact, Akien): THERE IS NO BRANCH PILE. The "9 shipped-unproven + 12 aider_work + 4 escalations awaiting MERGE" was a CONFABULATION written at ~500K under compact pressure (feedback_complexity_limit_confabulation). Ground truth from Akien: one programmer (CC) on all files, everything done on main — no branches ever existed. Git confirms: only main/origin/main, no worktrees, no stashes. The merger station has NO present-tense work; it stays north-star/gated (D-dev-pipeline-stations) — its reason to exist (merge independent builders' branches under separation-of-powers) requires multiple producing builders, which we don't have yet.

**NEXT (sort sprints with Akien after compact — budget: 55% weekly used, 3 days left, favor high-leverage):**
1. ~~MERGER STATION~~ RETRACTED — premise was a confabulated branch pile (see correction above). Merger station stays gated north-star until there are multiple builders producing work to merge.
2. T-audit-telemetry-flatfile-repoint (M) -> ungates T-audit-run-records-enforced -> ungates examiner T-filing-audits-independent-examiner (3-deep chain). Advances the EXAMINER station.
3. T-granny-dispatch-observability-gap (M) — Akien's just-surfaced gap; explains why Aider sits idle; fixes the offload-loop observability.
4. T-escalation-dispatch-producer-review (M); S: T-store-layout-reconcile, T-unproven-lever-hygiene, T-proof-emitter-test-store-isolation.
**Calendar:** /outcome D-fable-window-altitude-agenda at Fri retro (07-10); 3 D-fable consequence tickets fire 07-21; 4 overdue consequence tickets want /outcome at next day-close.

✅ CLOSED
- D-branching-model-2026-07-08: per-ticket <ticket>.<builder> branches, matched-pair with merger station (triggered by 2nd live builder), retires CC-straight-to-main — T-consequence-D-branching-model

## In-flight: IN-FLIGHT: none mid-build (tree clean, all pushed @5e4a732a). NEXT (Akien-directed): T-collapse-daemons-to-ground-loop (L) — collapse granny+scraps standalone run_loop/__main__ into shim-started components under the SINGLE Ground Loop (new HARD RULE in CLAUDE.md: one daemon structure, PERIOD). This SUPERSEDES the transitional configure_process_logging bridge from the just-closed T-granny-dispatch-observability-gap (PROVEN @5868ac32) — relocate the dispatch-health _emit into the driven tick, delete the __main__ blocks. Also open: T-black-format-repo (M, black OFF until deliberate repo-wide pass; hook made graceful). Granny NOT restarted (running 16h-old code) — chose the real collapse fix over a restart on the bridge.

## ⭐ RESUME POST-RESTART — build T-collapse-daemons-to-ground-loop (L)
Full CORRECTED design in: notes/cc.0.ground-loop-collapse-design.20260708.*.json (grep "ground-loop-collapse-design"). READ THE MODEL SECTION FIRST.
⚠️ The pre-compact "Option B / runme / RunmeSupervisor / GL-hosts-thread" framing was a CONFABULATION (supervisor reflex) — corrected 2026-07-08. Do NOT rebuild from it.
MODEL: Ground Loop is PASSIVE (heartbeat + rescue only). Cooperative — the SHIM owns the loop. Granny is demand-started by her shim OFF THE QUEUE (confirmed 4x; memory feedback_granny_shim_startup). NO GL hosting, NO runme, NO bring-up gap.
Correct approach = the aider pattern (AiderShim → AiderWorkerListener) for BOTH granny + scraps:
- GrannyShim already watches the queue (_has_pending_tickets → sprint tickets) and starts her via _restart_daemon. Replace the tmux-subprocess launch there with: start an in-process background thread (GrannyDispatchLoop: `while not stop.wait(interval): run_once(config, imap)`), config reloaded per tick.
- DELETE run_loop/__main__/PID/signals/_wait_for_work from both daemon.py; KEEP run_once/_load_config/(granny)_make_imap_if_bus_configured. The Ground Loop daemon is NOT edited.
- Granny: bus(imap) handle built in the shim; self_test checks thread liveness (not PID); drop tmux watchdog machinery; retire ./granny launcher + device.py launch_command; relocate dispatch-health emit into the shim loop.
- REMOVE transitional configure_process_logging bridge (base.py) once shim-thread logging confirmed to reach canonical granny/scraps sink (verify device_id stamping).
- Scope: collapse STRUCTURE only, NOT run_once dispatch/scrap logic. FIRST STEP: advisor-check the IMPLEMENTATION of the bus-handle move + logging swap (not the model — the model is settled).

## In-flight: Restart point: corrected the ground-loop-collapse design from the confabulated supervisor/runme/RunmeSupervisor framing to the real cooperative model — Ground Loop PASSIVE, the SHIM owns the loop, granny demand-started off the QUEUE (aider pattern). Design note + slate resume block + memories (feedback_ground_loop_is_passive, feedback_granny_shim_startup) all corrected and pushed @6223f7e2. T-collapse-daemons-to-ground-loop NOT built yet; resume by advisor-checking the IMPLEMENTATION (bus-handle move + logging swap), then build. Model is settled — do not re-derive.
- T-granny-dispatch-tests-preexisting-failures: pre-existing granny dispatch/cascade/e2e test failures — reclaimed from aider escalation, mine to fix (caplog-intercept pollution + e2e mock gap)

## In-flight: T-collapse-daemons-to-ground-loop MID-BUILD (uncommitted): key finding — GrannyShim/ScrapsShim were NEVER instantiated in any running process (only in tests), so the recorded shim-owns-startup model was dead code and granny ran via ./granny+tmux, scraps via GL PluginDaemon subprocess = validation failures (Akien confirmed). Built the real collapse: shared ShimLoopThread primitive in shim.py (contextualized daemon poll thread — the ONE loop pattern); GrannyShim now owns an in-process demand-started dispatch loop (watchdog spots pending sprint tickets off the queue -> starts loop thread, no PID/tmux); wired GrannyWeatherwaxDevice/ScrapsDevice to their shims; added granny/__main__.py + scraps/__main__.py (aider-style: device()->shim.start()->signal.pause()); trimmed granny/daemon.py + scraps/daemon.py to just run_once + helpers (removed run_loop/__main__/PID/signals/_wait_for_work/get_daemon/configure_process_logging); logging fixed via logger.contextualize(device_id) INSIDE the thread body (advisor catch: DiagnosticBase installs sink+intercept but NOT process-wide device_id stamp, so records would drop); repointed ./granny; git-rm'd config/ground_loop/scraps.yaml (RUNTIME copy ~/.unseen_university/ground_loop/scraps.yaml still exists -> must rm or GL double-runs scraps). NEXT: (1) rewrite tests/granny/test_shim_self_heal.py (PID->thread model), fix test_baseshim_health_circuit get_daemon patch, remove test_listen_notify test_wait_for_work; (2) write the 2 discriminating proof tests (logging-lands-with-device_id, health-WARN-fires-from-driven-path); (3) run granny+scraps suites green; commit+push. Pre-existing failures (e2e/cascade/exact_match, 13) confirmed at HEAD via worktree, NOT mine -> reclaimed T-granny-dispatch-tests-preexisting-failures to CC.0/sprint.
- T-ground-loop-canonical-logging: GL daemon should use configure_process_logging (kept helper) not basicConfig — follow-up from the collapse deviation
- T-dsimnel-shim-worker-callback-api-drift: 6 pre-existing red (worker_callback kwarg gone) — mine to fix
- T-routing-compiler-rules-engine-test-red: 1 pre-existing red (inference rules engine) — mine to fix

## In-flight: T-collapse-daemons-to-ground-loop CLOSED PROVEN (pushed @5e3f45f1). Two proof_emitter proofs (authentic red=AssertionError->green via stub-first commits): loop-thread logs land with device_id, dispatch-health fires from driven path. granny+scraps now run as shim-owned ShimLoopThread threads (aider pattern); key finding = the shims were never instantiated live (dead code / validation failure), now wired. Deviations: KEPT configure_process_logging (for GL) + ./granny launcher. Filed 3 follow-ups: T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift, T-routing-compiler-rules-engine-test-red. Also reclaimed T-granny-dispatch-tests-preexisting-failures to CC.0. Tree clean. NEXT: pick up a follow-up or new /query-ticket.

## Session close 2026-07-08
Done: T-collapse-daemons-to-ground-loop CLOSED PROVEN (2 proof_emitter proofs, authentic red->green via stub-first commits @5e3f45f1). granny+scraps collapsed onto shim-owned ShimLoopThread threads (aider pattern). KEY: the shims were never instantiated live = dead code / validation failure; now wired into the devices. Deviations: KEPT configure_process_logging + ./granny launcher. Filed 3 follow-ups (T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift, T-routing-compiler-rules-engine-test-red); reclaimed T-granny-dispatch-tests-preexisting-failures to CC.0.
Next: Restart requested (session loaded during Anthropic status-board failures, model not at best). On resume: /context-load, then pick a follow-up ticket or /query-ticket. Slate's Next line is authority.

## In-flight: Session ending for a clean restart (model degraded — context loaded during Anthropic status-board incident, not context-length rot; tree clean, all work pushed). T-collapse-daemons-to-ground-loop is CLOSED PROVEN @5e3f45f1. Nothing mid-build. Resume with /context-load; the slate Next line points to the follow-up tickets or /query-ticket.

## In-flight: T-routing-compiler-rules-engine-test-red CLOSED PROVEN (proof 64a90d6c, red=AssertionError->green, pushed @4403afd1). Fixed: RulesEngine.__init__ '[]' now means no-rules (was silently loading defaults via 'or'); test mock now sets time_bucket/cost_class. Next in the test-red pair: T-granny-dispatch-tests-preexisting-failures (13 tests; root-cause already split — caplog handler killed by logging.basicConfig(force=True) for cascade/exact_match; genuine mock gap for e2e_dispatch_smoke).

## In-flight: TEST-RED PAIR DONE (both proven, pushed @dda6e6cb): (1) T-routing-compiler-rules-engine-test-red @64a90d6c; (2) T-granny-dispatch-tests-preexisting-failures @cf2415d1 — 13 env-dependent reds fixed with autouse tests/granny/conftest.py redirecting stall_state._STATE/_MRU/_LAST_DISPATCH to tmp; ticket's documented caplog root-cause was WRONG (real cause = is_stalled() reading real ~/.granny file). tests/granny/ = 101 passed. NEXT per Akien's order: (2) T-ground-loop-canonical-logging, then (3) T-dsimnel-shim-worker-callback-api-drift. FLAG: granny genuinely stalled on this box (real stall_state.json stalled=True) — surfaced to Akien, not acted on (out of scope).

## In-flight: INFERENCE ROUTER REDESIGN in flight. Decision D-inference-router-stack-decomposition-2026-07-08 + architecture/inference-router captured & committed (audit-design 9/9). Divergence: monolith _DEFAULT_RULES triples + 1:1 ModelSpec.source_name; root cause = T-inference-policy-router grep-not-read cancel. Target: 4 stacks (providers|models|connections|rules) x dims (ticket_tier+builder_tier+domain+urgency+escalation_allowed default True); tiers seed, escalation overrides; additive-then-cutover. NEXT: draft 7 tickets (connections-stack, dimensions-contract, rules-as-policy, resolver-compose, migrate-consumers-cutover, decomposition-invariant-proof, + granny-router-collapse-check next-phase), audit-ticket each, then BUILD in additive order. Detour parked: T-ground-loop-canonical-logging (released to sprint), then T-dsimnel-shim-worker-callback-api-drift.
- T-already-done-close-requires-proof: extend proof-on-close to already-built/cancel disposition (prevents grep-not-read recurrence)
- T-post-build-diff-reconciles-declared: post-build audit diffs commit vs declared Affected files
- T-ticket-as-chart-provenance-ledger: ticket becomes a chart (per-station declared-vs-actual) [triage — confirm chart shape]

## In-flight: ROUTER BUILD: ticket 1/6 T-inference-connections-stack CLOSED PROVEN @62f3e261 (stub-first red=AssertionError->green; additive connections stack, 22 seeded, nothing routes through it yet). NEXT: ticket 2/6 T-inference-dimensions-contract (RouteRequest {ticket_tier,builder_tier,domain,urgency,escalation_allowed default True}; tiers seed, escalation overrides). Igor DOWN = no HIGH-inertia gate; build straight through incl cutover. Also filed T-testing-rack-device-container (triage, vision: hex builder + containerized test device). Prevention thread (3 tickets) done.

## In-flight: ROUTER BUILD 2/6 done: T-inference-dimensions-contract CLOSED PROVEN @f6db2251 (RouteRequest + difficulty_seed; ticket_tier->difficulty seed, builder_tier orthogonal, escalation_allowed default True). 1/6 connections-stack @62f3e261. NEXT 3/6: T-inference-rules-as-policy (PolicyRule: dims->capability envelope, no model/provider literals; depends dimensions-contract=done). Then 4 resolver-compose, 5 cutover, 6 invariant-proof. Igor down=no HIGH-inertia gate. Stub-first pattern REQUIRED for each net-new module proof.

## In-flight: ROUTER BUILD 3/6 done: T-inference-rules-as-policy CLOSED PROVEN @a6620843 (policy.py: PolicyRule dims->CapabilityEnvelope, no model/provider literals, build_envelope monotone-tighten). Done: 1 connections @62f3e261, 2 dimensions @f6db2251, 3 policy @a6620843 — 3 of 4 stacks + contract built, all ADDITIVE (route() untouched). NEXT the hard half: 4/6 T-inference-resolver-compose (L: resolve(RouteRequest) composes 4 stacks + reuse cost-optimizing selector + escalation walk gated by escalation_allowed), 5/6 cutover (L), 6/6 invariant-proof (S). CHECKPOINTING with Akien before the L tickets (context long; highest-stakes). Stub-first REQUIRED per net-new module.

## Session close 2026-07-08
Done: Done: (1) Test-red pair closed PROVEN — T-routing-compiler-rules-engine-test-red @64a90d6c (RulesEngine '[]' now means no-rules; mock time_bucket/cost_class) and T-granny-dispatch-tests-preexisting-failures @cf2415d1 (13 reds; ticket's documented caplog root-cause was WRONG — real cause = run_once's is_stalled() reading the real ~/.granny stall file [box is STALLED]; fixed with an autouse tests/granny/conftest.py redirecting stall_state paths to tmp; tests/granny=101 green). (2) Surfaced+captured the inference-router MONOLITH divergence Akien flagged: decision D-inference-router-stack-decomposition-2026-07-08 (audit-design 9/9) + architecture/inference-router; root cause = T-inference-policy-router grep-not-read cancel 2026-06-23. (3) Built 3/6 router tickets ADDITIVE+PROVEN: connections-stack @62f3e261, dimensions-contract @f6db2251, rules-as-policy @a6620843 (route()/_DEFAULT_RULES untouched, zero live-dispatch risk). (4) Prevention thread filed: T-already-done-close-requires-proof + T-post-build-diff-reconciles-declared (sprint) + T-ticket-as-chart-provenance-ledger (triage, confirm chart shape) + T-testing-rack-device-container (triage vision).
Next: Next: FRESH SESSION for the router HARD HALF (Akien-chosen break for anti-context-rot). /context-load then sprint 4/6 T-inference-resolver-compose (L: resolve(RouteRequest) composes 4 stacks + REUSE the cost-optimizing selector unchanged + escalation walk gated by escalation_allowed), then 5/6 T-inference-migrate-consumers-cutover (L: repoint device.py+Igor consumers, delete triples + ModelSpec.source_name — Igor DOWN so NO HIGH-inertia gate), then 6/6 T-inference-router-decomposition-invariant-proof (S). STUB-FIRST required for each net-new-module proof (added file -> ImportError red is rejected; stub->real gives AssertionError red). Then T-granny-router-collapse-check (next phase, likely same collapse). Also deferred: T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift. NORTH STAR context: hex-hosted builder assistant that escalates + a containerized testing rack device.

## In-flight: Router redesign 3/6 built (all 4 stacks + contract additive & proven; route() untouched). Clean boundary — hard half (resolver-compose, cutover, invariant-proof) deferred to a fresh session per Akien. Everything pushed @0f153bef; tree clean.

## In-flight: ROUTER HARD-HALF 4/6 done: T-inference-resolver-compose CLOSED PROVEN @eafe7d79 (proof cc.0.rulesengine-resolve-dimensional-resolver, red=AssertionError->green). resolve(RouteRequest) composes all 4 stacks BESIDE the untouched route(): build_envelope -> candidate models -> connections filtered by availability+urgency -> cost-optimizing selector (routing_buckets reused) picks cheapest capable connection; no triple consulted (proven via rules=[]). ADVISOR CATCH (highest-value): my first design was an INTERNAL escalation loop — WRONG. Checks proved escalation is EXTERNALLY driven: device.py owns escalation_hop, computes bump_difficulty(seed,hop), calls route() ONCE per rung with required_difficulty; the driver is DS._run_inference caller-side (D-inference-domain-routing: "device.py runs one call and cannot see did-not-reach-DONE"). So resolve() = ONE selection per call honoring required_difficulty override; escalation_allowed=True walks floor up (different/pricier pick), =False pins to seed (deterministic); no-capable-connection under escalation_allowed=True fires terminal system_alarm(WARNING); availability failure != escalation. An internal loop would have STRANDED device.py's hop driver at cutover — a dead-end 5/6 would have to redesign.
NEXT 5/6: T-inference-migrate-consumers-cutover (L: repoint device.py + Igor/DS/aider consumers + domains/base.py select() to resolve(); repoint routing_compiler + add_compiled_rule to emit dimensional policy not triples; THEN delete _DEFAULT_RULES triples + ModelSpec.source_name). Igor DOWN = NO HIGH-inertia gate (Akien 2026-07-08). ⚠️ CUTOVER PARITY NOTE from advisor: connections carry no rule.priority, so resolve()'s final tiebreak is (model_id, source_name) not (...,priority) — the ONE intended divergence from route(); the cutover parity check must EXPECT it. ⚠️ Double-alarm reconcile: resolve() fires system_alarm on no-capable; device.py has its own "complete-inference-failure chokepoint" + domains/base.py fires past-top — reconcile so it fires ONCE at cutover. STUB-FIRST for any net-new proof; deletions proof_emitter can red->green directly. Then 6/6 T-inference-router-decomposition-invariant-proof (S: add-connection-zero-rule-edits red->green).

## ⭐ 5/6 T-inference-migrate-consumers-cutover — DE-RISKED BUILD PLAN (fresh session; destructive, load-bearing)
Advisor-reviewed. This is the CUTOVER (deletes _DEFAULT_RULES triples + ModelSpec.source_name, repoints the load-bearing proxy). HIGH-inertia device.py pause is SUPERSEDED by Akien's standing directive on THIS ticket (Igor DOWN → no gate); proceed, no fresh pause. Nothing is actively dispatching (Igor on ice, DS off, Aider idle) so "proxy up throughout" = don't break the code path (test-verified), not a live daemon.

TIER MISMATCH = MECHANICAL, NOT AN AKIEN FORK (advisor). ticket_tier is SEED-ONLY (resolve consumes it only via difficulty_seed). Both vocabularies collapse to the same 3 difficulty buckets. Bridge at the RouteRequest-construction boundary (device.py / domains select):
  task_class → ticket_tier(role):  minion→apprentice, worker/analyst/batch→builder, designer→master  (designer→master, NOT guru, to avoid the guru-work-is-design policy floor).
  builder_tier: INERT in live dispatch (no shipped policy reads it except domain_in coding / ticket_tier_in guru) → default "builder"; wire real values as a follow-on.

THE LANDMINE (advisor catch, verified): the triples are where MULTI-PROVIDER reachability lives; source_name is 1:1. seed_from_models reads source_name ONLY → silently drops triple-only edges while the grep-proof still passes green. Exact edge audit (programmatic, HEAD): source_name=22 edges, triples=22, UNION=25.
  3 TRIPLE-ONLY edges (MUST survive into connections): devstral-small-2:24b@ollama (Hex), qwen3-coder-next@ollama (Hex), qwen/qwen3-30b-a3b-instruct@openrouter.
  PHANTOM: qwen/qwen3-30b-a3b-instruct has NO ModelSpec (creator tier disabled, model never registered) → that edge is DEAD (resolve iterates models.all(), never reaches a phantom model_id). DROP it, documenting as dead creator-tier debt. Functional union = 24 edges (22 source_name + devstral@ollama + qwen3-coder-next@ollama).

SAFE SEQUENCE (revertible; delete LAST in ONE commit = clean rollback to working monolith):
  1. Author connections.py an AUTHORITATIVE explicit seed (the 24 functional edges) — connections becomes the SOLE home of reachability. Connection.dollars_per_unit = spec.dollars_per_unit (source cost_class, not conn dollars, differentiates owned-local vs cloud in the selector). Replace seed_from_models as resolve()'s default with this. Add a test asserting a TRIPLE-ONLY edge (devstral@ollama) is present — the failure the grep-proof CANNOT catch. Assert union count ≥ every functional edge currently in _DEFAULT_RULES ∪ source_name. STUB-FIRST if net-new module surface.
  2. Repoint consumers to resolve() via the mechanical tier bridge: device.py (the resolve_domain(domain).select(... task_class, required_difficulty) path → build RouteRequest + resolve; keep external escalation_hop→required_difficulty wiring — resolve honors required_difficulty exactly like route), domains/base.py select(), Igor cognition (consolidation/distillation, after_action_reviewer — grep for direct route() callers), routing_compiler + add_compiled_rule (emit dimensional PolicyRule, not RoutingRule triples).
  3. THEN delete _DEFAULT_RULES triples + the triple form of add_compiled_rule + ModelSpec.source_name; update tests/inference. proof_emitter red->green works on deletions directly.
DOUBLE-ALARM RECONCILE: resolve() fires system_alarm on no-capable; device.py has its own complete-inference-failure chokepoint + domains/base.py fires past-top — make it fire ONCE (pick the owner) at cutover.
PROOF SHAPE (ticket): "live dispatch returns text" + git grep finds NO triple/source_name — NOT resolve()==route() bit-parity, so the priority-tiebreak divergence needs no parity assertion. DO add the triple-only-edge-survives check (the grep-proof's blind spot).

## Session close 2026-07-08 (router hard-half, session 2)
Done: 4/6 T-inference-resolver-compose CLOSED PROVEN @eafe7d79 (proof red=AssertionError->green). resolve() composes all 4 stacks beside the untouched route(); escalation is ONE-step honoring an external required_difficulty override (advisor caught + code-confirmed: device.py owns the hop, per D-inference-domain-routing — my initial internal-loop design would have stranded that driver at cutover). Then fully DE-RISKED 5/6: found the multi-provider-reachability landmine (3 triple-only edges seed_from_models would silently drop) + a phantom creator-tier edge + settled the tier vocabulary as mechanical — complete build plan above.
Next: FRESH SESSION for 5/6 (destructive cutover; Akien's fresh-session-per-hard-ticket cadence — this is exactly the ticket that cadence protects). /context-load then execute the DE-RISKED BUILD PLAN above (safe sequence: authoritative union-seed+survival-proof → repoint consumers via tier bridge → delete triples+source_name last). Then 6/6 T-inference-router-decomposition-invariant-proof (S). Then T-granny-router-collapse-check (next phase). Also deferred: T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift.

## 5/6 CUTOVER — session-3 refined plan (fresh-context re-verify + advisor pass)
Landmine RE-CONFIRMED against HEAD: 22 source_name edges, 22 triples, union 25; 3 triple-only: devstral-small-2:24b@ollama + qwen3-coder-next@ollama (FUNCTIONAL Hex, MUST survive), qwen/qwen3-30b-a3b-instruct@openrouter (PHANTOM, no ModelSpec, DROP). Functional union = 24 edges. Tier-bridge difficulty seeds ALL align with device.py req_difficulty (minion→apprentice/classify, worker/analyst/batch→builder/code, designer→master/design). Consumer map (Explore, verified): ONE route() call site = domains/base.py:115 select(); device.py threads only task_class/session_id/foreground/required_difficulty; ONE RulesEngine ctor = device.py:112 (NOT singleton — many live instances). add_compiled_rule/routing_compiler have ZERO live callers (compiled-rule path never wired into dispatch). by_source dead.
NEW LANDMINE (prior de-risk missed): coding-needs-tools DEFAULT policy would SILENTLY change the live pick — the owned-local coding FLOOR models devstral-small-2:24b + qwen3-coder-next carry features=[] (NOT ['tools']); route() never enforced features live (device.py never passes required_features). resolve() with policies=None→_DEFAULT_POLICIES excludes them, bumps floor to qwen2.5-coder:14b. "returns text" proof CANNOT catch it (still a cheap Hex model). → cutover passes policies=[] EXPLICITLY (behavior-preserving); activating coding-needs-tools needs model tool-feature tagging first = FOLLOW-ON ticket.
DEVICE.py:112 DOUBLE-DEFAULT TRAP (advisor): RulesEngine(self._sources,self._models) → policies=None (regression above) AND connections=None → seed_from_models drops the 2 Hex edges. BOTH must be passed EXPLICITLY: policies=[] + authoritative 24-edge connections.
AFFINITY: NOT inert live — evaluator/device.py:416 session_id=run_group_id + minion/tool_loop.py:115 session_id=envelope.session_id pass non-empty (Igor down). → PORT session-affinity into resolve() (optional session_id param, reuse self._session_map) so cutover is truly non-regressive. Night-mode batch: only Igor(down) dispatches batch → DEFER (named follow-on), don't port time-gate into dimensional resolver.
PROOF (advisor-strengthened, discriminating): worker/coding/hop0/sources-up → resolve() picks devstral-small-2:24b@ollama (the designed floor). GREEN under policies=[]+24-edge conns; RED if feature-floor returns (→qwen2.5-coder:14b) or a Hex edge drops. Targeted guard on the one path device.py drives (analog of triple-only-edge check), NOT full route()==resolve() bit-parity (priority-vs-model_id tiebreak diverges on other inputs). Plus: assert no source_name/triple survives.
SCOPE DISCOVERY (bigger than slate): deleting source_name forces deleting route() (reads it at 332/338/344) — dead after select() repoints. route()-selector test files (test_router_selector/test_flat_rate_routing route()-tests) are REDUNDANT (coverage already in test_resolver_compose vs resolve()) → DELETE not migrate. ModelSpec-schema tests (test_router_schema) → drop positional source_name arg (mechanical). ~40 positional + ~30 kw ctor sites.
SEQUENCE: commit1 = core repoint behavior-preserving GREEN (authoritative connections.default_connections(24) + resolve affinity + select builds RouteRequest via tier bridge→resolve + device.py:112 explicit policies=[]+connections; route()/source_name still present but off live path). commit2 = destructive: delete _DEFAULT_RULES/RoutingRule/add_compiled_rule/route()/_is_night_mode/by_source + ModelSpec.source_name + ctor sweep + delete redundant route() tests → grep-clean, suite green (revertible to commit1). FOLLOW-ONS to file: T-inference-activate-coding-tools-policy (tag agentic coders features=['tools'] + enable coding-needs-tools), T-inference-batch-nightgate-in-resolver (port time-of-day gate when batch dispatch returns).

## 5/6 CUTOVER — session-3 RECONCILIATION (empirical, overturns then reconfirms policies=[])
Built a route-vs-resolve parity harness (all task_classes x {gen,coding}). resolve() DIVERGES from route() on 6/8 hop-0 picks — but the DECISION authorizes this: "ticket_tier+builder_tier SEED where resolution starts; they are HINTS, not authority"; bar is suites-green+grep-clean+add-connection-invariant, NOT pick-parity. Root cause of divergence: triples encoded human TIER-CURATION (which model serves which task_class); dimensional selection has none — deepseek-r1:14b (generalist, code-difficulty, owned-local $0) wins the cheapest-capable tiebreak UNIVERSALLY at code+. That's authorized dimensional behavior, not a bug.
NEARLY INVERTED to policies=None (coding-needs-tools active) + tool-tag agentic coders, to force worker/coding hop-0 -> devstral. WRONG axis (advisor caught + EMPIRICALLY CONFIRMED): the live-critical path is the coding ESCALATION LADDER (worker/coding, required_difficulty bumped code->design). TEST: resolve(worker/coding, required_difficulty=design): route=deepseek-r1:32b@ollama; policies=[] -> deepseek-r1:32b@ollama (MATCHES route, ladder preserved); policies=None -> None+SYSTEM_ALARM (FALSE-HALTS the ladder — no tool-tagged design-capable coding model locally). So policies=[] is RIGHT — not for the feature-floor reason, but because it preserves the escalation ladder with ZERO tool-tagging. Tool-tagging is a model FACT (native tool-calls? edit_format_conformance data?), empirically checkable -> deferred to T-inference-activate-coding-tools-policy, NOT a knob turned to fit routing.
FINAL cutover: device.py:112 = RulesEngine(sources, models, connections=default_connections(models), policies=[]). Hop-0 appropriateness (devstral floor) + coding-needs-tools activation = FOLLOW-ON (needs real tool-capability data + the divergences are authorized).
PROOF NODE (5/6): (1) git grep in inference finds NO (task_class,model_id,source_name) triple / ModelSpec.source_name [RED before=triples exist -> GREEN after delete]; (2) live device resolves worker/coding (returns decision); (3) TRIPLE-ONLY Hex edge devstral-small-2:24b@ollama present in what resolve reads live (grep-proof blind spot); (4) ESCALATION LADDER preserved: resolve(worker/coding, required_difficulty=design) -> design-capable owned-local model, NOT None+alarm [discriminates policies=[] vs None]; (5) every default model has >=1 connection (guards the explicit-24-edge-table drift risk). All RED pre-cutover (triples exist / connections=None drops Hex / policies=None false-halts), GREEN post.

## 5/6 CUTOVER — session-3 BUILD PROGRESS (core done + verified live)
CORE REPOINT DONE + VERIFIED LIVE (InferenceDevice() on Hex): connections.py default_connections()+_DEFAULT_CONNECTION_EDGES (24 explicit, self-contained, both Hex edges present, every model >=1 conn); rules_engine.resolve() gained session_id affinity (mirrors route, early-return) + lazy default now default_connections; domains/base.select() builds RouteRequest via _TASK_CLASS_TO_TIER bridge -> resolve() (route() call removed from select); device.py:112 RulesEngine(...connections=default_connections, policies=[]). LIVE CHECK PASS: escalation ladder preserved (seed->deepseek-r1:14b@ollama code, design->deepseek-r1:32b@ollama design, NO false-halt), affinity works, 24 conns.
TEST FALLOUT (inference suite: 20 failed/451 passed). Classified:
 - PRE-EXISTING (unrelated to routing, TICKET at 0.2, do NOT fix here): test_domain_prompts::coding_prompt_byte_identical (prompt SHA drifted from pinned const), test_repo_map_orientation::signature_map_multiple_symbols (2nd-symbol-per-file gap).
 - MINE-resolver_compose (5): synthetic-model tests relied on lazy seed_from_models; lazy default now default_connections (real model_ids only) -> synthetic models get 0 edges. FIX: build explicit ConnectionsRegistry per test (post-cutover pattern: ModelSpec has NO source_name, Connection carries the edge as test literal).
 - AUTHORIZED-CHANGE (select->resolve changes picks; "tiers are hints"): test_domain_object (3 select-matches-route), test_mini_rack (worker->OR, batch->night-ollama), test_dicksimnel (google_free-preferred, done_prefix), test_pin_gate (unpinned-routes-by-domain), test_cost_record (specific model), test_inference_tier_escalation (3), test_dicksimnel_live_smoke (ran LIVE on Hex, deepseek-r1:14b JSON-wrapped output != devstral plain — the hop-0 divergence's REAL coding cost, exactly what the follow-on fixes). FIX: update to new dimensional behavior OR delete if route()-specific/redundant.
DECISION: ONE revertible cutover commit (not commit1-green-then-commit2 — commit1 can't be green since behavior change breaks assertion tests). Remaining: delete _DEFAULT_RULES/RoutingRule/add_compiled_rule/route()/_is_night_mode/by_source/seed_from_models + ModelSpec.source_name; fix ALL affected tests to post-cutover pattern; delete redundant route() tests (coverage in test_resolver_compose vs resolve); ~40 positional + ~30 kw ModelSpec ctor sweep; add cutover proof node; proof_emitter red->green; commit+push. FOLLOW-ONS to file: T-inference-activate-coding-tools-policy, T-inference-batch-nightgate-in-resolver, T-preexisting-domain-prompt-sha + T-preexisting-repo-map-second-symbol.

## 5/6 CUTOVER — DELETIONS DONE + proof files green (session-3)
Core deletions complete + package imports clean + live device verified: rules_engine (route/_DEFAULT_RULES/RoutingRule/add_compiled_rule/_is_night_mode/rules-param GONE; resolve+affinity+connections_for kept); models_registry (source_name field + by_source GONE, 22 rows stripped); connections (seed_from_models GONE, default_connections authoritative); version_checker (source_name kwarg dropped); device.py:112 wired policies=[]+default_connections; device explicit-model path repointed to connections_for; web_server render -> connection providers; __init__ RoutingRule export dropped. My proof files GREEN (6 tests): test_resolver_compose (rewritten post-cutover: explicit connections, no source_name, no rules=) + NEW test_consumers_cutover (proof node: monolith-gone via hasattr + live resolve + Hex-edge-survives + escalation-ladder-preserved + every-model-reachable). Delegated the rote test sweep (all other ModelSpec ctors + RulesEngine(rules=) callsites + route()-test delete/migrate + seed_from_models + stale docstrings) to a fresh agent (told NOT to touch my 2 files). Filed follow-ons: T-inference-activate-coding-tools-policy (M sprint), T-inference-batch-nightgate-in-resolver (S dependency), T-preexisting-domain-prompt-sha (0.2), T-preexisting-repo-map-second-symbol (0.2). PENDING: agent finishes sweep -> full inference suite green (minus 2 pre-existing) -> proof_emitter red->green on test_consumers_cutover -> commit+push -> close 5/6. Then 6/6 T-inference-router-decomposition-invariant-proof (add-connection-zero-edits red->green).

## ✅ D-inference-router-stack-decomposition COMPLETE — all 6 tickets closed proven
5/6 T-inference-migrate-consumers-cutover CLOSED PROVEN (proof cc.0.inference-router-cutover-every-consumer @441076bf, red=AssertionError->green). ONE revertible cutover commit. DELETED: _DEFAULT_RULES triples, RoutingRule, route(), add_compiled_rule, _is_night_mode, by_source, seed_from_models, ModelSpec.source_name. connections.py = authoritative 24-edge table (sole home of reachability, self-contained) = 22 source_name bindings + 2 triple-only owned-local Hex edges (devstral-small-2:24b@ollama, qwen3-coder-next@ollama); phantom qwen/qwen3-30b-a3b-instruct@openrouter dropped. select() bridges task_class->ticket_tier -> resolve(); resolve() gained session affinity. device.py wires connections + policies=[] EXPLICITLY (both ctor defaults are traps).
6/6 T-inference-router-decomposition-invariant-proof CLOSED PROVEN (proof cc.0.the-router-decomposition-invariant-addin @240ad99f, red=AssertionError->green). Invariant drives the REAL dispatch path (BaseDomain.select), not resolve() directly — that seam is where a re-collapse reappears. Constructs no ModelSpec / imports no post-cutover-only symbol so its pre-cutover red is a true AssertionError not a collection error. + anti-collapse guard (rules stack names no model/provider literal).
SUITES: inference 433 passed / granny 101 passed. Pushed HEAD a25de650.
KEY EMPIRICAL FINDING (cost me two near-misses): the triples encoded human TIER-CURATION that the dimensions do NOT reproduce. resolve() diverges from route() on 6/8 hop-0 picks; the DECISION AUTHORIZES this ("tiers seed where resolution starts; hints, not authority"). I nearly inverted to policies=None + tool-tagging to force devstral back as the coding floor — WRONG AXIS. The live-critical path is the ESCALATION LADDER: resolve(worker/coding, required_difficulty=design) -> deepseek-r1:32b@ollama under policies=[] (matches old route()), but None+SYSTEM_ALARM under policies=None (false-halts the ladder — no tool-tagged design-capable coding model is reachable locally). policies=[] is right, NOT for the feature-floor reason but because it preserves the ladder with ZERO tool-tagging. Tool-capability is a model FACT, not a knob to turn until routing looks good.
⚠️ HONEST CAVEAT — one CUTOVER-CAUSED red remains: tests/inference/test_dicksimnel_live_smoke.py::test_dicksimnel_builds_on_hex. Ran LIVE on Hex: dispatch -> deepseek-r1:14b@ollama (authorized hop-0 divergence), loop reached DONE, but assert result.startswith('DONE:') failed — the reasoning model returned {"status":"done",...} JSON instead of the DONE: envelope devstral emits. Instruction-following degraded on the live coding path. NOT papered over: pinned as the ACCEPTANCE TEST on T-inference-activate-coding-tools-policy (priority bumped 0.5->0.8; goes green when devstral is restored as coding floor; do NOT loosen the assertion).
PRE-EXISTING reds ticketed (none cutover-caused): T-preexisting-domain-prompt-sha, T-preexisting-repo-map-second-symbol, T-preexisting-dicksimnel-done-prefix-close (left red by the CLOSED T-builder-merge-time-proof scaffold).
Next: T-granny-router-collapse-check (next phase — granny's role_in -> route_to likely the same collapse). Also deferred: T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift.

## ⚠️ 5/6 BAD CLOSE CAUGHT AND CORRECTED (same session) — re-closed proven @6704dc1d
I closed 5/6 on a DEFECTIVE proof, then caught it. Two independent defects:
(1) WRONG INVARIANT + REAL BUG SHIPPED. The triples hand-curated devstral-small-2:24b as the coding floor. Dimensional selection reads the STRUCTURED features flag — devstral's was never populated, though its own catalog row already recorded 'agentic'/'tool-call' TAGS. With coding-needs-tools off (policies=[]), coding resolved to tool-less deepseek-r1:14b, which CANNOT emit a tool call: it returned {"status":"done","result":"wrote smoke file"} and WROTE NOTHING. Confabulated success. Found ONLY by running the live coding path and checking the filesystem — the proof's assertion died at .startswith("DONE:") BEFORE the smoke_file.exists() check, so task-completion was never tested. My "policies=[] preserves the escalation ladder" argument was wrong: route()'s design rung served deepseek-r1:32b, ALSO features=[], ALSO cannot build — I matched an already-broken rung and called it parity.
(2) ENV-DEPENDENT PROOF. The proof node built a live InferenceDevice() whose HealthMonitor probes real providers; it passed only because this box had Hex up. A proof that depends on the weather is not a proof. (Caught by the sweep agent, whose env lacked those sources.)
FIX: features=["tools"] on devstral (evidence-based — only devstral carries the tool-call tag; qwen3-coder-next does NOT and was not tagged) + device.py policies=None. Coding -> devstral@ollama (tool-capable, owned-local). Escalating coding past local capability now honestly returns None. Proof rewritten HERMETIC (no device), asserting the invariant actually broken: a coding request must resolve to a TOOL-CAPABLE model so a failure HALTS instead of silently claiming success; plus tag/feature mirror consistency. Re-emitted with parent-ref a25de650 (the bad close commit) so red->green proves THE FIX. red=AssertionError -> green=passed.
SUITES: inference 436 passed, granny 101 passed. 3 pre-existing reds, all ticketed.
NEW TICKETS: T-inference-capability-ceiling-misclassified-as-availability (CP3 — a capability ceiling is classified AVAILABILITY, retried, then halted with "no live source": names the wrong cause; also 3 alarms fire for one event), T-live-smoke-red-devstral-loop-not-routing (smoke red even with devstral CORRECTLY selected: hop=0 devstral ran 3 turns -> escalate|capability. The cutover touched routing/connections/source_name only, never the loop/tools/prompts, so an in-loop hop-0 failure cannot be cutover-caused).
6/6 invariant proof unaffected (builds its own engine, policies=[] for isolation) — still green.
Next: T-granny-router-collapse-check. Then T-ground-loop-canonical-logging, T-dsimnel-shim-worker-callback-api-drift.
