# Slate 2026-07-03 (CLOSED)

## Done today

🎉 **MILESTONE — FIRST PROVEN LIVE DS.0 BUILD ON HEX.** T-ds-smoke-hello: DS.0 fetched the
ticket, routed worker→devstral-small-2:24b@Hex, agentic ToolLoop called Write →
/tmp/ds_smoke/SMOKE_OK.txt ('dicksimnel smoke ok'), result 'DONE', cost $0.00, 3 turns/~15s.
The DS.0 Phase-1 build path is proven end-to-end.

Tickets closed:
- T-ds-smoke-hello — MILESTONE, first proven live DS.0 build (devstral@Hex, $0). [proven]
- T-dicksimnel-cc-queue-path-fix — f0640371: DS.0 resolves cc_queue via uu_root() (parents[2]
  reorg fallout → couldn't fetch any ticket). Enabled the live build. [proven]
- T-granny-rules-engine-tests-red — 085f4280 (shipped-unproven): REAL BUG — package-collapse
  refactor wrote non-running CC.1 into dispatch → Granny self-parked → no master dispatch.
  Fixed CC.1→CC.0 in _default_config + granny.yaml; 27/27 green. ⚠ live Granny still parked
  from this bug at close — needs restart to un-park (surfaced to Akien).
- T-inference-mark-anthropic-defunct — 1c35ff0c (shipped-unproven): first-class Source.defunct
  flag (policy beats connectivity); anthropic defunct-by-policy (browser-auth/zero-balance);
  4 tests pass. Lever: stub-first split not done.
- T-cc-tmux-session-dot-naming-broken — 2014e68f + proof 0880d438 [PROVEN]: dot-free tmux
  session names (hostname_cc_N); tmux mangles dots. red=AssertionError green=passed. comms://
  bus addresses stay dotted.
- T-launcher-1m-default-fresh-session — 569bf200 (shipped-unproven): 1M context now DEFAULT in
  superclaude/rescueclaude; always-fresh conversation; --new removed. --200k/--no-1m = inverse
  A/B lever. Akien on 5x Max plan, browser auth, no API keys.
- T-launcher-help-and-1m-flags — shipped-unproven (no launcher-proof harness; --1m validated
  in prod).

Decisions filed (4):
- D-granny-pull-dispatch-model-2026-07-03 — faae8f34: Granny PULL-dispatch + shim-owns-startup
  + class-semaphore + self-clearing no-builders alarm.
- D-env-creds-device-cleanup-2026-07-03 — 5e7eab38 / retargeted accecdb5: clean env/creds/
  device-data before DS.0; single credential home = the VAULT device (not uu_bash_profile.sh).
- D-ds-builder-leveling-2026-07-03 — 9035e8a8: level DS.0 builder to CC.0 (listener close bug,
  prompt fixes, advisor review ladder, v2 redesign).
- D-swarm-location-independent-devices-2026-07-03 — f4e33b52: location-independent devices,
  master-only DB, capability inventory, placement=shared rules-engine, cooperative shim-to-shim.

## Notes
- ⚠ OPERATIONAL: the LIVE Granny is parked (is_stalled) from the CC.1→CC.0 bug — needs a
  resume + config reload (restart) to un-park and route master work to CC.0.
- ⚠ AKIEN: OpenRouter key was rotated (done); vault migration for secrets can proceed.
- TOPOLOGY CONFIRMED: CC.0 (this session, discuss+work) + DS.0 (builder on Hex). No local CC.1
  builder (crashes this 4-core/16GB box; multi-CC-over-MCP failed — CC doesn't use MCP unprompted).

## Session close 2026-07-03
Done: FIRST PROVEN LIVE DS.0 BUILD ON HEX (devstral-small-2:24b, $0, SMOKE_OK.txt end-to-end).
Closed 7 tickets incl. real fixes: granny CC.1→CC.0 dispatch bug (27 green; Granny had
self-parked), anthropic-defunct flag, DS.0 cc_queue path (reorg fallout), launcher
1M-default+fresh, dot-free tmux naming (proven). Filed 4 decisions (granny-pull-dispatch,
env-creds-cleanup→vault, ds-builder-leveling, swarm-location-independent). Analyzed DS.0
builder prompt; verified close-bug is in listener _post_result (bare close refused by
proof-on-close → escalates every real ticket).
Next (rolled into 07-04): the DS.0 observe-run + /sorted landed early 07-04 → D-ds-inference-
context-management: DS.0's FIRST leveling blocker is inference context management (sources.py:352
never sets num_ctx → ollama silently truncates → model works BLIND), NOT the listener close bug.
Critical path: T-ollama-source-set-num-ctx (S, unblocker) → T-ds-observe-rerun-after-ctx-fix.
Pending: restart parked Granny; controlled Granny→DS.0 dispatch E2E; T-granny-bus-dispatch-tests-red.

## In-flight (trail — running ledger, chronological)

- T-slate-writer-dirty-tree filed (S, devex friction); clean end of 6-ticket proven chain.

- POST-DAY-CLOSE (07-02 closed, 10 tickets proven). 3 tickets filed this tail, all pushed:
  T-compact-at-150k-context [SPRINT] (native /compact at ~150K ctx replacing dead /autocompact
  hook; settings.json hook removed, backup .bak-20260703); T-fold-weekly-into-daily
  [OPEN_QUESTIONS→SPRINT, Q1 answered (a) progressive-across-week]; T-self-diagnostic-trouble-
  tickets [OPEN_QUESTIONS, ANCHOR] (alarms carry failure-time diagnostics, No.5 Crossbar).

- /ticket batch (Akien 2026-07-03 post-reload): closed T-launcher-help-and-1m-flags; filed
  T-launcher-1m-default-fresh-session, T-levers-doc-compact-threshold; discarded duplicate
  prebuild ticket (already T-uu-build-packet-compiler); undeferred T-converge-worker-dispatch
  ('we don't defer'). D-granny-pull-dispatch-model filed.

- T-launcher-1m-default-fresh-session CLOSED (569bf200). 1M default via clearing
  CLAUDE_CODE_DISABLE_1M_CONTEXT; rescueclaude always-fresh; --new removed. Launcher Phase-0 DONE.

- CLEANUP NEXT (Akien-flagged, part of DS.0 course): .bashrc/env/creds/device-data cleanup —
  D-env-creds-device-cleanup filed. CORRECTION: single credential home = the VAULT device
  (unseen_university/devices/vault/, Fernet+Postgres, connect-time client), NOT uu_bash_profile.sh.
  DEVICE-DATA LAYOUT: class + INSTANCE dirs are FLAT SIBLINGS (devices/DickSimnel + devices/DS.0),
  not nested.

- T-inference-mark-anthropic-defunct CLOSED (1c35ff0c/78cb56b8). Source.defunct flag; anthropic
  defunct-by-policy; 4 tests pass. DISCOVERED+filed T-granny-rules-engine-tests-red (20 pre-existing
  failures on the DS.0 dispatch path).

- T-granny-rules-engine-tests-red CLOSED (085f4280). ROOT was a REAL BUG: CC.1 in dispatch config
  for a builder never run on this box → Granny PARKED. Fixed CC.1→CC.0; 27/27 green. Filed
  T-granny-bus-dispatch-tests-red (10 sibling failures).

- T-inference-status-report [sprint, Akien-approved]. DS.0/HEX SUBSTRATE VERIFIED READY (Hex up
  10.0.0.100:11434, devstral+qwen pulled, routing present, anthropic defunct, granny fixed).

- 🎉 MILESTONE: first proven live DS.0 build on Hex (see Done today). Surfaced+fixed
  T-dicksimnel-cc-queue-path-fix (parents[2] reorg fallout). Systemic: same parents[N] fallout in
  vetinari/web_server/installer → T-reorg-parents-path-sweep [sprint/builder].

- DS.0 builder-prompt leveling: analyzed coding.md; close-bug is in listener _post_result (bare
  cc_queue close refused by proof-on-close). D-ds-builder-leveling filed (9035e8a8): T-ds-fullcycle-
  observe-run (do first), T-ds-listener-proof-on-close, T-ds-builder-prompt-fixes, T-ds-advisor-
  review-ladder, T-builder-prompt-sota-webrun, T-ds-builder-prompt-redesign-v2, T-consequence.

- Dot-free tmux session naming FIXED+PROVEN (2014e68f, proof 0880d438). Filed T-superclaude-cold-
  start-kill-server-footgun (split), T-cc-session-naming-reload-harness-fragility.
  D-swarm-location-independent-devices filed (f4e33b52, 7 tickets).

- DS.0 OBSERVE-RUN on T-reorg-parents-path-sweep (controlled direct, devstral@Hex, $0) → None/HALT
  after ~34min. 114 tool calls (102 Read + 12 Bash), ZERO Edit/Write. CORRECTED root-cause
  (advisor-checked): sources.py:352 never sets num_ctx → ollama silently truncates → model works
  BLIND. Turn-39 = oversized-prompt wall (flat ~16s/turn through t38, then 120s cliff x3 → HALT).
  Retracted 2 wrong mechanisms; capability NOT fairly tested until model sees ticket. → captured
  in D-ds-inference-context-management-2026-07-04 (filed early 07-04, 6 tickets). Guards used +
  restored (dual push-block, reset e5dd863f, tree clean, origin restored).

## Ad hoc
- OR key rotated out of ~/.bashrc → commented/defunct in ~/.unseen_university/akien/akien.credentials.cfg.

✅ CLOSED
