# Slate 2026-06-25

## Notes

## In-flight
NONE

## Planned

## Ad hoc

## Done today

- T-remove-autocompact-calls — Remove all /autocompact CALLS (skill itself stays) (Repo skills/ swept correct; guard green.)
- T-uu-sweep-skill-psql-creds — De-hardcode dead DB creds in skill SKILL.md psql commands (Repo skills/ swept correct; guard green.)
- T-day-close-audit-skill-stale-paths — BUG: day-close-audit (+ other skills) reference retired wild_igor/igor + lab/ paths (Repo skills/ swept correct; guard green.)
- T-skills-single-source-flip — Flip survivor skills to single-source (project-scope .claude/skills) (Single-source flip LIVE + door closed. 49 managed skills flipped from drifted real-dir copies to symlinks into repo skil)
- T-skills-content-merge-survivors — One-time content merge of drifted survivor skills to canonical (Resolved to repo-canonical: repo skills/ verified as the clean fs-store rewrite (D-canonical-memory-consolidation); loca)
- T-skills-prune-deprecated-merge — Delete deprecated skills; fold sprint; reconcile igor-diagnose (Deprecated cognition-debug + debug-pe-chain already deleted from repo + ~/.claude/skills + manifest (verified gone from )
- T-skill-dispositions-investigate — Determine disposition of unclear skills (map-igor, outcome, eval-run, weekly-retro, factory-create, critic, new) (Disposition table recorded. map-igor: KEEP local-only — Igor-runtime full-state snapshot tool (Haiku, writes ~/.unseen_u)
- T-skills-deadstep-followup — Decide fate of 3 never-built day-close helper scripts (All 3 never-built helper-script intents RETIRED — each superseded by an existing mechanism, no capability lost, nothing )
- T-uu-cli-dispatcher — uu <verb> dispatcher — zero-inference view/control CLI (Built bin/uu — zero-inference uu <verb> dispatcher. First-token dispatch + shift + "$@" passthrough (the convention the )
## Day-close summary (2026-06-24 final)
**Done:** Env-vars grand-cleanup phase complete. T-uu-sweep-hostname + T-uu-sweep-instance-name both
CLOSED PROVEN today. DB renamed (Igor-wild-0001→unseen_university), decoupled from tenant via
UU_HOME_DB_NAME. All supporting pieces (rotate password, config-profile, dburl-unify, etc.) closed
(10+ tickets).

**Lesson learned:** Igor-wild-0001 was misnamed substrate-home-DB (not just an igor instance folder).
The DB rename was RIGHT and valuable. The instance-folder sweep was scope creep / rabbit hole — igor
is LAST priority; don't pour care into de-hardcoding an archived instance. That work (T-uu-sweep-instance-name)
is left value-preserving; it waits for the igor phase.

**Next sprint (priority order):** SKILLS (D-skills-two-products, Friday 2026-06-26 presentation) →
DickSimnel → Granny → cleanup → igor (last). Scoped but NOT EXECUTED: content-merge-survivors (4-axis
drift), skill-psql-creds (dead creds + DB name). Deferred: single-source flip (destructive, holds for
Akien approval).

## In-flight (day-close)
NONE. All work from the env-vars phase is durable (committed, pushed). Fresh post-compact session will
sprint skills.

## SKILLS — PHASE A DONE + pre-compact handoff (2026-06-25 ~mid-day; context degraded -> Akien compacting)
Pushed: ec7ae52d (sweep+prune), 2a4dcb53 (bookkeeping). Repo skills/ correctness GREEN under
tests/test_skills_hygiene.py (7 checks: dead-creds->$UU_HOME_DB_URL, instance-paths->$IGOR_INSTANCE_ID,
/decided->/sorted, THEIGORS_HOME->UU_ROOT, /autocompact CALLS removed [skill kept], dead-script steps
removed, audit-workspace + scanner-needles exempted). Pruned: question/concept/migrate-decisions/recover
+ manifest. Closed: T-remove-autocompact-calls, T-uu-sweep-skill-psql-creds, T-day-close-audit-skill-stale-paths.
Filed: T-skills-deadstep-followup. NEW canonical corpus: skills/questions/questions.json (84127d20).

⛔ HARD CORRECTION (Akien, emphatic): **NO SKILLS SYNC. EVER.** Copy-sync between repo skills/ and
~/.claude/skills IS the drift disease that broke day-close. I almost re-proposed it — DO NOT. See memory
feedback_no_skills_sync_symlink. Single-source mechanism = DIRECTORY SYMLINK (spec in T-skills-single-source-flip):
committed in-repo .claude/skills -> skills (project scope) + fallback per-skill ~/.claude/skills/<name> -> repo;
two-tier canonical(repo-symlink)/local-only(Igor-runtime real dirs); skills-sync/run deploy = ensure-symlink, NOT copy.

NEXT (skills, still front-burner, the COMPLETE end-intent, order CC's choice):
- Make fixes take effect LOCALLY via the SYMLINK flip (T-skills-single-source-flip) — NOT sync. This is the
  daily-tooling cure (repo fixes only protect us once ~/.claude/skills links to repo). Destructive-ish (clears
  managed local copies) -> Akien sign-off when executing.
- Remaining prune sub-parts: fold /sprint -> query-ticket+sprint-ticket; resolve igor-diagnose vs diagnose
  (T-skills-prune-deprecated-merge still open).
- Phase B view-layer: T-uu-cli-dispatcher -> device-skills -> readfeed/alarms -> views-retire -> content-merge.
PRIORITY UNCHANGED: skills -> DickSimnel -> Granny -> cleanup -> igor LAST. Igor-wild-0001 = archived Wild0.

- D-decisions-as-guards-anchors-2026-06-25: decisions carry executable guards (immediate test) + parent-narrative anchor (decompose-by-intention, N-of-M, stamped parent invariant/rejected-alt, distilled rollup-on-close) so fresh contexts re-orient without a look-back instruction — T-decisions-as-guards-convention, T-sorted-decompose-by-intention-gate, T-child-close-narrative-rollup-to-parent, T-consequence-decisions-as-guards

## Session close 2026-06-25 (skills Phase A + meta-design)
Done: (1) Skills Phase A — correctness sweep + prune, guard-green (tests/test_skills_hygiene.py), pushed ec7ae52d/2a4dcb53; 3 tickets closed.
(2) Canonical questions corpus seeded — skills/questions/questions.json (84127d20).
(3) HARD CORRECTION recorded: NO SKILLS SYNC EVER; single-source = directory SYMLINK (memory feedback_no_skills_sync_symlink; spec T-skills-single-source-flip).
(4) Meta-design /sorted -> D-decisions-as-guards-anchors-2026-06-25 (4 tickets): decisions carry executable guards (immediate test, vs consequence=delayed) + parent-narrative anchor (decompose-by-intention, N-of-M, stamped parent invariant/rejected-alt, distilled rollup-on-close) so fresh contexts re-orient WITHOUT a look-back instruction.
Next: skills STILL front-burner. The move that makes Phase A fixes live + permanently kills the dual-location bug = SYMLINK single-source flip (T-skills-single-source-flip) — NOT sync, Akien sign-off. Then prune remainder (fold /sprint, igor-diagnose vs diagnose) + Phase B uu CLI (T-uu-cli-dispatcher -> device-skills -> readfeed/alarms -> views-retire -> content-merge). Decisions-as-guards tickets ready when picked. Priority unchanged: skills -> DickSimnel -> Granny -> cleanup -> igor LAST.

## SKILLS SINGLE-SOURCE FLIP — DONE + PROVEN (2026-06-25, post-compact, Akien: "do it right NOW")
The drift cure is LIVE. No more two-copies. Commits f3a2d44d (cure) · e17611f1 (door) · 0776144c (proof).
- FLIP: ~/.claude/skills/<name> flipped from drifted real-dir COPIES → symlinks into repo skills/ for all 49
  managed skills. In-repo .claude/skills -> ../skills (relative) committed = project-scope canonical load.
  Verified: live skills resolve to repo content; loader follows symlinks.
- DOOR CLOSED: installer deploy_skills converted copy→SYMLINK (SymlinkBackend; rsync/copytree backends gone).
  Idempotent ensure-link; unlisted dirs left alone (two-tier). manifest.json regenerated from real repo dirs.
  skills-sync/run docstring corrected. Installer tests updated + dst-is-symlink regression test. 38 tests green.
- DELETED deprecated: cognition-debug, debug-pe-chain (decision step 3). Backup at scratchpad/skills_local_backup_*.tar.gz.
- LOCAL-ONLY PRESERVED (two-tier, untouched): concept, debug, diagnose, health, igor-diagnose, map-igor,
  migrate-decisions, question, readigor, recall, recover.
- Closed: T-skills-single-source-flip (PROVEN: tests/test_skills_single_source_proof.py), 
  T-skills-content-merge-survivors (shipped-unproven: resolved to repo-canonical, no superset-harness yet).
REMAINING (D-skills-two-products): T-skills-prune-deprecated-merge (deprecated deleted ✓; STILL TODO: fold
  /sprint→query-ticket+sprint-ticket, resolve igor-diagnose vs diagnose). Phase B uu CLI view-layer
  (T-uu-cli-dispatcher → device-skills → readfeed/alarms → views-retire). recover repo-status = Phase-A follow-up.

## Session close 2026-06-25 (skills single-source flip, post-compact, "do it right now")
Done: Single-source flip COMPLETE & PROVEN. 49 managed skills ~/.claude/skills/ flipped from drifted
  copies → symlinks into repo (f3a2d44d cure). Installer deploy converted copy→symlink so drift can't
  return (e17611f1 door). Proof: test_skills_single_source_proof.py red→green (60c72cdc + 0776144c).
  Deprecated cognition-debug/debug-pe-chain deleted. Igor-runtime/view skills preserved local-only.
  Closed T-skills-single-source-flip (proven) & T-skills-content-merge-survivors (shipped-unproven:
  repo-canonical verified). Tree clean. All 5 commits pushed.
Next: T-skills-prune-deprecated-merge (fold /sprint→query-ticket+sprint-ticket, igor-diagnose vs
  diagnose) + Phase B uu CLI (T-uu-cli-dispatcher). Skills still front-burner (Friday work demo).
  Priority order unchanged: skills → DickSimnel → Granny → cleanup → igor LAST.

## Skills prune-merge CLOSED PROVEN (2026-06-25, post-flip)
T-skills-prune-deprecated-merge closed proven (commits 00bb3fad fold+guard, d1fb5c9c proof).
- /sprint folded → thin alias: selection now via /query-ticket (canonical), no more raw
  cc_queue.py list grep. Guard: tests/test_skills_hygiene.py::test_sprint_selects_via_query_ticket (red→green).
- igor-diagnose: KEPT DISTINCT — self-deprecated redirect already superseded by /diagnose igor
  (invariant preserved in /diagnose Steps 1-3); LOCAL-ONLY Igor-runtime, left per two-tier,
  deletion deferred to igor phase (igor=last). Not deleted (would violate two-tier + igor-last).
- Deprecated cognition-debug/debug-pe-chain confirmed gone from repo+local+manifest; no stray
  slash-refs (2 cognition-debug hits = MCP-capability/ticket refs, not the skill).
Skills set now tidy for Friday demo. REMAINING D-skills-two-products: Phase B uu CLI view-layer
(T-uu-cli-dispatcher M → device-skills → readfeed/alarms → views-retire) + housekeeping
(T-skills-deadstep-followup, T-skill-dispositions-investigate, T-workflow-index-and-uu-help).

## Skills S-housekeeping CLOSED (2026-06-25, Akien: freeze skills + low-risk polish for Friday)
T-skill-dispositions-investigate (shipped-unproven, investigation):
  map-igor=KEEP local-only (Igor-runtime snapshot, two-tier; fold to `uu device` later);
  outcome=KEEP (core learning-loop workflow skill, not a weekly track);
  eval-run+weekly-retro=FOLD recommendation (already only auto-fire in day-close Friday; don't
  promote as standalone verbs) — actual fold deferred (touches frozen day-close).
T-skills-deadstep-followup (shipped-unproven, retire-intent): all 3 RETIRED, each superseded —
  docs_sync.py→fs-store; validate_files.py→path_moves_monitor.py (day-close-audit 18.8);
  github_sync.py→redundant (tickets are committed+pushed files = already cloud-backed).
  Enforcement = standing guard test_no_missing_script_refs.
DEFERRED (gated on Phase B dispatcher): T-workflow-index-and-uu-help — `uu help` needs the
  uu CLI dispatcher (T-uu-cli-dispatcher); /workflow reframe would advertise non-existent
  device/view families. Waits until after Friday with the rest of Phase B.

## PHASE B started — uu CLI dispatcher DONE+PROVEN (2026-06-25, Akien: wrap skills completely ASAP)
T-uu-cli-dispatcher closed proven (commits 088e5384 impl+test, 84fc8f64 proof).
bin/uu — zero-inference `uu <verb>` dispatcher; first-token dispatch + shift + "$@" passthrough
(chain inherits: uu device/readfeed nest additively). Verbs: mytickets/opentickets read the
CANONICAL FS STORE via ticket_store+ticket_status (⛔ DB is NOT source of truth for any build
artifact — Akien correction; also NOT cc_queue.py list which hides tickets); recall/research →
librarian scripts; help/no-verb lists verbs; bogus → help+nonzero. bin/ on PATH via versioned
profile processor (idempotent); `command -v uu` verified in fresh sourced shell. ⚠️ AKIEN: re-source
your profile (or new shell) for `uu` to appear; ~/bin uu* retirement is yours (no collision).
Proof: tests/test_uu_dispatcher.py (hermetic).
Ungated by this close: T-device-skills-via-uu-device (L), T-uu-alarms-cli.
NEXT: T-workflow-index-and-uu-help (S, now unblocked) → then device-skills (L, igor-touching, heaviest
— surface to Akien). readfeed (M) + views-retire (M) are gated BEHIND device-skills.
