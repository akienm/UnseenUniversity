# Slate 2026-06-23

## Notes

- New (to us) hardware incoming — see ~/.unseen_university/akien/20260623.Apple.txt (Akien's surprise;
  review together). May reshape "what's next" (deploy targets / inference host).
- Skills: repo is canonical; `deploy` is NOT safe until the 19-skill per-axis merge lands (both trees
  half-migrated across theigors→unseenuniversity, /decided→/sorted, .dsb→fs-store, ~/TheIgors→UU).
- Proof-on-close: default to `--shipped-unproven` w/ named lever until proof-emission is wired in.

## Planned

- **Skills 19-skill per-axis merge** (top of T-skills-location-audit) — REVIEW-gated decisions:
  which grader design is canonical (EvaluatorDevice vs inline-subagent in sprint-ticket), day-close
  feature union (gate-sweep + prune + discussion note), productize research/critic/factory-create?
- **Config-identity sweeps** (next batch, now ungated): T-uu-sweep-db-connection-string (L, HIGH-inertia
  db_proxy.py), T-uu-sweep-instance-name (L), T-uu-sweep-hostname (M) → then env-coverage-audit → rotate.
- Bootstrap trio (emitter, inplace-red, close-gate) awaits Akien validation.

## Ad hoc

## Done today

- Skills cleanup (safe portion, 4b5697f1): deleted ADCHelp/decided, promoted query-ticket/mytickets/
  opentickets/concept to repo+manifest, revised harmony doc with the real 4-axis drift finding.
- Day-close 2026-06-22: slate closed, this slate opened.
- Day-close audit (full suite): **210 failed / 8002 passed / 73 skipped / 28 errors** (8:22). Tonight's
  additive changes NOT in the failing set (verified). Failures are pre-existing/environmental
  (inference `openrouter source unavailable`; bus/IMAP integration; .dsb/docs_sync migration churn).
  Filed **T-test-suite-failing-baseline-triage** (M, triage); failure list saved to
  devlab/runtime/memory/daycloseaudit_20260623_failures.txt. NOT blocking — pre-existing baseline.
- D-skills-two-products-2026-06-23: skills = uu CLI (views) + single-source workflow skills — T-uu-cli-dispatcher, T-uu-readfeed, T-skills-views-retire-to-cli, T-skills-prune-deprecated-merge, T-skills-content-merge-survivors, T-skills-single-source-flip, +consequence (audit-proportionality DEFERRED to discussion)
- D-inference-provider-router-redesign-2026-06-23: inference fix=redesign (root cause of dark audits + Igor silence) — 7 tickets + consequence; ollama_cloud/google_free WORK, routing pointed at dead OR

- T-loguru-ownership-to-base — Move loguru/stdlib-intercept ownership from Igor to the base (common-environment logger) (Moved loguru/stdlib-intercept ownership Igor→base; installs per-process at device boot (covers rack via Skeleton + every)
- T-system-alarms-primitive — system_alarms primitive — unignorable, deduped, self-clearing failure surfacing (Flat-file system_alarms primitive: raise_alarm dedups by signature with a caller-breakdown punch-list, logs + drops in o)
- T-system-alarms-fatal — raise_alarm(fatal=True) — report then halt (throws after the drop) (raise_alarm(fatal=True) reports then halts (drops+logs, then raises SystemAlarmFatal); fatal=False stays fail-soft. Proo)
- T-system-alarms-web-panel — ALARMS PANEL — conditional, all web pages + status panels (the loud visual channel) (Black ALARMS PANEL (red inset box, bold-red SYSTEM ALARMS, 3-line scrollable clickable list, 4-line detail, no JSON) inj)
- T-system-alarms-panel-coverage — ALARMS PANEL on the pages that bypass _html_wrap (chat SPA, dashboard, metrics) (ALARMS PANEL now on every page: _inject_alarms_panel inserts the single _ALARMS_PANEL source after <body>, wired into _i)
- T-system-alarms-tmux-nag — tmux nag on new/reopened alarm (out-of-band) for workers running in tmux (Out-of-band notify_new_alarms nags once per new/reopened alarm via tmux send-keys (points at 'uu alarms'); increments do)
- T-inference-resolve-requests-by-tier — Inference resolves by tier + best-available-match + fall-through (fixes Igor path) (Shipped + proven (#1 of original 3 + the alarm): removed the dead hardcoded-openrouter dispatch fallthrough — unknown mo)
- T-workflow-md-source-of-truth — workflow.md as single source of truth; /workflow + uu workflow both output it (skills/workflow.md is the single source of truth (skills/ root, spans all skills); /workflow SKILL.md cats it (no embedd)
- T-skill-write-paths-canonical — Every writing skill (the RUNNING copy) targets devlab/runtime/memory/ — retire lab/ + design_docs write-paths (Writing skills (repo + running ~/.claude copy) emit decisions JSON-only to devlab/runtime/memory/ via memory_emit; /outc)
- T-path-moves-monitor — Day-close path-moves monitor — Python, file-index-backed, tickets/alarms non-canonical writes (Python day-close path-moves monitor: registry (devlab/runtime/memory/rules/path_moves.json) + monitor (devlab/claudecode)
- T-lab-designdocs-ref-sweep — Sweep all lab/ + design_docs/ read-refs to canonical paths — proven by grep zero (Swept retired-path read-refs to canonical across the live fileset. claudecode 1:1 bulk (lab/claudecode->devlab/claudecod)
## In-flight: Two design decisions filed today, nothing mid-sprint. KEYSTONE = D-inference-provider-router-redesign-2026-06-23: root cause of dark audits + Igor silence is the proxy routing every tier to DEAD OpenRouter while ollama_cloud + google_free WORK (self-test confirmed). 7 tickets+consequence, all sprint. NEXT: sprint inference batch — T-inference-provider-billing-model (ungated) -> T-inference-request-priority-split -> T-inference-policy-router (L; finishing it RESTORES Igor, proven via live round-trip). Also pending: D-skills-two-products-2026-06-23 (uu CLI + single-source skills, 6 tickets, T-uu-cli-dispatcher ungated). Mac Studio M1 Max incoming ~Jun27-Jul3 = future local inference host. Day-close 2026-06-22 done; full-suite 210-fail baseline is PRE-EXISTING/environmental (T-test-suite-failing-baseline-triage). Resume by sprinting the inference batch.
- D-system-alarms-and-tier-requests-2026-06-23: system_alarms primitive + tier-based inference requests — 7 tickets + consequence; SUPERSEDES built parts of D-inference (provider/billing/router already existed 2026-06-12); live bug = device.py fallthrough to dead OR

## In-flight: Alarms phase: shipped proven 4 closes (loguru-ownership, system-alarms-primitive, fatal, web-panel). Notify reshaped (email/push dead → visual). Coverage gap filed (chat SPA/dashboard/metrics panel); tmux-nag + uu-alarms-cli queued. Keystone routes: alarms-coverage or inference-restore-igor. Deep-clean substrate work done (logging ownership, alarm mouth, fatal behavior, web visual channel all proven).

## In-flight: 2026-06-23 Done: 7 proven closes (alarms complete + inference keystone #1+alarm); workflow.md source-of-truth; canonical-memory law locked in CLAUDE.md; 11 misfiled decisions migrated + .dsb merged. Next: canonical-memory consolidation design (5 tickets, ready to /sorted) or audit-layer proportionality pass (14 audits vs proof-on-close spine; deferred, never ticketed).

## In-flight: Two design conversations in-flight: canonical-memory consolidation (ready to lock via /sorted) or audit-layer proportionality (audit skills vs proof-on-close, deferred decision).
- D-canonical-memory-consolidation-2026-06-23: all dev-process artifacts -> devlab/runtime/memory/ only; retire lab/ + design_docs; architecture intention-points; path-moves monitor — T-skill-write-paths-canonical, T-retire-decision-folders, T-retire-designdocs-architecture, T-lab-designdocs-ref-sweep, T-architecture-intention-points, T-path-moves-monitor, T-consequence-canonical-memory-consolidation
## In-flight: D-canonical-memory-consolidation FILED (7 tickets). NEXT = sprint T-skill-write-paths-canonical: /sorted Step 6 -> JSON-only via memory_emit (repo skills/sorted Step 7 already dropped; RUNNING ~/.claude copy still has .dsb+lab/design_docs writes = the live misfiler). Also fix /outcome (appends decision .md), /day-close:211 (git add lab/design_docs/), /migrate-decisions (obsolete once .md retired). Propagate via /skills-sync repo->~/.claude. Prove functionally: emit decision -> lands in devlab/runtime/memory/decisions/, nothing under lab/. JSON-only confirmed; architecture intention-points must explain BOTH dev-artifact + graph-tree memory in small bites (T-architecture-intention-points amended).

## In-flight: 2026-06-23 Done: 3 alarms-phase proven (tmux-nag, panel-coverage); inference keystone #1+alarm proven; workflow.md single-source proven; canonical-memory law locked + consolidation design (7 tickets) filed canonically. In-flight: T-skill-write-paths-canonical (#1) scoped precisely — running ~/.claude/skills copy is live misfiler; ready to sprint fresh. Next: Sprint T-skill-write-paths-canonical (dual-copy /sorted + skills rewrite to JSON-only via memory_emit, functional proof).

## In-flight: Consolidation design locked (D-canonical-memory-consolidation-2026-06-23, 7 tickets, decision emitted canonically); two-kinds-of-memory (dev-artifact + graph-tree) captured on #4; JSON-only confirmed; #1 precisely scoped for next sprint.
- T-skill-write-paths-canonical CLOSED (proven, afe46e9b): writing skills emit JSON-only to devlab/runtime/memory/ via memory_emit (repo + running ~/.claude copy — live misfiler dead). Ungated T-retire-decision-folders + T-lab-designdocs-ref-sweep. NEXT in consolidation: T-lab-designdocs-ref-sweep (~80 read-refs, delegate-able) / T-architecture-intention-points (L, both memory kinds) / T-path-moves-monitor / then T-retire-* folder removals. NOTE: repo<->~/.claude skills broadly drifted (~19 skills) — separate from #1, relates D-skills-two-products single-source.
- T-path-moves-monitor CLOSED (proven, 07f8ef55): registry + Python monitor scans git file index, 1 deduped alarm per retired root, wired day-close-audit Step 18.8; fail-soft, read-only. Live tree: 2 grouped alarms (design_docs x108, design_docs_for_igor x1) = exactly what T-retire-* will remove. CONSOLIDATION now: #1 + #5 done; remaining T-lab-designdocs-ref-sweep (M, delegate-able), T-retire-decision-folders (S), T-architecture-intention-points (L, both memory kinds) -> T-retire-designdocs-architecture (S, gated on it). NOTE: skills repo<->~/.claude drift handled by reconciled T-skills-content-merge-survivors -> T-skills-single-source-flip (DIRECTORY LINK, not sync).
- T-lab-designdocs-ref-sweep CLOSED (proven, allowlist grep-zero): claudecode 1:1 bulk delegated to subagent (27 files, 1eb4d6d9); design_docs read-refs repointed by hand. Surfaced+filed 2 logic bugs: T-cc-queue-rollup-misfiles-lab (cc_queue WRITES .md to lab/), T-cc-nightly-reads-decision-md. CONSOLIDATION: #1 #5 #4 DONE. Remaining: T-architecture-intention-points (L, both memory kinds) -> then T-retire-decision-folders + T-retire-designdocs-architecture (deletes, LAST). NEXT = architecture-intention-points.
