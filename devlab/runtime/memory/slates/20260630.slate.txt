# Slate — 2026-06-30

## Rewound? (read this if you were rewound, not compacted)
A rewind rolls your CONVERSATION back but NOT git/this slate — so your memory is now BEHIND reality. Read this slate + `git log --oneline -10`, trust slate+git over your memory, and `git status` for working-tree divergence (a code-rewind can delete committed files — `git restore` fixes it) before acting.

## Summary
(empty — session just started)

## Notes

## In-flight
NONE

## Planned

## Ad hoc

## Done today

- T-igor-preparse-via-or-reroute — Reroute or delete preparse_via_openrouter (SUPERSEDED/withdrawn — scope absorbed by T-igor-delete-local-preparse (expanded to whole preparse concept) per Akien 202)
- T-igor-inner-cc-assess — Delete inner_cc entirely (tool + dreaming usage) — open_question answered: dead (Deleted inner_cc tool (Proxy-bypassing direct-OR call) + training_pass.py (inner_cc-only); retired all 5 dependent featu)
- T-igor-cloud-mode-assess — Delete cloud_mode (old routing, superseded by Proxy budget_gate/rules_engine) (Deleted cloud_mode.py + write-only-dead cloud_ok_override. Stripped is_cloud_training_active gate (embedding backfill un)
- T-igor-delete-orphaned-reasoner-classes — Delete orphaned reasoner classes + relocate surviving consts (Excised 8 orphaned reasoner class bodies (Ollama/OpenRouter/Base/Local/API/Browser/ModelFamily/ClaudeFamily) + winnow tr)
- T-igor-embedder-via-rack — Repoint embedder.py to the rack embeddings generator (SUPERSEDED by D-graph-tree-embeddings-teach-on-miss-2026-06-30 (esp. T-graph-embed-retire-nomic). The repoint reframed e)
- T-graph-embed-verify-existence — Verify whether a current graph-tree embedder actually exists (don't trust 'it's built') (Resolved: no node-set+overlap embedder exists. The activation PRODUCER does exist — WordGraph.text_to_activation_vector()
- T-graph-embed-backfill-existing — Backfill node-activation reps for existing memories (hard prerequisite of nomic deletion) (Moot — Igor is starting over (all old memories killed). There is no pre-existing corpus to strand, so no backfill is nee)
## In-flight: Morning 2026-06-30 check: all clean, branch=origin/main, inference-reroute chain (A/B/C+bypassers) holds proven. Akien morning directives RESOLVE sweep open-questions (all = DELETE, too old): (1) ALL preparse dead (predates word-graphs) — delete whole concept not just local_preparse: gateway preparse purpose + preparse_router + preparse_via_openrouter + use_local_preparse + all call('preparse') callers; T-igor-delete-local-preparse expanded, supersedes T-igor-preparse-via-or-reroute. (2) inner_cc all goes — delete tools/inner_cc.py + dreaming usage. (3) cloud_mode all goes — old routing. (4) embedder->rack embeddings generator (exists, find+repoint). Tickets patched with directives. PENDING: igor hibernation restart = boot test for C (Akien's gate). NEXT (post-compact): execute InferenceProxyOnly sweep — clear deletes first (preparse all, inner_cc, cloud_mode), then embedder->rack + delete-orphaned-reasoner-classes(L). Research project, userbase=Akien+CC, old machinery removable on sight.
- D-dev-pipeline-map-2026-06-30: mapped dev pipeline (vetinari->hubert->granny->builders+verifier+critic) in architecture/cc.0.dev-pipeline.*; fixed /sorted intention-mapping — T-sorted-intention-open-then-map
- D-intentions-store-2026-06-30: create first-class intentions store + backfill from chatlog scan (Haiku, then Akien review + reconciliation) — T-intentions-store, T-extract-intentions-from-chatlogs, T-consequence-D-intentions-store
- D-graph-tree-embeddings-teach-on-miss-2026-06-30: KEYSTONE — LLM grows the graph never answers; embeddings=node-activation, LIKE=node-overlap, nomic retires entirely, teach-on-miss w/ proof-on-write gate. arch: graph-embeddings-teach-on-miss. 9 tickets+consequence (design-then-build).
## In-flight
InferenceProxyOnly sweep COMPLETE: preparse/inner_cc/cloud_mode/orphaned-reasoners all deleted+proven+closed (-1300+ lines); igor is clean Proxy-only substrate. embedder NOT deleted -> reframed as graph-tree-embeddings epic (D-graph-tree-embeddings-teach-on-miss). NEXT: design pass on T-graph-embed-define-representation + T-graph-embed-teach-on-miss (need CC, not Haiku), then verify-existence, then the Haiku-buildable impl chain. Hex (M1 Studio) arriving today = free local inference for the teach loop. Commits local on main, NOT pushed.

## Session close 2026-06-30
Done: Done: InferenceProxyOnly sweep COMPLETE — preparse/inner_cc/cloud_mode/orphaned-reasoners all deleted, proven (grep-gate red->green), and closed (-1300+ lines); igor is a clean Proxy-only substrate. Earlier: dev-pipeline arch map + intentions-store epic filed. Embedder reframed (not deleted) into the KEYSTONE graph-tree-embeddings/teach-on-miss epic (D-graph-tree-embeddings-teach-on-miss, 9 tickets+consequence, arch principle filed). All work delegated to fresh workers; CC stayed control-plane (verify+prove+close).
Next: Next: CC design pass on T-graph-embed-define-representation + T-graph-embed-teach-on-miss (need design, not Haiku), then T-graph-embed-verify-existence, then the Haiku-buildable impl chain. Hex (M1 Studio) arrives today = free inference for the teach loop.

## In-flight: NONE actively executing — clean stop. InferenceProxyOnly sweep done+committed; graph-tree-embeddings keystone epic filed+committed (19 commits local on main, unpushed). Awaiting CC design pass on define-representation + teach-on-miss post-compact.
