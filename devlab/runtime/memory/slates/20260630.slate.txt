# Slate — 2026-06-30

## Rewound? (read this if you were rewound, not compacted)
A rewind rolls your CONVERSATION back but NOT git/this slate — so your memory is now BEHIND reality. Read this slate + `git log --oneline -10`, trust slate+git over your memory, and `git status` for working-tree divergence (a code-rewind can delete committed files — `git restore` fixes it) before acting.

## Summary
(empty — session just started)

## Notes

## In-flight
NONE

## Planned

## Ad hoc

## Done today

- T-igor-preparse-via-or-reroute — Reroute or delete preparse_via_openrouter (SUPERSEDED/withdrawn — scope absorbed by T-igor-delete-local-preparse (expanded to whole preparse concept) per Akien 202)
- T-igor-inner-cc-assess — Delete inner_cc entirely (tool + dreaming usage) — open_question answered: dead (Deleted inner_cc tool (Proxy-bypassing direct-OR call) + training_pass.py (inner_cc-only); retired all 5 dependent featu)
- T-igor-cloud-mode-assess — Delete cloud_mode (old routing, superseded by Proxy budget_gate/rules_engine) (Deleted cloud_mode.py + write-only-dead cloud_ok_override. Stripped is_cloud_training_active gate (embedding backfill un)
- T-igor-delete-orphaned-reasoner-classes — Delete orphaned reasoner classes + relocate surviving consts (Excised 8 orphaned reasoner class bodies (Ollama/OpenRouter/Base/Local/API/Browser/ModelFamily/ClaudeFamily) + winnow tr)
- T-igor-embedder-via-rack — Repoint embedder.py to the rack embeddings generator (SUPERSEDED by D-graph-tree-embeddings-teach-on-miss-2026-06-30 (esp. T-graph-embed-retire-nomic). The repoint reframed e)
- T-graph-embed-verify-existence — Verify whether a current graph-tree embedder actually exists (don't trust 'it's built') (Resolved: no node-set+overlap embedder exists. The activation PRODUCER does exist — WordGraph.text_to_activation_vector()
- T-graph-embed-backfill-existing — Backfill node-activation reps for existing memories (hard prerequisite of nomic deletion) (Moot — Igor is starting over (all old memories killed). There is no pre-existing corpus to strand, so no backfill is nee)
- T-igor-strip-sudo-relay — Strip sudo_relay out of igor/tools — the rack device owns it; Igor reaches it over the bus (Yanked igor/tools/sudo_relay.py + __init__ export; rack device is sole owner. Verified+committed f22f3870: grep tools.su)
- T-router-data-schema — Inference router increment 1 — the per-source + per-call data schema the selector reads (Increment-1 data schema shipped: routing_buckets.py (TIME/DIFFICULTY/COST_CLASS ordered axes + pure mappers task_class_t)
- T-router-selector — Inference router increment 2 — the stateless selector: two bucket-filters + argmin(dollars) (Stateless cost-optimizing selector shipped: route() replaces the binary billing_type sort with TIME-eligibility x DIFFIC)
## In-flight: Morning 2026-06-30 check: all clean, branch=origin/main, inference-reroute chain (A/B/C+bypassers) holds proven. Akien morning directives RESOLVE sweep open-questions (all = DELETE, too old): (1) ALL preparse dead (predates word-graphs) — delete whole concept not just local_preparse: gateway preparse purpose + preparse_router + preparse_via_openrouter + use_local_preparse + all call('preparse') callers; T-igor-delete-local-preparse expanded, supersedes T-igor-preparse-via-or-reroute. (2) inner_cc all goes — delete tools/inner_cc.py + dreaming usage. (3) cloud_mode all goes — old routing. (4) embedder->rack embeddings generator (exists, find+repoint). Tickets patched with directives. PENDING: igor hibernation restart = boot test for C (Akien's gate). NEXT (post-compact): execute InferenceProxyOnly sweep — clear deletes first (preparse all, inner_cc, cloud_mode), then embedder->rack + delete-orphaned-reasoner-classes(L). Research project, userbase=Akien+CC, old machinery removable on sight.
- D-dev-pipeline-map-2026-06-30: mapped dev pipeline (vetinari->hubert->granny->builders+verifier+critic) in architecture/cc.0.dev-pipeline.*; fixed /sorted intention-mapping — T-sorted-intention-open-then-map
- D-intentions-store-2026-06-30: create first-class intentions store + backfill from chatlog scan (Haiku, then Akien review + reconciliation) — T-intentions-store, T-extract-intentions-from-chatlogs, T-consequence-D-intentions-store
- D-graph-tree-embeddings-teach-on-miss-2026-06-30: KEYSTONE — LLM grows the graph never answers; embeddings=node-activation, LIKE=node-overlap, nomic retires entirely, teach-on-miss w/ proof-on-write gate. arch: graph-embeddings-teach-on-miss. 9 tickets+consequence (design-then-build).
## In-flight
InferenceProxyOnly sweep COMPLETE: preparse/inner_cc/cloud_mode/orphaned-reasoners all deleted+proven+closed (-1300+ lines); igor is clean Proxy-only substrate. embedder NOT deleted -> reframed as graph-tree-embeddings epic (D-graph-tree-embeddings-teach-on-miss). NEXT: design pass on T-graph-embed-define-representation + T-graph-embed-teach-on-miss (need CC, not Haiku), then verify-existence, then the Haiku-buildable impl chain. Hex (M1 Studio) arriving today = free local inference for the teach loop. Commits local on main, NOT pushed.

## Session close 2026-06-30
Done: Done: InferenceProxyOnly sweep COMPLETE — preparse/inner_cc/cloud_mode/orphaned-reasoners all deleted, proven (grep-gate red->green), and closed (-1300+ lines); igor is a clean Proxy-only substrate. Earlier: dev-pipeline arch map + intentions-store epic filed. Embedder reframed (not deleted) into the KEYSTONE graph-tree-embeddings/teach-on-miss epic (D-graph-tree-embeddings-teach-on-miss, 9 tickets+consequence, arch principle filed). All work delegated to fresh workers; CC stayed control-plane (verify+prove+close).
Next: Next: CC design pass on T-graph-embed-define-representation + T-graph-embed-teach-on-miss (need design, not Haiku), then T-graph-embed-verify-existence, then the Haiku-buildable impl chain. Hex (M1 Studio) arrives today = free inference for the teach loop.

## In-flight: NONE actively executing — clean stop. InferenceProxyOnly sweep done+committed; graph-tree-embeddings keystone epic filed+committed (19 commits local on main, unpushed). Awaiting CC design pass on define-representation + teach-on-miss post-compact.

## Session close 2026-06-30
Done: Reshaped graph-embeddings epic to Igor-reset reality (verify-existence closed: no node-set embedder exists, the activation producer does; backfill closed moot; retire-nomic widened to ALL consumers both stacks; teach-on-miss target = the EXISTING verifier, not the pushing LLM). RESET IGOR: truncated ~117.7k rows across instance/clan/infra (memories, interpretive_edges 102k, code_index, palace, word-graph, logs), cleared 10 nomic embed-cache files; _migrations + all other devices preserved; Igor was not running. Committed 2507c78b.
Next: DickSimnel — get it from exists -> picks up a Granny ticket, builds, closes. A read-only Explore scan of DS state was launched (re-run it post-compact; its result did not persist). Then act on the single smallest gap it names. Hex (free local inference) lifting the DS cost constraint is the unlock.

## In-flight: NONE in-flight — tree clean, all work committed (2507c78b: epic reshape; Igor corpus wiped via DB truncate, no repo changes). Only a disposable read-only DickSimnel state-scan was running; re-run post-compact.
- D-ds-first-live-build-on-hex-2026-06-30: route devstral worker tier to local Hex + trivial fixture + live smoke — T-ds-local-ollama-route, T-ds-hello-build, T-ds-smoke-pytest
- D-uu-cleanup-2026-06-30: rename flags->run_flags (keep breaker); strip sudo_relay out of igor — T-uu-rename-flags-to-run_flags, T-igor-strip-sudo-relay

## In-flight: Hex (10.0.0.100) LIVE + provisioned: ollama tier roster pulled (minion llama3.2:3b, worker devstral-small-2:24b + qwen2.5-coder:14b, analyst deepseek-r1:14b; 48GB qwen3-coder-next stays cloud as escalation). Passwordless SSH wired. Filed D-ds-first-live-build-on-hex (routing/fixture/smoke) + D-uu-cleanup; T-ds-local-ollama-route widened to full roster + local-first cascade (NOT yet built — until it ships Proxy still routes cloud). sudo_relay stripped from igor (closed). NEXT: build T-ds-local-ollama-route to make Hex tiers routable, OR run the DS keystone smoke on ollama_cloud now (zero routing surgery) to prove devstral drives ToolLoop to DONE.

## Session close 2026-06-30
Done: Hex (10.0.0.100) LIVE + provisioned: local tier roster pulled (minion llama3.2:3b, worker devstral-small-2:24b + qwen2.5-coder:14b, analyst deepseek-r1:14b; 48GB qwen3-coder-next stays cloud as escalation). Passwordless SSH wired (Host hex 10.0.0.100 / igor_ed25519). Reframed T-ds-local-ollama-route around a COST-RANK billing taxonomy (0 graph-tree/1 flat-rate-owned-local/2 free-throttled/3 subscription-metered/4 token-direct) — fixes the inverted flat_rate labels that were the root cause of cloud beating Hex; now L + cross-cutting (reorders ALL routing). Stripped sudo_relay dup from igor (closed shipped-unproven). Rebased 25 local commits onto Hex's incoming sudo_relay path-fix (108fcaf3); modify/delete resolved by keeping the deletion; tree clean, imports green.
Next: Start next sprint on DS/Hex routing. TWO OPEN SCOPE Qs for Akien before building T-ds-local-ollama-route: (1) split the cross-cutting cost-rank taxonomy fix into its own foundation ticket, with Hex-registration depending on it? (2) escalation scope — just (a) Hex-down availability fallback, or ALSO (b) capability-escalation to the 48GB cloud model (needs trigger + re-dispatch, own mechanism)? Alternative fast win: run the DS keystone smoke on ollama_cloud now (zero routing surgery) to prove devstral drives a ToolLoop to DONE. 25 commits unpushed (push only when asked).

## In-flight: Session paused for compaction after integrating Hex's pushed fix; next sprint = DS/Hex routing (T-ds-local-ollama-route) pending Akien's split + escalation-scope calls, or the cloud keystone smoke.
- DESIGN PIVOT (Akien): inference routing = cost-optimizing SELECTOR over data (argmin dollars+time | difficulty+features over capable sources), NOT hardcoded cascades. Complexity in the DATA, not rules. Dissolves the (a)/(b) escalation fork + static cost-rank ladder. Hex-down=system_alarm. NEXT SPRINT: /sorted this as an EPIC -> data-increments (schema -> populate -> trivial selector -> register Hex first). See memory project_inference_cost_optimizing_router. Open params: dollars-vs-time weighting; a-priori difficulty estimation.
- D-inference-cost-optimizing-router-2026-06-30: HARD-AGREE epic — routing = cost-optimizing selector over data (2 categorical bucket-filters urgency->time & task_class->difficulty, argmin dollars, mechanical-failure bump, LIVE re-measure). T-router-data-schema -> T-router-selector -> T-ds-local-ollama-route(reshaped=register Hex) -> T-router-live-resource-read + consequence. "what I was trying to build all along"
- ✅ T-router-data-schema CLOSED (shipped-unproven: stub-first proof-lever) — router increment 1 data schema; 45 tests green; ungated T-router-selector

## In-flight: Router epic in flight: increment 1 (T-router-data-schema) CLOSED shipped-unproven (stub-first proof lever); increment 2 (T-router-selector) ungated + next — the trivial selector replacing the billing_type sort. Building down the chain autonomously.
- ✅ T-router-selector CLOSED PROVEN (red→green @8e814ad4) — stateless cost-optimizing selector (2 filters + argmin); 114 routing tests green; foreground→time-only + google_free>ollama_cloud (intentional); failure-bump descoped→T-router-failure-bump-escalation; ungated increment 3 (register Hex) + increment 5

## In-flight: Router epic: increments 1 (schema, shipped-unproven) + 2 (selector, PROVEN) DONE + committed. Increment 3 (T-ds-local-ollama-route = register Hex, ungated) is next — the payoff that makes Hex routable + lifts DS cost constraint; involves live-hardware wiring (INFERENCE_ENDPOINT→10.0.0.100) + DS device change. Paused for Akien checkpoint before wiring live.

✅ CLOSED
