
## In-flight: T-ds-harvest-corpus-batch IN_PROGRESS — corpus-gen chain link built (harvest_batch.py + 4 curated seeds spanning design-stuck/capability-stuck + hermetic eval-only-seal plumbing proof, red→green confirmed, cb2f7af4). REAL validation batch LIVE in background (bf71yus48): 4 seeds @ cap 12 on free Hex, seals eval-split transcripts to a held-out slice via eval_slice. ON BATCH COMPLETE: check bf71yus48 for BATCH REPORT + class-intent distribution; close T-ds-harvest-corpus-batch (plumbing PROVEN via test_harvest_batch; quality SHIPPED-UNPROVEN pending Akien's human-label pass on the sealed slice — that's the named lever). Then classifier (T-ds-defeating-question-classifier) is un-gated but needs Akien's labels before its quality-eval. Earlier this session: T-ds-first-harvest-run CLOSED, aperture OPEN.

- note: CP6 HOLE + corpus contamination found by eyeballing the first harvest batch (bf71yus48). The domain cwd seam (T-ds-domain-cwd-isolation) threads cwd to Edit/Write, but AgenticLoop._tool_bash runs shell=True — the model issues 'cd ~/dev/src/UnseenUniversity && ...' and absolute Read(/home/akien/...) that ESCAPE cwd and reach the LIVE repo. ALSO the production coding domain hard-orients devstral on the real UU repo (cc_queue.py show reflex, SIGNATURE_MAP), so scratch-seeded toy-codebase context is IGNORED. Evidence: all 5 harvested seeds have 6-11 real-repo refs, ZERO scratch refs. No live mutation occurred (git clean), but an editor-phase Bash write would have landed on the live tree. Consequence: sealed slice stuck-corpus-20260706 is INVALID for the classifier — don't label it. Fix space: run harvest against an isolated CLONE (throwaway repo devstral can safely orient on + edit) with seeds = real-ish UU tickets, OR confine _tool_bash to cwd. | T-ds-harvest-corpus-batch T-ds-domain-cwd-isolation

## In-flight: T-ds-harvest-corpus-batch → HOLD, blocked on ONE root action. First real batch corpus was CONTAMINATED (devstral hard-orients on live repo via absolute paths; cwd seam leaks; 0 scratch refs) — slice quarantined INVALID. Fix built + committed (1eea901f): Akien chose dedicated resettable checkout → provision_ds_sandbox.sh (one-time root: user dicksimnel + throwaway checkout + venv + akien→ds sudo + locks /home/akien from ds) + harvest_batch --checkout mode (runs AS dicksimnel, resets checkout per seed) + real-area seeds (smarter-router/flexible-logging=design; ttl-lru/rd-parser=capability). ONE COMMAND for Akien: sudo bash devlab/claudecode/provision_ds_sandbox.sh — then the batch runs. Plumbing proof green. Feedback captured: stop-babysitting. Earlier today: T-ds-first-harvest-run CLOSED aperture OPEN; filed+gated the corpus-gen chain link.

## In-flight: ROOT CAUSE FOUND + FIXED (Akien: 'stop sending those paths in'): harvest contamination was coding.md hardcoding ~/dev/src/UnseenUniversity in 6 places, NOT a cwd leak. Made prompt paths RELATIVE + no-absolute rule (124553ee, prod-safe). Retired the whole system-user + sandbox-guard detour. harvest_batch --checkout auto-creates a throwaway clone (~/dicksimnel, plain dir, no user/sudo). ONE-SEED VALIDATION PASSED: 5/5 relative tool calls, 0 live-repo refs, live repo clean, devstral works the real router code. T-ds-harvest-corpus-batch back to in_progress, fully autonomous. NEXT (when one-seed run bj1tey2l7 finishes): launch full 4-seed batch → clean sealed slice for Akien label pass. Inert dicksimnel USER still exists from the retired approach (harmless; sudo userdel -r dicksimnel to remove).

## In-flight: T-ds-harvest-corpus-batch CLOSED (shipped-unproven). Clean distinction-bearing corpus sealed: devlab/runtime/memory/eval_slices/stuck-corpus-v2/ (31 entries, hash d3fcf05c, 2 capability + 2 design; joinable to io_corpus by ticket_id). Root cause of contamination was coding.md hardcoding ~/dev/src/UnseenUniversity — FIXED (relative paths, 124553ee). All seeds clean, live repo untouched. Distinction is visibly present (design=read-heavy exploration/no edits; capability=implement+test-fix churn). Auto-ungated T-ds-defeating-question-classifier (L, needs-deep-smell). AKIEN ACTION (async, no rush): human-label the stuck-corpus-v2 transcripts (design vs capability ground truth) — the reality-uncoupled eval the classifier grades against. NEXT BUILD (best started FRESH — L/HIGH-design-risk, long session now): classifier plumbing half (design_stuck->drop-ticket, capability_stuck->no-drop) is hand-fixture buildable WITHOUT labels; quality-eval waits on labels. Also inert: dicksimnel user (harmless, sudo userdel -r dicksimnel to remove) + ~/dicksimnel throwaway clone (reusable by next batch).
