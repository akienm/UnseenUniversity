# Slate 2026-07-06

## Done today

- **T-ds-harvest-corpus-batch** CLOSED shipped-unproven (8f856ca3) — clean, distinction-bearing stuck corpus generated + sealed: `stuck-corpus-v2` (31 entries, hash d3fcf05c, 2 capability + 2 design), joinable to io_corpus by ticket_id, sealed before the classifier exists (temporal firewall). Plumbing PROVEN (tests/inference/test_harvest_batch, red→green). Quality shipped-unproven pending Akien's human-label pass.
- **fix(coding-prompt)** (124553ee) — ROOT CAUSE of the harvest contamination: coding.md hardcoded `~/dev/src/UnseenUniversity` in 6 places; devstral prepended it to relative signature-map paths → wandered the live repo. Made all prompt paths RELATIVE + explicit no-absolute/no-cd rule. Production-safe (prod cwd=_REPO_ROOT resolves identically). Akien's steer: "stop sending those paths in."
- **harvest_batch.py** built (cb2f7af4) — batch runner: curated real-area seeds (smarter-router/flexible-logging=design-stuck; ttl-lru/rd-parser=capability-stuck), `--checkout` auto-creates a throwaway clone (~/dicksimnel, plain dir, no user/sudo), reset pristine per seed. Hermetic eval-only-seal plumbing proof red→green.
- Filed **T-ds-harvest-corpus-batch** into D-ds-harvest-mode-coding-loop (57539dd8) — the discovered corpus-gen chain link (classifier had assumed tickets 1+2 generate the corpus; they were the machine). Re-gated the classifier on it; closing it auto-ungated **T-ds-defeating-question-classifier**.
- First real batch (bf71yus48) was INVALID/contaminated (0ecbf92d) — caught by eyeballing (advisor discipline); slice `stuck-corpus-20260706` quarantined. Two wrong-layer detours (dedicated system user 1eea901f, then a sandbox-guard) retired once the root cause (prompt) was found.

- T-review-builder-process-guardrails — Review design: prior-art gate in /sorted + every-flag-stamps-io_corpus invariant (Design review complete: both guardrails KEEP+MODIFY. Prior-art gate -> fold into audit-design, trigger on decision-class)
- T-aider-builder-first-run — Aider builder — first headless run against Hex on a self-contained task (the known-quantity control arm) (aider proven viable on Hex: devstral-24b GREEN 35.6s, qwen3-coder:30b GREEN 16.6s (2.15x faster), both one-shot on the T)
- T-aider-multifile-orientation-test — Aider builder — multi-file coordinated-change task (the orientation gate DS died on) (aider GREEN on multi-file coordinated change, BOTH models: qwen3-coder:30b 5/5 in 17.0s, devstral-24b 5/5 in 38.8s. NO f)
- T-aider-device-live-proof — Aider device live proof — implement checkout on a throwaway clone (Aider.0[qwen3-coder:30b]: gate PASS branch=aider/T-aider-device-live-proof-1783379151 files=[shop/models.py,shop/service)
- T-aider-device-live-proof-2 — Aider device live proof 2 — device closes end-to-end (Aider.0[qwen3-coder:30b]: gate PASS — branch=aider/T-aider-device-live-proof-2-1783379485 files=['shop/models.py', 'shop)
- T-aider-rack-device-wrapper — AiderDevice — wrap aider as a rack builder (Granny-dispatched, throwaway-clone, branch-not-main, CC-validated) (AiderDevice shipped: devices/aider/ (runner standalone+bus-free with own __main__ = Windows-drivable; device/shim/listen)
## Notes

- Root cause of contamination was PROMPT path injection, not a cwd-seam leak — fix is at the source (relative paths), no dedicated user / sudo / guard needed.
- Corpus carries a VISIBLE distinction: design-stuck = read-heavy exploration, no convergence, no edits; capability-stuck = implement + test-fix churn. (Not pre-judging labels — Akien's pass is ground truth.)
- **AKIEN async action (no rush):** human-label `devlab/runtime/memory/eval_slices/stuck-corpus-v2/` transcripts (design vs capability) — the reality-uncoupled eval the classifier grades against.
- Inert cleanup: `dicksimnel` user (harmless — empty home, no sudoers, home perms untouched; `sudo userdel -r dicksimnel`) + `~/dicksimnel` throwaway clone (reusable by the next batch).
- Feedback captured to memory: stop-babysitting (minimize questions/hard-stops; on a real blocker hand ONE reviewable artifact).

## Session close 2026-07-06
Done: Delivered the DS harvest corpus. T-ds-harvest-corpus-batch CLOSED shipped-unproven — a clean, distinction-bearing stuck corpus sealed as stuck-corpus-v2 (31 entries, 2 design + 2 capability), ready for Akien's label pass. The hard part was tracing the first batch's total contamination (0 scratch refs) to its real root: the coding domain prompt hardcoded ~/dev/src/UnseenUniversity, so devstral wandered the live tree. Fixed by making prompt paths relative (124553ee, prod-safe). Two wrong-layer detours (a dedicated system user, then a sandbox guard) retired once Akien pointed at the source ("stop sending those paths in"). One-seed + full-batch validation: all seeds relative-only, 0 live-repo refs, live repo untouched. HEAD b6101648, clean pushpoint.
Next: T-ds-defeating-question-classifier (L, needs-deep-smell) — auto-ungated. Plumbing half (design_stuck→drop-ticket, capability_stuck→no-drop) is hand-fixture buildable WITHOUT labels; the classification-quality eval is gated on Akien's label pass over stuck-corpus-v2. Best started FRESH (L/HIGH-design-risk).

## In-flight
NONE — clean pushpoint (HEAD b6101648, tree clean, all pushed). Classifier ungated, awaiting Akien's labels for its quality half.

✅ CLOSED

## Post-close addendum — Fable session (2026-07-06 evening; day-close already ran, this block is the hand-off)

- **Fable strategic review filed + pushed (b48d045c):** `notes/cc.0.fable-review-builder-levers-2026-07-06` — remaining aider steals (edit-format ladder + whole-fallback NOW LIVE since editor is the bottleneck; per-model format conformance; commit-per-edit; external benchmark slice), plan-as-IR (nexus caches packet-fingerprint→plan→verdict triples; T-nexus-write-path-ds-architect-loop is underpriced), Cherny deltas (flag sprawl → every-flag-stamps-io_corpus invariant), skills tuning (prior-art gate in /sorted; funnel-as-CI), aider verdict = **control-arm first, fleet gated** (2×2: {DS, aider} × {devstral, qwen3-coder:30b}; laptops are shells, inference = Hex/Ollama Cloud; UU-swarm spin-up stays a separate gated decision).
- **Order of attack is IN THE NOTE** — Phase 0: label page ✓ / funnel-as-CI / the 2×2. Phase 1: edit-format ladder, classifier. Phase 2: nexus plan-cache, proxy-endpoint + AiderDevice, replay wind-tunnel.
- **Two ticket DRAFTS live inside the note** (T-funnel-2x2-model-harness-confound, T-stuck-corpus-label-page-*retroactive*) — **Opus: run them through /sorted + audit-ticket, do NOT re-derive or re-file from scratch.**
- **Label page BUILT by Fable** (Akien-sanctioned direct build; S, flat-file, no workflow entanglement): `devlab/claudecode/stuck_corpus_label_page.py` → generates `eval_slices/stuck-corpus-v2/label_page.html` (gitignored, regenerable). Validate/install round-trip tested (good passes, unknown-id/missing/bad-value rejected). **Akien flow:** open page → d/c/u keystrokes (shift = whole run) → Export → `python3 devlab/claudecode/stuck_corpus_label_page.py --validate ~/Downloads/labels.json --install`. When labels.json lands in the slice dir, the classifier's QUALITY half ungates.
- **Next (Opus):** /sorted the two drafts from the review note, then Phase 0 order. T-ds-defeating-question-classifier plumbing half remains hand-fixture buildable regardless of labels.

- D-fable-builder-empirical-program-2026-07-06: adopt Fable game plan (Phase 0 empirical spine) — T-qwen-vs-devstral-architect-probe (S,0.7,head), T-funnel-as-ci (M,0.6), T-funnel-2x2-model-harness-confound (M,0.55,gated), T-consequence-* (gate 2026-07-20). Label page already delivered (6394a341). Phase 1/2 held as gated followups in the decision narrative; T-nexus-write-path-ds-architect-loop already exists (recommend priority bump).
- T-review-builder-process-guardrails (S,0.75) filed — the two standing-habit guardrails (prior-art gate in /sorted; every-flag-stamps-io_corpus invariant) are now a high-pri design-review TICKET, not narrative prose. Decision narrative updated to point at it. Feedback captured: no recommendations in a narrative (file a ticket).

## In-flight (Fable session, post-close addendum)
T-review-builder-process-guardrails CLOSED shipped-unproven (design review): both guardrails KEEP+MODIFY. Prior-art gate -> fold into audit-design, trigger on decision-CLASS not ticket-size = T-sorted-prior-art-gate-in-audit-design (M,0.5). Flag-stamping -> confirmed io_corpus gap (loop-shape flags aci_mode/plan_mode/critic_enabled/history_window_turns/tool_names absent from the record), invariant = a STRUCTURAL TEST introspecting AgenticLoop.__init__ params = T-agentic-loop-flags-in-corpus (M,0.6). Both wired into D-fable-builder-empirical-program. aider source now LOCAL at /home/akien/dev/src/aider (git checkout v0.86.3.dev, NOT pip) — folded into the 2x2 ticket + it is the edit-ladder primary source. NONE in-flight; clean pushpoint HEAD 979b30d9, all pushed.

## In-flight (aider builder sprint — MILESTONE)
AIDER PROVEN VIABLE as a swarm builder. 4/4 GREEN on Hex ($0): ttl+checkout x devstral+qwen. Multi-file checkout (NO files pre-added -> aider found them via repo map + coordinated 2-file edit) = the exact orientation DS died on. qwen3-coder:30b 2.2x faster than devstral, both correct. Harness gap RESOLVED (same model, 0 edits -> GREEN by swapping the loop); model lever PRICED (qwen wins). Runner: devlab/claudecode/aider_smoke.py (--task ttl|checkout --model X, reproducible). Closed T-aider-builder-first-run + T-aider-multifile-orientation-test (both shipped-unproven, behavioral red->green via runner). Note: aider-first-run-harness-gap-confirmed-2026-07-06 (full matrix). NEXT = T-aider-rack-device-wrapper (L, triage, HIGH-inertia — new device + Granny dispatch; 4 design Qs for Akien in the ticket; best FRESH session). Clean pushpoint, all committed.

## Session close 2026-07-06 (Fable session — evening, after the earlier day-close)
Done: The builder problem (open since March) got a WORKING ANSWER. (1) Ran Fable as strategic reviewer -> adopted its game plan (D-fable-builder-empirical-program-2026-07-06, Phase-0 spine filed). (2) Sprinted + closed the guardrails design-review -> spawned T-agentic-loop-flags-in-corpus + T-sorted-prior-art-gate-in-audit-design. (3) THE MILESTONE: proved aider is a viable swarm builder — 4/4 GREEN on Hex ($0), single-file + multi-file (orientation, no files pre-added, found via repo map) x devstral + qwen, where the DS loop produced 0 edits. Harness gap RESOLVED (the wall was the loop, not the 24B model); model lever PRICED (qwen3-coder:30b ~2.2x faster, both correct). Runner devlab/claudecode/aider_smoke.py; note aider-first-run-harness-gap-confirmed-2026-07-06; memory project_aider_builder_viable. (4) With Akien's design answers, made T-aider-rack-device-wrapper sprint-ready (HIGH-inertia PRE-APPROVED: subprocess / 1-2 CPU-gated instances per laptop / black-box first / auto-gate + spot-checks) + filed T-aider-swarm-deployment (4 boxes = up to 8 builders). (5) Drafted the Fable last-day north-star brief (T-fable-northstar-port-and-compile-brief) + seeded 2026-07-07 slate to fire it FIRST. Standing directive captured: intention -> answer design Qs -> WALK AWAY (never ask keep-building). All pushed, clean pushpoint HEAD 9b62bbba.
Next: 2026-07-07 FIRST — fire T-fable-northstar-port-and-compile-brief (Fable last day); Akien adds cortical-columns specifics pre-fire. THEN /sorted its output + sprint T-aider-rack-device-wrapper fresh.

## In-flight
NONE — clean pushpoint (HEAD 9b62bbba, tree clean, all pushed, origin reachable). Tomorrow seeded to fire Fable first. Aider proven; device pre-approved; swarm specced.

## Ad hoc (2026-07-06 PM — Akien steer: rackmount aider shim is critical path to a Windows aider-builder)
- T-aider-rack-device-wrapper (L, HIGH-inertia PRE-APPROVED) — IN PROGRESS. Pivoted from T-agentic-loop-flags-in-corpus (unclaimed clean).

## MILESTONE (2026-07-06 PM) — the rackmount aider shim SHIPPED
- **T-aider-rack-device-wrapper CLOSED (shipped-unproven).** devices/aider/ built in 4 committed stages (a026c2b3 runner+gate / e9b2e4f7 device+shim+listener / ffb0647a Granny wiring / bc8160d6 shipped-unproven close). PROVEN: 66 contract tests + runner end-to-end on Hex real clone (qwen 24.8s) + LIVE device end-to-end (real queue ticket -> AiderDevice -> aider qwen@Hex -> gate PASS 13.1s -> status=closed).
- **runner.py is standalone + bus-free with its own __main__** — the Windows-drivable core: "python -m unseen_university.devices.aider.runner --repo R --message M --test T". This is what Akien spins up on a Windows box.
- **Findings surfaced by the live proof (all ticketed):** (1) editable-install MetaPathFinder beats PYTHONPATH -> clone tests import the ORIGINAL tree (fake-green trap) -> T-aider-real-uu-ticket-proof needs clone-test isolation. (2) branch-builders structurally cannot emit a HEAD-valid proof (impl on unmerged clone branch) -> honest close is shipped-unproven; real proof at merge time -> T-builder-merge-time-proof (builder-wide: DickSimnel has the same plain-close gap). (3) no builder load-balancing -> T-granny-builder-load-balancing (aider is Aider-tag opt-in for now). (4) pre-existing test_core_values stale igor path -> T-fix-core-values-igor-brainstem-path.
- **T-aider-swarm-deployment auto-ungated** (gate was this ticket).
- Follow-ups filed under D-fable-builder-empirical-program: T-builder-merge-time-proof (0.6), T-aider-real-uu-ticket-proof (0.55), T-granny-builder-load-balancing (0.4), T-fix-core-values-igor-brainstem-path (0.2).

## In-flight (post-aider-device)
NONE — T-aider-rack-device-wrapper closed shipped-unproven; 4 follow-ups filed; all committed+pushed (HEAD a577ae5d + memory/slate commits). Windows-drivable runner ready for Aks Windows box.
