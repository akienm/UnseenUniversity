# Slate — 2026-07-02

## Done today
- T-domain-owns-loop-and-escalation (XL) — shipped-unproven @b3ccc1f3+91ae4b73; ONE shared AgenticLoop + pluggable codec, BaseDomain.run() single escalation owner, 16/16 money-safety parity proven
- T-thin-ds-to-domain-consumer (M) — shipped-unproven @165a52b3; DS now a pure coding-domain consumer (deleted prompt/skill/loop glue)
  → D-domain-object-encapsulation batch COMPLETE 3/3 (base @38768e41 closed 07-01); consequence ticket date-gated 2026-07-15
- Filed T-fix-routing-compiler-add-compiled-rule (pre-existing selector fail, pri 0.2)
- DESIGN SESSION (Akien's accumulated dump → sorted): captured the compiled-inference thesis + filed alarm tickets + captured the big refactor for review.

- T-capability-mixin-coding-capability — Introduce CapabilityMixin + CodingCapability (thin mixin HAS-A a Domain, delegates) (Introduced unseen_university/capabilities/ — CapabilityMixin (thin HAS-A a Domain, lazy _domain, no __init__) + CodingCa)
- T-codebuilder-compose-ds — Compose CodingCapability onto the coding worker (DickSimnel) — prove the mixin delegation path (DickSimnelDevice now composes CodingCapability (class DickSimnelDevice(CodingCapability, BaseDevice)); _run_inference de)
- T-worker-instance-identity — Worker instance identity surface: instance_name = abbrev.number; agent_id = self.instance_name (IdentityMixin (capabilities/identity.py) gives a worker instance_name='<abbrev>.<number>'; DickSimnel composes it (insta)
- T-alarm-callsites-groundloop-db-install — Give ground_loop / DB / installer a mouth: raise_alarm at three should-not-happen callsites (raise_alarm wired at 3 should-not-happen callsites (5 insertions): ground_loop supervisor import+runtime failure (ground)
- T-vetinari-owns-alarm-escalation — Vetinari escalates unresolved system alarms into Akien's feed / a Claude-readable channel (Vetinari's periodic sweep (DirectiveListener ~5min block) now calls sweep_system_alarms(), which reuses the existing not)
- T-vetinari-alarm-retry-default-path — Fix hollow retry contract: Vetinari alarm escalation retry-on-failure is dead on the default (production) channel-post path (_default_channel_post returns bool (False on swallowed failure); _escalate_alarm treats 'posted is False' as failure so )
## Filed today (sprint, NOT done)
- T-alarm-callsites-groundloop-db-install (M) — alarm SOURCES: raise_alarm at ground_loop-start / db-unreachable / install-permission-denied; + docstring principle "an alarm = anything Akien and Claude must look at together". Linked D-system-alarms-and-tier-requests.
- T-vetinari-owns-alarm-escalation (M) — alarm SINK: Vetinari (already the administrator) escalates unresolved alarms to Akien's feed / a Claude channel; wires the orphaned system_alarm_notifier. Depends (soft) on the sources ticket.

## Captured for review (durable, NOT yet work)
- architecture/compiled-inference-thesis (thesis note) — three-layer starvation model (resolver→0 / proof-obligation→partial / gates CP1–CP6→never); contradiction-not-veto; warden(moodless invalidation)-vs-evaluator(appraisal dial) split; cortical columns → reference-frames-in-the-fascia ("same question" = same frame-position, not vector-cosine); decompose→triage "starvation gate" pipeline stage; intent-preservation keystone; belief-as-inspectable-tool. Source: ~/.unseen_university/akien/20260702.ClaudeWebOnCorticalColumnsAndEachQuestionATreeAndPrediction.txt
- decision D-agent-capability-mixins-over-domains-2026-07-02 (status: open, spawns NO tickets yet) — THE BIG NEXT REFACTOR. Agent(root) + capability = MIXIN that HAS-A Domain object (CodingCapability has-a CodingDomain; compose, not is-a — don't re-fuse what D-domain-object-encapsulation just split). CodeBuilder = Agent + CodingCapability + glue. Domains generalize (coding → research/review/…). Three layers become KINDS OF MIXIN, not an inheritance spine. Retire Discworld CLASS names for functional ones BUT preserve the stance-vs-capability signal (the names silently encoded judgment-never-starves vs resolver-starves). Vetinari=strict admin (wrong to interview); Moist=people-person interviewer/intake (doesn't exist yet). Open Qs: compose-vs-is-a confirm, instance-handle de-Discworld?, layer/role taxonomy expression, Agent-vs-BaseDevice relation + migration blast radius.

## Notes
- Two filed tickets carry advisory warnings (missing Completion-criteria section / `I intend that…` line) — cosmetic, add on claim.

## Session close 2026-07-02
Done: Completed D-domain-object-encapsulation batch (3/3). Then a full design session from Akien's accumulated dump: filed 2 system-alarm tickets (sources + Vetinari sink), captured the compiled-inference thesis as an architecture intention-point, and captured D-agent-capability-mixins-over-domains as a decision-in-progress for review (the largest-yet-but-simplifying refactor: Agent + capability-mixins-over-Domains + de-Discworld naming).
Next: FRESH SESSION to tackle D-agent-capability-mixins-over-domains — start by reconciling its 4 open questions (compose-vs-is-a, instance handles, layer/role taxonomy, Agent↔BaseDevice migration order) via the interview/reconcile step BEFORE spawning any tickets. Do NOT mass-file the rename. Alarm tickets (sources+sink) are sprint-ready if a build session is wanted instead.

## In-flight
NONE — clean break for a fresh-session refactor.

✅ CLOSED

---

## Session 2 (2026-07-02) — RECONCILE of D-agent-capability-mixins-over-domains

Ran the interview/reconcile on the big refactor (the slate's Next). Outcome: the captured decision was TWO orthogonal threads; SPLIT them.

**Decompose audit (the load-bearing check):** of the 7 character-named devices, exactly ONE (DickSimnel) collapses to `Agent + capability-mixin`. The other 6 are governance/dispatch/scheduler/reasoner-proxy/dev/validator — each genuinely-unique. So capability-mixins is a WORKER pattern, narrow and real, NOT an everything-collapses claim. (Avoided the hollow-collapse trap.)

**Resolved (A1–A4):**
- A1 compose/HAS-A ✓ (CodingCapability has-a CodingDomain; don't re-fuse the split).
- A2 handles stay terse/provisional (no handle rename); only CLASS names de-Discworld, in stream B. Device→instance-name web page later (NOT ticketed). Headology-for-intake noted (Granny is NOT the interviewer).
- A3 taxonomy: TYPE + mixin category declares layer — plain BaseDevice=utility / Agent+CapabilityMixin=resolver(starves) / Agent+GovernanceMixin=judgment(never starves) / Agent+orchestration=dispatch·schedule.
- A4 (AKIEN CORRECTION): **Agent is a SUBCLASS of BaseDevice, NOT a rename.** BaseDevice stays universal root; utilities stay plain devices. Blast radius shrinks — introduce Agent(BaseDevice), reparent only ~dozen agentic devices, incrementally. Order: prove-on-DS (stream A) → Agent subclass+reparent (B) → de-Discworld 6 + GovernanceMixin (B). "Everything is mixins over Agent, no inheritance spine" (Akien).

**Durable output:**
- Decision A reconciled → emitted (status=reconciled, scoped to worker capability-mixin pattern). Spawns tickets.
- Decision B new+GATED → D-agent-subclass-and-de-discworld-2026-07-02 (Agent subclass + de-Discworld 6 + GovernanceMixin; open Qs B1–B4; held behind A's proof; spawns NO tickets).
- Filed stream A tickets (audit-ticket PASS, amendments applied): T-capability-mixin-coding-capability (S), T-codebuilder-compose-ds (M, structural-proof discipline baked in per proof_emitter-cant-prove-refactors), T-consequence-agent-capability-mixins (S, gate 2026-07-16).
- PlanningMixin deliberately NOT ticketed (no consumer yet = speculative).

**Skipped /sorted deliberately** — no /design marker this session, so /sorted would sweep the whole convo incl. gated Decision B and mass-file the rename. Filed A directly instead.

## Session 2 close
Next: **stream A is sprint-ready** — /sprint-batch decision:D-agent-capability-mixins-over-domains (T-capability-mixin first, then T-codebuilder-compose-ds; consequence ticket is date-gated). Stream B (D-agent-subclass-and-de-discworld) stays GATED until A proves the pattern — reconcile B1–B4 only after A closes. Alarm tickets (T-alarm-callsites…, T-vetinari-owns-alarm-escalation) also still sprint-ready.

## Session close 2026-07-02
Done: Reconciled D-agent-capability-mixins-over-domains via decompose audit → Decision A (worker capability-mixin pattern, 3 tickets filed) + Decision B (Agent subclass + de-Discworld, GATED). Decision hygiene complete.
Next: Compact, then /sprint-batch on stream A tickets + alarm tickets.

## In-flight: NONE — stream A sprint-ready, stream B gated. Reconcile durable and committed.

## In-flight: T-capability-mixin-coding-capability CLOSED PROVEN (proof 66c18b2c). Ticket 2/2 T-codebuilder-compose-ds next: compose CodingCapability onto DickSimnel, structural-proof discipline.

## In-flight: T-codebuilder-compose-ds CLOSED PROVEN (proof fc1b2e36). Stream A build 2/2 done — capability-mixin pattern proven on DickSimnel. Decision A 2/3 shipped; consequence ticket date-gated 2026-07-16.

## Session close 2026-07-02
Done: Shipped stream A of D-agent-capability-mixins-over-domains: T-capability-mixin-coding-capability + T-codebuilder-compose-ds, both CLOSED PROVEN (stub-first red->green; proofs 66c18b2c, fc1b2e36) — capability-mixin worker pattern proven on DickSimnel. Then a design session: captured 2 decisions (D-worker-instance-identity, D-shim-frontdoor-on-groundloop) + filed 3 tickets. Filed T-outcome-check-reads-fs-store (palace->fs drift, 0.2).
Next: Build runway is CLEAR. Sprint-ready: T-worker-instance-identity (S), alarm tickets (T-alarm-callsites-groundloop-db-install, T-vetinari-owns-alarm-escalation). T-shim-frontdoor-on-groundloop is open_questions (resolve Q1-Q3 interface before its downstream tickets). Stream B + consequence checks still gated.

## In-flight: NONE — all work durable + pushed (HEAD 2d14e641). Stream A proven; design captured as decisions+tickets; runway clear for a post-compact build sprint.

## Session close 2026-07-02
Done: Shipped all 3 sprint-ready runway tickets PROVEN: T-worker-instance-identity (IdentityMixin -> DS.0, agent_id off the class-id literal), T-alarm-callsites-groundloop-db-install (raise_alarm at 3 sources; db-unreachable flat-file/no-DB invariant proven x3), T-vetinari-owns-alarm-escalation (sink reuses notify_new_alarms, no parallel dedup; proven x2). The 2 M alarm tickets' writing was delegated to Haiku; CC owned review+proof+close. Filed T-fix-igor-layer3-4-test-seed-imports (pre-existing collection errors, 0.2).
Next: Runway's next layer is the front-door: T-shim-frontdoor-on-groundloop stays open_questions until Q1-Q3 (Ground-Loop-per-host / shim-attach interface / wake protocol) resolve; its downstream tickets (shim-lease-instance-numbers, granny-wake-not-launch, granny-mru-and-stall-alarm) file after. Stream B (D-agent-subclass-de-discworld) + consequence checks (2026-07-16, 2026-07-30) still gated. Note: savestate 'run' must be invoked as ~/.claude/skills/savestate/run, not from repo root.

## In-flight: Batch complete: all 3 runway tickets (identity + 2 alarm) shipped PROVEN and pushed (HEAD 75cdfff8). Tree clean. No work in flight.

## In-flight: Post-close addendum: advisor review of the Vetinari escalation caught a hollow retry-proof (default channel-post path swallowed failures → alarm always stamped notified). Filed + fixed + proved T-vetinari-alarm-retry-default-path (4e217094). All 7 escalation tests green. Tree clean, nothing in flight.

## In-flight: Design session on shim front-door: resolved all 3 open Qs (GL per-host, runme.py attach, idle-timeout sleep). Discovered: singleton=pool-of-1 (max_instances property); tag-based capability selector converges with inference router later (deferred). Leasing algorithm: per-class free-list (live object + PID fallback), rebuild on front-door restart. Data dirs: devices/<classname>/ (shim) vs devices/<abbrev>.<number>/ (instances). Idle-sleep via bus ask. Full buildable surface mapped: front-door attach, leasing+singleton, idle-sleep, Granny-wake-not-launch, MRU/stall, deferred convergence. Ready to ticket or design further (wake verb/ack shape, Stream B/de-Discworld).
