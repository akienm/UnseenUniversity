# Slate — 2026-06-16

## Notes
- Web server is now self-healing via Ground Loop (systemctl --user, linger already on — no sudo). 3 layers: backend crash→proxy respawn, daemon crash→systemd Restart, reboot→user unit at boot. Reboot path *should* survive (unit enabled + linger on) but is NOT yet validated against a real reboot.
- Granny dual-dispatch (CC.0 + DS) is safe overnight: live config has CC.0 launch_cmd=null → Granny can NEVER subprocess-spawn a CC. dispatch.py's CC-spawn fns are dead code (ticketed for removal).
- CC.0 left .false overnight on purpose — no sprint-CC consumes the cc.0 mailbox, so available would just churn cooldowns. Press /available on when a CC consumer is ready.

## In-flight

## Planned

## Ad hoc
- T-granny-dispatch-deadcode-spawn (sprint): remove dead CC-spawn fns from dispatch.py (footgun)
- T-ground-loop-proxy-passthrough-status (sprint): proxy masks backend 4xx/5xx as 502
- T-cc-consumer-groundloop-wiring (sprint): CC.0 consumer (CCWorkerListener/runme.py) is built+SAFE (notify-only since ca433bd7) but orphaned — nothing starts it, so CC.0 bus dispatches aren't consumed. Add a daemon-mode Ground Loop descriptor (symmetric w/ web_server). GO-LIVE ATTENDED: ExecStartPre auto-deploy would auto-start it on next GL restart, so descriptor stays OUT of config/ground_loop/ until Akien present.
- T-per-ticket-checkpoint REFRAMED (M→L, role master, worker claude): now "clear-to-base context via native /rewind". Mechanism = bookmark post-/context-load base, drive `/rewind → Restore conversation` between tickets via send-keys (like uucompactclaude) — excise ticket delta, base never re-tokenized, code+commits intact. Research-confirmed (claude-code-guide): /rewind keeps code, /compact is lossy, JSONL surgery unsupported, hooks can't trigger rewind. Fork backend DEFERRED (spawn-CC risk). clear_to_base runs PRE-task (don't trust prev task ended cleanly). Consequence ticket + decision doc updated. CONSTRAINT: CC.0 OK, CC.1 OK, CC.n NOT APPROVED — 2nd builder gated on Akien.
- T-per-ticket-usage-metrics FILED (M, master, claude, prio 0.6, decision D-architecture-as-code-...): per-ticket token/usage attribution + Postgres store keyed by ticket id. THE critical-path unblocker for cost-aware builders — estimator + claim-gate + burn-breaker all downstream of it. Budget/burn tools exist but lack per-ticket attribution.
- Builder employment order (design, decision doc): CC.0→designer, DS.0→primary builder (gated on DS-up-to-CC-level validation + trust-tracking), CC.1→overflow/reach-for-anything (only 'stop' = usage dropping too fast). Cost-awareness intention (Akien's words) captured + compiled.
- REFLEXIVE REALIZATION (Akien): the dev process IS the architecture-as-code compiler run by hand — joins user intentions ⊕ system-embodied architectural intentions → decisions → tickets → code. Monthly-Opus session = manual compiler run. Process is the prototype of the product. Saved as project_intention_compiler_reflexive.

## Done today
- DS 2ND BUILD — CODE RIGHT, CLOSED PREMATURELY (key trust-tracking finding). Dispatched T-queue-view-akien-not-legacy → DS ($0 devstral, commit 03277859, closed). DS's code change was scope-correct: lab/claudecode/queue_view.py akien label → "👤 Akien (yours)", moved out of legacy block, minimal diff. BUT the ticket explicitly asked to "confirm no other renderer still tags akien legacy" — and there WAS one: the LIVE /queue page renders via a SEPARATE _STATUS_LABEL in devices/web_server/server.py (dup-source drift), which still showed "Akien (legacy)" + warn styling. DS did the literal primary edit but skipped the investigative "verify nothing else needs it" step → premature close. CAUGHT because my pre-registered acceptance required a RENDER-asserting test; DS instead added a grouping-KEY-presence assert (`"akien" in data["grouped"]`) that could never catch a label revert. CC completed it (commit b0e74e72, on main): fixed server.py label+order+css, strengthened test_renders_status_groups with `assert "Akien (yours)" in html` + `assert "legacy" not in html.lower()` — that guard found the un-fixed source. Full tests/test_web_queue.py 15/15 green. LESSON for DS-as-primary-builder plan: DS executes the named edit well but doesn't cross-check sibling sources; tickets for DS must name ALL edit sites explicitly OR CC validates render-level, not just test-passes. Filed T-status-label-source-consolidation (M, 0.3): 3 dup _STATUS_LABEL dicts (queue_view.py + server.py + bin tools) drift — consolidate to one canonical source so a label change can't half-land again.
- DS BUILD-AND-CLOSE LOOP VERIFIED END-TO-END ✅ ($0 devstral). Dispatched the genuinely low-inertia T-web-queue-status-test-stale → dicksimnel.0 (tags TicketStatus/Tests/Queue — no HIGH-inertia tag, so it REACHED inference, unlike the Security bounce). Full loop proven: dispatch (msg 64539 seen) → WORKING event (64542) → tier-0 devstral inference (OLLAMA-ONLY, _TIER_CASCADE has OR/paid DISABLED → confirmed $0, no paid fallthrough) → Read+Edit tool loop → git commit 165890f0 → DONE: → cc_queue close. CC VALIDATION PASSED: diff is non-gamed and scope-correct — changed ONLY the test_renders_status_groups HTML asserts to canonical display labels (In progress/Ready/Hold, +added Akien), left the lines-74-76 data["grouped"] raw-status-KEY asserts UNTOUCHED (the scope-discrimination trap — DS distinguished render-assert from grouping-key-assert), well-commented, full tests/test_web_queue.py 15/15 green. METHOD: pre-registered acceptance criteria BEFORE the run (advisor's "test-fix loop is gameable" warning) so validation was comparison not post-hoc judgment. CORRECTS yesterday's "DS build surface ~nil" finding — DS CAN build real thin mechanical tickets correctly; the constraint is supply of low-inertia self-contained tickets, not DS capability.
- MINOR FOLLOW-UPS from the DS run (noted, not blocking): (1) DS commit co-author is hardcoded "Claude Sonnet 4.6" in devices/dicksimnel/device.py:114 + toolloop.py:124 — cosmetic MISLABEL (actual run was devstral/$0), should attribute to DS/devstral; (2) git author was akien@example.com placeholder. Neither is a cost event. Ticket-worthy cleanup, low prio.
- T-queue-view-akien-not-legacy FILED (S, pri 0.25): lab/claudecode/queue_view.py:_STATUS_LABEL still tags `akien` as "Akien (legacy)" — contradicts the keep-akien-as-ownership-bucket decision; same regression family as the uuopentickets/uumytickets fix (those already corrected to "👤 Akien (yours)"). Advisor flagged: file it as a real ticket, don't leave as a chat parenthetical.
- DS DISPATCH PATH TESTED ✅ (Akien: "get everything built by CC.1 or DS … we have things to test there"). Dispatched T-granny-dispatch-deadcode-spawn → dicksimnel.0 via mcp__datacenter__dispatch_ticket. Full path VERIFIED end-to-end: envelope received → dispatch_ack → DS pre-inference guardrail fired (`escalated … HIGH-inertia tags: ['Security']`) → bounced back to CC.0 at $0 (never reached the model). Cost-runaway impossibility re-confirmed first: DS _escalate_ticket is a PASSIVE handoff (append-note + setstatus=escalated, NO Popen/_launch_cc_instance); the Granny Popen footgun lives only in the MinionDevice path, bypassed by dispatching straight to dicksimnel.0; inference floor is devstral-only, no paid fallthrough. FINDING: CC.1 consumer is orphaned (can't build now) and ~all remaining backlog is CC.0 design work or HIGH-inertia (bounces from DS) — so the realistic DS build surface on this backlog is ~nil; the guardrails are doing their job. This was a path-test, not a backlog-drain.
- AKIEN BUCKET — LIVE REGRESSION FOUND & FIXED (commit 3a9da5e3, main). Consequence-check of step-2 (84da5557) against Akien's keep-akien correction: 84da5557 had put `akien` in _DEPRECATED_STATUSES, so cmd_setstatus REJECTED it — Akien could no longer tag a ticket "mine" (the exact bucket he said to keep). Fix: dropped `akien` from _DEPRECATED_STATUSES (now {approval, awaiting_approval}); it stays OUT of _ACTIONABLE_STATUSES → settable-but-non-actionable (Akien can mark his, never auto-dispatched). Verified: akien settable=yes, actionable=no, valid=yes; cert ticket still status=akien. 106 cc_queue/status tests pass.
- T-ticket-status-approval-akien-removal SCOPE-CORRECTED (append-note + worker=claude/CC.0, NOT a builder hand-off). Revised: remove ONLY approval/awaiting_approval; KEEP akien settable; the "migrate the 1 live akien ticket" step CANCELLED — T-uc-cert-domain-migration STAYS akien (earlier DEPENDENCY open-Q overruled by Akien). The code half of this is now already shipped (3a9da5e3); ticket tracks the doc/label cleanup remainder.
- T-web-queue-status-test-stale FILED (S, pri 0.2, pre-existing — confirmed independent of my change via git-stash run). test_renders_status_groups asserts raw 'in_progress' but status-model display work renders label 'In progress'; fix the TEST to the canonical vocab.
- OBSERVED (non-blocking): Granny is re-dispatching T-constraint-normalizer-store → CC.0 every ~11min (channel) because it's in_progress with no live CC.0 consumer (orphaned, T-cc-consumer-groundloop-wiring). Untracked devices/hubert/constraint_normalizer.py is that in-flight work. Noise, not harm.
- DEVLAB MEMORY STORE — forward hooks SHIPPED (branch devlab-memory-store, committed): foundation (99412000) + ticket migration 2015 tickets idempotent (18c6aed3) + **dual-write hooks**: cc_queue._save() now projects every persisted ticket into devlab/runtime/memory/tickets/ (9216a027, fail-open, reuses migrate_tickets.migrate_one → idempotent by construction, catches status transitions = AR-009); /sorted projects decisions via migrate_decisions.migrate_one (66d19204, fail-open). CHOKEPOINT REALITY (advisor-confirmed): only cc_queue (tickets) + /sorted (decisions) are clean live python hooks. SURFACED + DEFERRED: slate_manager.py & session_manager.py are DEAD (zero live callers); decision_manager.py is legacy TheIgors.archive (would reintroduce TheIgors dep — skipped); slates/sessions are skill-authored markdown fragmented across ~20 skills + a 3rd .slate.txt path → do NOT scatter emit calls (the N-sources-N-files trap the SPEC warns of). Akien's call on a slate/session chokepoint when back.
- T-ground-loop-proxy-passthrough-status CLOSED (e80f0feb, on main): plugin_proxy._forward was remapping every urlopen exception to send_error(502), masking real backend 404/401/500 as 502. Now catches HTTPError separately (it IS the response object) → relays real status+headers+body verbatim; 502 reserved for genuine transport failures (URLError/conn-refused). HTTPError arm precedes generic handler (subclass ordering). AR-009 INFO log w/ backend_status+path. 2 regression tests pass (404 passthrough; conn-refused→502). Note: pre-existing test_proxy_starts_on_first_request silently skips (http.server.test defaults to BaseHTTPRequestHandler → 501 on GET); used SimpleHTTPRequestHandler for a true 404.
- AKIEN BUCKET CLARIFIED (Akien): `akien` is NOT a legacy approval gate — it's Akien's at-a-glance "these are mine" view. T-uc-cert-domain-migration STAYS akien (do NOT reclassify to DEPENDENCY — I had that backwards). Fixed the mislabel I introduced: uuopentickets + uumytickets now show `akien` as "👤 Akien (yours)". cc_queue deprecation logic untouched (orthogonal: that's auto-dispatch/approval-gating, not ownership).
- WORKFLOW NORM (Akien): commit straight to main — NO feature branches until other people use the repo. Branched devlab work off main by harness reflex (wrong); fast-forwarded main → branch + deleted it. Saved feedback_commit_to_main_directly. local main ahead of origin = normal.
- STEP-5 LEDGER — "all the skills changes we ticketed today" (dated audit: tickets created 2026-06-16, bucketed). SHIPPED [skills]: /sorted dual-write decisions→memory store (66d19204); status-model step-1 fold design/open_questions/needs_review→triage across cc_queue/queue_view/uuquestions/uushowticket/web_server (4ad73a98); status-model step-2 approval/akien/awaiting_approval no longer settable+actionable (84da5557, T-ticket-status-approval-akien-removal CLOSED); uuopentickets+uumytickets canonical 7-status vocab (this session). DEFERRED-GATED (Akien required — NOT built): T-per-ticket-checkpoint (L, CC.n/spawn-CC risk), T-cc-consumer-groundloop-wiring (attended go-live), T-consequence-architecture-as-code-cognition-pipeline (gate 2026-06-30), **T-ticket-status-assigned-shim-nag (L, step 3 — net-new autonomous nag/back-off + Granny dispatch; `assigned` not yet in cc_queue STATUS_ORDER and bare-add would be a dangling status, so NOT half-wired solo while away)**. DEFERRED-DESIGN-OPEN/LARGE (surface): T-rewind-output-contracts (M, savestate split — design Q on boundary markers), T-build-log-digester (needs structured markers or degrades), T-why-sorter (L, high-inertia DB migration), T-per-ticket-usage-metrics (M), T-state-inventory-leak-audit. NON-SKILL INFRA available if wanted (outside this directive): T-granny-dispatch-deadcode-spawn (footgun cleanup, touches dispatcher), T-ground-loop-proxy-passthrough-status. NET: the skill-file changes ticketed today are DONE; the remainder is gated/design/large/non-skill.
- DEVLAB DUAL-WRITE IDEMPOTENCY VERIFIED (advisor-flagged re-projection concern, now closed): `_save(tasks)` re-projects the FULL ticket list on every mutation, but churn is zero — empirically confirmed twice. A full load→save touches ONLY the ticket whose body actually changed; all ~2000 unchanged tickets re-emit byte-identical (stable `created_at`→stable stamp→stable filename; identical body→identical bytes; `emitted_at` derived from stamp, not wall-clock). Verified: no-op-ish full save → exactly 1 file modified, 0 unchanged tickets touched. No guard needed on _project_to_memory. Probe ticket T-projftest-delete-me fully cleaned (devlab.tickets row + memory-store file + /tmp/probe.json all removed; was never committed).
- TICKET STATUS MODEL step 2 SHIPPED (84da5557, T-ticket-status-approval-akien-removal): approval/akien/awaiting_approval no longer settable (_DEPRECATED_STATUSES, cmd_setstatus rejects) or actionable (_ACTIONABLE_STATUSES={sprint}); cmd_propose→triage not approval; legacy_map folds approval→triage. The 1 live `akien` row (T-uc-cert-domain-migration, Akien's TLS/domain action GH#413) LEFT UNTOUCHED — where it lands (recommend DEPENDENCY) is Akien's call. queue_view/web_server legacy labels deferred (still needed to render that 1 row).
- uuopentickets + uumytickets updated to canonical 7-status vocabulary (vocab-only, DB stays source; legacy buckets flagged "legacy → reclassify" not hidden). Originals backed up to ~/.unseen_university/bin_backups/*.bak.20260616 (outside repo, no git net).
- D-filesystem-memory-store-2026-06-16 (FOUNDATION + decisions-pilot, UNCOMMITTED): move the palace out of Postgres into grep-able git-backed JSON files at `devlab/runtime/memory/`. WHY: "palace not working" — 4 stores, no sync, /sorted wrote files never DB → DB stale; match substrate to operator (CC = files+grep+git). Shipped: dir tree+.gitkeep; SPEC.md; `lab/claudecode/memory_emit.py` single chokepoint (envelope + `<emitter>.<yyyymmdd>.<hhmmssuuuuuu>.json` + idempotent atomic writes + `stamp_for_day_only` semantic-id→valid-time hash, tested); genesis decision dogfooded; `migrate_decisions.py` pilot projected all 82 lab/design_docs/decisions/*.md in (idempotent, semantic-id links extracted). Policy LOCKED: projection-not-relocation, idempotent-via-original-stamp, semantic-ids-in-links-never-filenames, reserved names (judge/chat.cc.0/chat.igor) verbatim. OPEN (gated on Akien red-pen): hook rewire, bulk migrate (Haiku), goals→decisions→tickets→commits matching, cutover. FLAG: same record in N sources → N files; dedup key = semantic id; cutover declares canonical source per category. Memory: project_filesystem_memory_store. **Akien away → left uncommitted on main for his commit call.**
- D-rewind-as-workflow-primitive-2026-06-16 (/sorted): rewind as a workflow primitive — the conversation is restartable because all state is durable; `/rewind → Restore conversation` keeps code+uncommitted edits (VERIFIED via claude-code-guide vs official checkpointing docs — own snapshot mechanism, separate from git; caveat: Bash rm/mv NOT checkpoint-tracked). WHY: lower-cost context via continuous cheap reduction, without autocompact's disruption tax. Lens: durability × readiness — everything crossing a context boundary must be durable AND predigested. 3 workstreams filed: T-build-log-digester (inference-free Ground-Loop minion, ticket-keyed digest = same key as per-ticket-usage-metrics, attempt→outcome timeline as anti-loop fuel; advisor precondition: needs structured boundary markers or degrades to flat timeline), T-rewind-output-contracts (contract table + savestate split; export-chat-before-rewind MANDATORY, conversation-only never code-restore), T-state-inventory-leak-audit. T-per-ticket-checkpoint is now the builder-loop instance of this principle.
- D-ticket-status-model-2026-06-16: canonical 7-status model "set in concrete" (Akien's ask). Decision doc + decisions_log. SHIPPED step 1 (T-ticket-status-fold-design-triage CLOSED): design/open_questions/needs_review → triage in cc_queue.py/queue_view.py/uuquestions.py(rewritten)/uushowticket.py/web_server; dropped design from _ACTIONABLE_STATUSES (conscious claim-path change); migrated 1 DB row (T-scenario-generator, snapshot+AR-009 log); opentickets now shows canonical labels (READY=sprint via display-label, no string rename). FILED step 2 T-ticket-status-approval-akien-removal (sprint, low-blast — rename collapsed; open Q: where does T-uc-cert-domain-migration land), step 3 T-ticket-status-assigned-shim-nag (sprint, net-new ASSIGNED+10min nag). Also fixed export_chat.py (wrong glob depth → "No transcripts found").
- D-architecture-as-code-cognition-pipeline-2026-06-16: whys-as-lever + cognition pipeline (Opus design conv) — filed T-why-sorter (L, sprint), T-per-ticket-checkpoint (M, sprint), T-consequence-architecture-as-code-cognition-pipeline (S, sprint, gate 2026-06-30). CC.0/CC.1 split + intent-graph IR deferred to narrative (need Akien). Cadence note: monthly Opus session > existing audits.
- T-web-server-auto-recovery CLOSED — root-caused (descriptor never deposited + bare python3 ImportError + wrong backend_port 8080→8082) and fixed; verified cold-start/crash-recovery/`/api/health` all 200 through :8078. Commits 7a02e973, c2c150b4, + user-systemd unit.
- feat: availability button — /available skill + lab/claudecode/cc_available.py (self-serve Granny availability reset)
- fix: Ground Loop proxy captures backend stdout/stderr to logfile (was DEVNULL — hid the ImportError)
- fix: ground_loop user unit self-deploys descriptors via ExecStartPre (commit 220fd110) — verified: wiped live web_server.yaml, restart re-deposited it, :8078 /api/health 200. Web server is now self-deploying from a bare checkout.
- T-cc-consumer-groundloop-wiring FILED — root-caused the "dual-dispatch is DS-only" gap: CC consumer built+safe but nothing starts it.
- fix: ground_loop ExecStartPre is now fail-open (commit 1342bc87) — `-` prefix + `|| true`, so a descriptor-copy failure can never block the supervisor. Validated: restart → descriptor redeposited → :8078 / and /api/health both 200.
- **AUDIT SYSTEM OVERHAUL** (part of architecture-as-code monthly Opus design): (1) Created `audit-regression` skill — fast (Haiku), specific binary PASS/FAIL checks against core invariants (no SQLite, all devices inherit Base*, no silent failures, no blocking IDLE loops, no deprecated paths, device boundaries maintained, etc.). Runs FIRST before costly audits. (2) Consolidated EXPERTS.md: replaced 11 broad DEFAULT_EXPERTS with 8 UU-specific experts (regression-focused lenses: Systems Architect, Security Engineer, Process/Meta, Product, Reliability, HCI, Self-Improving Systems, Evaluator Quality, Observability-First). Embedded in audit-expert/run so it loads by default. (3) Created `audit-workspace` skill — finds orphaned directories (age+size+references+naming heuristics). Initial scan: 1 Tier-2 candidate (cert-walks), others have existing refs. (4) Single source of truth: UU repo `/skills/audit-*` + synced to `~/.claude/skills/audit-*`; deleted obsolete audits from archives. (5) RAN regression audit: found 10 violations (non-inheriting devices, concurrent writes w/o DB, bare excepts, blocking IDLE loops, print() in devices, deprecated psycopg2.connect, device boundaries crossed, IMAPServer outside bus/). Commit 4ae15220. FILED 8 regression tickets (T-regression-*) + 3 cleanup investigation tickets (T-cleanup-*). Status: 83% remaining before autocompact. TO PICK UP: (a) fix the 10 regression violations + run verify-tickets-closed loop, (b) investigate cleanup Tier 2/3 candidates, (c) run audit-regression weekly to prevent regressions, (d) consequence-check this design (gate 2026-06-30 on per-ticket-checkpoint + why-sorter + architecture-as-code batch).
