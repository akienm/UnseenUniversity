# Slate 2026-06-26

## Notes

## In-flight
NONE

## Planned

## Ad hoc

## Done today

- T-skills-workflow-goal-purge — Remove Goals(G-xxx) block from skills/workflow.md (Removed Goals block, /audit-goal, KR refs, goal-progress from workflow.md — grep returns 0 goal-language matches)
- T-skills-goal-skills-retire — Delete /goal and /audit-goal skills + remove from manifest (Deleted skills/goal/ and skills/audit-goal/ + symlinks. Manifest had no entries. grep for goal/audit-goal in skills/ ret)
- T-skills-audit-design-goal-reframe — Reframe audit-design Check 1 from 'goal' to 'intention' (Check 1 reframed: 'Intention stated as observable target'. grep -ic for 'which goal|G-xxx|goal stated' = 0.)
- T-skills-kr-references-purge — Purge goal-KR machinery from outcome, eval-run, weekly-retro skills (Dropped KR machinery from outcome (Step 5 removed), eval-run (KR mapping removed), weekly-retro (Q2 goal KR removed). gr)
- T-skills-audit-hypothesis-goal-reframe — Remove G-xxx goal-link check from audit-hypothesis (Check 3) (Check 3 reframed to 'Intention is present and well-formed'; all G-xxx/palace.goals/goal-link vocabulary removed. grep = )
- T-skills-goals-to-intentions — Skills track intentions, not goals (drop G-xxx goal-tracking) (Removed links.goals from the live emission schema (LINK_KINDS in memory_emit.py, _LINK_KEYS in ticket_store.py) + update)
- T-mcp-finish-igor-librarian-migration — Finish igor->librarian MCP migration: delete the duplicate `igor` alias (Repointed all 10 live mcp__igor__ refs across 7 files to mcp__librarian__ (1:1 rename; igor/librarian were byte-identica)
- T-context-load-slate-is-entry-point — [awaiting_validation] context-load starts the session by reading the slate, not /query-ticket (Fixed: SKILL.md hard-stop banner makes the slate the session entry point (not /query-ticket); Step 1 of run now surfaces)
- T-remove-adc-launcher — [awaiting_validation] Remove the dead ADC-named launcher wrappers (bin/adc, bin/adc.ps1) (Removed bin/adc + bin/adc.ps1 (dead ADC bootstrap wrappers). Proof: git ls-files grep for adc launcher files is empty; w)
- T-launcher-help-and-1m-flags — [awaiting_validation] superclaude/rescueclaude gain --help and --1m flags; root scripts become bin/ pass-throughs (Implemented --1m (ANTHROPIC_MODEL=opus[1m], off by default, flag consumed) + --help (wrapper flags then claude --help) o)
- T-launcher-unfailable-recovery — [awaiting_validation] Launchers must always reach exec claude — unfailable recovery contract (venv best-effort, no fatal interject) (Unfailable launch contract shipped. rescueclaude: best-effort venv-shell create (no pip install), UU_RESCUE_DIR override)
## FIRST UP — /sorted: git-stash attribution + hygiene (design DONE 2026-06-26 ~06:30 w/ Akien)

PROBLEM: stale/anonymous git stashes are hidden cross-session divergent state. Bit us
2026-06-25 — an 841-commit-old stash popped during a baseline check → phantom "SyntaxError"
(conflict markers) in shim.py. Multi-agent makes it worse: refs/stash is SHARED across all
worktrees of a repo, so one agent's stash is globally visible/poppable.

PRINCIPLE: attribute, don't forbid. Design for ease — make the safe path the reflexive path.

DESIGN (all agreed; how-to left to builder):
1. PATH-OVERRIDE WRAPPER. superclaude startup + DS startup PREPEND the shim dir to PATH so
   plain `git` IS the wrapper (agents call `git` by reflex — zero new discipline required).
   Wrapper intercepts ONLY stash-create (push/save); everything else (list/show/pop/drop,
   all other git) falls straight through to REAL git (found by skipping its own PATH entry
   to avoid recursing into itself).
2. AUTO-TAG to canonical message format:  agent:T-Ticket:WIP[:any other args]
   - agent  = from launch env (already set in the agent's call)
   - ticket = from sprint-claim context (this agent's in-progress ticket)
   - no current ticket → RESCUE: namespace (never anonymous)
   - HANDLE BARE `git stash` (no -m): inject -m, else the lazy reflexive path escapes attribution
   - idempotent: don't double-prefix if already agent:... or RESCUE:
   WHY this format: ticket field → relevance/scoping (tie stash to ticket status/files);
   agent field → per-builder stash-FREQUENCY telemetry (is one builder leaving WIP more
   than another = builder-health signal). Colon-delimited → parseable by field (cut -d:).
3. RESCUE: = reserved override namespace for operator/non-ticket/recovery work.
4. TICKET-CLOSE = scoped check (now multi-agent-safe BECAUSE attributed): close of T-x greps
   its own `agent:T-x:` tag; dangling → ticket not really done. Never touches a sibling
   agent's stash. (This is what your ticket-name idea unlocked — a global check was unsafe.)
5. ORPHAN SWEEP (ticket-close + day-close + periodic): any stash whose ticket is closed or
   unknown → drop a system_alarm. SURFACE, NEVER BLOCK (a hard gate could nuke a sibling's tree).
6. day-close step 4.6 (already shipped dc9d1840) keeps the analyze→preserve→clear human pass.

TICKETS TO DRAFT under the new decision:
  (a) git-stash wrapper shim + PATH injection in superclaude + DS startup
  (b) ticket-close scoped stash check (grep own agent:ticket: tag)
  (c) orphan-stash sweep → system_alarm (wire into ticket-close + day-close + periodic)
  (d) consequence-check ticket
REFS: memory git-stash-is-hidden-divergent-state; day-close SKILL step 4.6.

- D-git-stash-attribution-hygiene-2026-06-26: git-stash attribution+hygiene — T-git-stash-attribution-wrapper, T-stash-ticket-close-check, T-stash-orphan-sweep-alarm, T-consequence-git-stash-attribution
- D-mcp-surface-homogenization-2026-06-26: collapse per-device MCP namespaces to dispatch-by-addressee (B finish igor->librarian remove-behind, C rack dispatch) — T-mcp-finish-igor-librarian-migration, T-mcp-dispatch-by-addressee, T-consequence-mcp-surface-homogenization
- D-goals-purge-remaining-skills-2026-06-26: purge goal(G-xxx) language from 8 remaining skill files (workflow/audit-design/goal/audit-goal/outcome/eval-run/weekly-retro) — T-skills-workflow-goal-purge, T-skills-goal-skills-retire, T-skills-audit-design-goal-reframe, T-skills-kr-references-purge, T-consequence-goals-purge-remaining-skills

## Session close 2026-06-26
Done: Done: goals→intentions floorboard solid (5 skills + schema.goals removed); 5 tickets closed; proof_emitter gap surfaced. MCP-surface work filed and ready.
Next: Next: T-mcp-finish-igor-librarian-migration + T-mcp-dispatch-by-addressee (MCP homogenization, 2 decisions filed).

## In-flight: Goals→intentions batch complete (shipped-unproven with proof_emitter lever filed). MCP-surface homogenization queued. No work in progress.

## In-flight: T-mcp-finish-igor-librarian-migration closed (shipped-unproven); live mcp__igor__ refs repointed to librarian + ~/.mcp.json igor block deleted. T-mcp-dispatch-by-addressee (L) now ungated — the dispatch-server redesign is next.

## In-flight: Skills floorboard solid; MCP migration M closed (igor→librarian repointed + ~/.mcp.json igor block deleted). Captured class-vs-instance addressing model + 'wait-for-the-better-lever' lesson + current debt (web=igor, granny inconsistent) onto the dispatch ticket. NEXT: T-mcp-dispatch-by-addressee (L, ungated, annotated) — design-heavy rack-dispatch redesign; warrants a /design pass.

## In-flight: T-mcp-dispatch-by-addressee is now SPRINT-READY: design resolved — dispatch = send_to(<instance>,{kind:verb,...args}) out + readfeed back + correlation id; reuse announce (announce_mcp.py), build only the await-with-timeout wrapper if needed; rack owns routing (comms:// is instance-native); manifest is the one new surface; to=instance, N=2, structured errors, add→migrate→deregister cutover. NEXT after compact+skills-restart: sprint that L. Also captured: class-vs-instance addressing model + wait-for-the-better-lever lesson + current debt (web=igor, granny inconsistent) on the ticket.

## Session close 2026-06-26
Done: Done: rescueclaude + resumeclaude moved into repo bin/, gained --new flag, ~/bin shadows deleted, fake-completion pattern noted
Next: Next: /sprint T-mcp-dispatch-by-addressee (L, sprint-ready — full design resolved)

## In-flight: Context-load skill floorboard fixed (awaiting_validation); ready for /sprint T-mcp-dispatch-by-addressee (L, sprint-ready, design resolved).

## In-flight: Launcher --1m/--help + root pass-throughs shipped (T-launcher-help-and-1m-flags, awaiting_validation). --1m relaunch is the forgetfulness A/B lever — Akien to test.

## In-flight: Unfailable launch contract shipped + proven (7/7 chaos). Surfaced stale core.hooksPath debt (T-stale-core-hookspath-rename-debt). Next: address /clear+/context-load forgetfulness data; Windows recovery scope question open.
