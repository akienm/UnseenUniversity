# Slate 2026-06-24

## Notes


## Planned

## Ad hoc

## Done today

- T-context-load-fs-store — context-load resolves UU_ROOT from any cwd + reads fs store (no palace DB) (context-load resolves UU_ROOT via uu_root() (works from any cwd), sorts decisions by emitted_at, Step 3 shows real statu)
- T-uu-env-coverage-audit — Pre-rotation: audit that every DB-connecting process has the env before fallbacks are gone (Audited every DB-connecting process class for UU_HOME_DB_URL env coverage. Checklist emitted (notes/env-coverage-audit-2)
- T-uu-eliminate-igor-home-env — Eliminate IGOR_HOME env var — UU_ROOT is the only canonical env var; data dir via resolver (Eliminated the IGOR_HOME env var. uu_home() (env-free, call-time, ~/.unseen_university) replaces it across 30 source rea)
- T-uu-igor-home-env-skills-scripts — Eliminate IGOR_HOME env from skill run-scripts (missed by T-uu-eliminate-igor-home-env) (Closed the IGOR_HOME scope hole from T-uu-eliminate-igor-home-env: 4 skill run-scripts (context-load/note/savestate/spri)
- T-uu-env-file-for-noninteractive — Processor emits a KEY=VAL env file so systemd/cron/launchers get UU_HOME_DB_URL (Processor emits ~/.unseen_university/uu.env (chmod 600, local, never committed) carrying UU_HOME_DB_URL + UU_ROOT + IGOR)
- T-uu-sweep-db-connection-string — Sweep: replace hardcoded DB connection-string fallbacks with the resolver (lazy) (Swept the hardcoded DB-password fallback out of all live code: 89 modules + the uu CLI now resolve via identity.home_db_)
- T-uu-dburl-name-unify — Unify IGOR_HOME_DB_URL -> UU_HOME_DB_URL (25 refs read a name nothing exports) (Unified legacy IGOR_HOME_DB_URL onto UU_HOME_DB_URL across all live code (15 readers + the uu CLI), most via home_db_url)
- T-uu-rotate-db-password — Rotate the DB password (the neutralizer) — quiesce/rotate/restart (Rotated the igor role password: ALTER ROLE igor PASSWORD <new>; scrubbed ~/.unseen_university/uu_bash_profile.sh (remove)
- T-uu-config-profile-db-creds — config/profiles/*.yaml embed the DB password in subsystem URLs — loader must compose from env (igor.yaml state_refs carry bare #fragment; compose_state_uri() resolves against UU_HOME_DB_URL at connect time. Last liv)
- T-uu-rename-role-and-db — Rename DB role igor->akien (employer) + DB Igor-wild-0001->${IGOR_NAME} (deferrable) (Live rename igor/Igor-wild-0001 -> unseen_university (role+DB), password reset, pg_hba LAN line fixed; verified: new ide)
- T-uu-sweep-doc-example-db-strings — Update illustrative DB-connection-string doc-examples to unseen_university identity (Updated 18 non-test source files' illustrative DB-string examples (igor:->unseen_university:, /Igor-wild-0001->/unseen_u)
## Notes
- Skills-harmony design (D-skills-two-products-2026-06-23) — Akien reading the 2 ref docs; may revise. Ticketed his dispositions 2026-06-24 (pre-/sorted capture):
  - EXPANDED T-skills-prune-deprecated-merge: + retire /recover (supersedes T-recover-skill-message-wording), /question, /concept, /autocompact+uucompactclaude (new Anthropic compact obsoletes it), /migrate-decisions (backfill spent).
  - EXPANDED T-skills-goals-to-intentions: + retire /goal skill, /audit-goal->/audit-intentions.
  - NEW: T-readfeed-generalizes-readigor, T-device-skills-via-uu-device (L, needs advisor), T-workflow-index-and-uu-help, T-audit-proportionality-vs-proof-on-close, T-skill-dispositions-investigate.
  - OPEN TENSION for /sorted: readfeed as top-level /readfeed <dev> vs folded into /device <dev> feed. Pick one surface.
  - ~/.claude was missed by the repo path-sweep (separate non-git dir) -> single-source flip is the permanent fix.

## Planned
- /sorted reconcile D-skills-two-products once Akien finishes the 2nd doc (lock revisions + resolve readfeed/device tension + advisor on the L ticket)
- Skills cluster sprint order (after lock): prune-deprecated, workflow-index+uu-help, dispositions-investigate, then readfeed/device-skills/goals-to-intentions/audit-proportionality

## Notes — Akien amendments to skills-harmony (2026-06-24, post-2nd-doc)
- 'new agent' = the new-agent skill (exists; resolves the unknown).
- Igor skills go WITH igor (into igor device space), NOT local in ~/.claude. The doc'''s 'keep igor skills local' idea is WRONG — it would rebuild the split-home canonical-memory just killed.
- /device <dev> <verb> [<noun>] for ALL devices (confirmed). Each device'''s shim has a bin/ folder = that device'''s executor scripts only. uu device <dev> <verb> -> devices/<dev>/bin/<verb>.
- skills-sync: NO two-way sync — DIRECTORY LINKS only (symlink). Two-way sync was the drift engine.
- Akien has ZERO local skills -> clean symlink replacement. Others might have local skills; on conflict drop a system_alarm (operations/system_alarms/) and ask. That conflict-resolution is the ONLY non-scriptable part.
- RESOLVED tension: readfeed folds into /device <dev> feed (device model wins).

## Notes — fork #1 resolved (2026-06-24)
- Symlink granularity: PER-SKILL links (not folder). Flip iterates repo skills/, links ~/.claude/skills/<skill> -> repo each. Local-only skills coexist untouched; same-name collision with a repo skill -> system_alarm, ask user. (only non-scriptable part)
- /device igor feed [channel] = winning format; readfeed folds into the device model.
- Fork #2 (device bin/: all verbs vs zero-inference only) — Akien replying separately; HOLD device-skills ticket rewrite until then.

## Done today
- /sorted LOCKED D-skills-two-products-2026-06-23 (amended for 2026-06-24 design). advisor-reviewed the L. Reconciled (not re-filed): discarded dup T-readfeed-generalizes-readigor; repointed pre-existing T-uu-readfeed -> /device igor feed (gated on device mechanism); rewrote T-device-skills-via-uu-device as DISPATCH MECHANISM (bin/+skills/ failover, unique-name-per-device, /device thin-shim over uu device, igor skills move INTO devices/igor/{bin,skills}/), gated on T-uu-cli-dispatcher; amended T-skills-single-source-flip (per-skill ~/.claude symlinks + collision->system_alarm, fork #1 overrides project-scope-primary); added uu help to dispatcher; amended T-consequence (device collisions + symlink clashes); populated decision spawned_tickets (13) + fixed measurement signal (uu device igor feed). Filed T-cc-queue-list-hides-tickets (visibility bug that made the dup).

## In-flight (pre-compact)
NONE — T-uu-config-profile-db-creds closed (proven). Env-vars phase: rename-role-and-db or profile-loader remaining. Tree CLEAN.
## Notes — stale-skill cleanup (2026-06-24, Akien spotted via context-load)
- Skills predate the canonical-memory consolidation. Two work items, both under D-skills-two-products:
  - T-day-close-audit-skill-stale-paths (amended): MECHANICAL path-token sweep, ~14 survivors, EXCLUDE retirees (recover/question/concept/autocompact/migrate-decisions/goal/audit-goal/cognition-debug/debug-pe-chain), target repo skills/ (flip inherits), sequence after prune. Delegate-to-cheaper-agent candidate.
  - T-skills-palace-db-to-fs-store (NEW, L): palace-DB->filesystem-store MECHANISM rewrite — context-load Steps 2a-3 + day-close palace.days + audit-audits/hypothesis/smell + eval-run/outcome/weekly-retro. context-load is priority (post-compact entry point; bit us today). Needs CC judgment + advisor.
- Decision spawned_tickets now 15.

## Done today
- T-context-load-fs-store CLOSED PROVEN (87f0c9e0 + proof). context-load run executor was already fs-store-based; real bugs: UU_ROOT defaulted to cwd (silently dropped Steps 2a/2b/3 off-root, e.g. post-compact) -> now uu_root(); decisions sorted by emitted_at (was alphabetical); Step 3 shows real status; SKILL.md rewritten to the fs-store executor (dropped psql/palace docs + nonexistent subcommands). Proof: briefing from non-root cwd surfaces recent decisions w/ status.
- Split from L parent T-skills-palace-db-to-fs-store (back to sprint; 7 survivor skills left: day-close, audit-audits/hypothesis/smell, eval-run, outcome, weekly-retro).
- Filed T-context-load-violation-summary-missing (pre-existing: run lacks violation_summary() its test expects; 4 tests red at HEAD before my change — confirmed via git show+stash; low pri).

## Done today
- T-uu-env-coverage-audit CLOSED shipped-unproven. Env-vars work is now TOP priority (Akien: skills depend on env being right). Checklist: notes/env-coverage-audit-2026-06-24. FINDING: composed UU_HOME_DB_URL reaches only interactive shells; GAPS = ground_loop.service (no DB URL, can't source bash profile), 8 crons (no .bashrc; several on retired TheIgors/lab/tmp paths), 25 IGOR_HOME_DB_URL readers (processor never sets that name). Filed T-uu-env-file-for-noninteractive (env-delivery mechanism = sweep blocker) + T-uu-dburl-name-unify. Corrected inverted gate: sweep+rotate now gate on the env-file mechanism. Three sweeps remain (db-connection-string L, instance-name L, hostname M) = delegate-to-cheaper-agent.

## In-flight (restart-ready)
NONE — T-uu-config-profile-db-creds closed (proven). Env-vars phase: rename-role-and-db or profile-loader remaining. Tree CLEAN.
## Done today (2026-06-24 session 2)
- T-uu-eliminate-igor-home-env CLOSED PROVEN (5e828ee8 + proof). uu_home() env-free/call-time resolver replaces the IGOR_HOME env var across 30 source readers (unseen_university/ + devices/ + devlab/claudecode/ — initial grep missed devlab/claudecode, caught via skill_telemetry/cost_to_close still passing); igor shim/device -> _igor_paths().instance. 18 test isolation seams moved setenv(IGOR_HOME) -> monkeypatch(uu_home), ALL rack/infra (none deleted, none were igor-self-programming). CLAUDE.md two->one canonical env var. processor + ground_loop units (repo + installed ~/.config copy) drop IGOR_HOME. Excludes IGOR_HOME_SEARCH_PATH + IGOR_HOMEOSTATIC_*. NOTE: installed ground_loop unit edited but daemon-reload not needed (uu_home env-free = the line was inert). devices/igor/<instance> runtime restructure = SEPARATE coming ticket.
- Dogfooded: post-restart venv active (launcher fix works); context-load fix surfaced decisions from the executor. Confirmed ~/.claude/skills/context-load still STALE (single-source flip needed — Skill tool loads the old psql copy).
- ENV-VARS PHASE remaining: T-uu-env-file-for-noninteractive (sweep blocker), T-uu-dburl-name-unify, 3 big delegate-sweeps, rotate, rename.

- T-uu-igor-home-env-skills-scripts CLOSED PROVEN (5cd5319d + proof, pushed 48776d0b). Fast-follow on the IGOR_HOME elimination: caught at compact that 4 live skill run-scripts (context-load/note/savestate/sprint-ticket) still read IGOR_HOME — the footprint grep skipped skills/ and the proof guard'''s *.py was blind to extensionless run scripts. Scripts -> uu_home(); guard widened to ALL tracked files. SKILL.md doc-snippets (notify, sorted) DEFERRED to skills content-merge phase (boundary named). IGOR_HOME env now eliminated from ALL live code, verified.

- T-uu-env-file-for-noninteractive CLOSED PROVEN (proof @ c370c65d). Processor emits ~/.unseen_university/uu.env (chmod 600, local) carrying UU_HOME_DB_URL + 5 derived vars; proven sourceable AND systemd-parseable (systemd-run). ground_loop unit reads it via EnvironmentFile=-%h/.unseen_university/uu.env (all 3 units + daemon-reload, NO restart — live on next natural GL restart). ARCHITECTURE LEARNED (Akien): GL's env is the ROOT every shim-launched device inherits (shim = heartbeat target + front door + lazy launcher; shim-launch -> GL env, interactive launcher -> caller env). So one line on GL's unit delivers to the whole non-interactive fleet (web_server/scraps/Nanny/igor-when-pinged) — Nanny needs no separate wiring. Captured in ground_loop + nanny architecture intention-points. AUTO-UNGATED T-uu-sweep-db-connection-string (now READY).
- Earlier: T-uu-igor-home-env-skills-scripts CLOSED PROVEN (caught the 4 skill run-scripts the IGOR_HOME sweep missed; guard widened past *.py).

- T-uu-sweep-db-connection-string + T-uu-dburl-name-unify BOTH CLOSED PROVEN (one sweep, 96 files @ 28e7713c, 2 proofs). Replaced the choose_a_password connection-string fallback with identity.home_db_url() across 89 modules + the uu CLI; unified IGOR_HOME_DB_URL -> UU_HOME_DB_URL. home_db_url() raises if unset (no baked cred). ALL module-scope bindings -> call-time/lazy (Stage 1 canonical 60; Stage 2 inline 39 consts; Pass 3a redundant-outer-get; Pass 3b ast-lazify default args; passes 4-8 or/const/doc/detector/template). GATES: 0/89 import-raisers w/ env unset; both code greps ZERO; 8430 tests collect (only pre-existing run_test.py error, unrelated). GL RESTARTED FIRST (was on fallback, pid 1660->3523499) so live rack carries env. Caught uu (extensionless) blind spot again (the saved lesson). OUT named: config/profiles creds -> filed T-uu-config-profile-db-creds; memory-store history; skill-doc psql. UNBLOCKS rotation (stripped ~75 password copies from source).
- NOTE pre-existing: run_test.py imports test_inference_basic/_with_list which do not exist (broken at HEAD, not mine). Old git stash@{0} present (not mine, left alone).

- T-uu-rotate-db-password CLOSED shipped-unproven (operational — no committed red/green for a live credential rotation; verified live instead). Rotated igor role password (ALTER ROLE); scrubbed uu_bash_profile.sh (old removed, NEW renamed to UU_DB_PASSWORD); regenerated uu.env; restarted GL. VERIFIED: old password REJECTED, new connects, rack healthy (web 200, GL+web_server+scraps reconnected), no vault/local file re-injects old. The ~75 source copies + git-history leak of choose_a_password are now WORTHLESS — the real security fix. Role name unchanged (md5 includes username — rename is next: T-uu-rename-role-and-db).
- ⚠️ choose_a_password is now DEAD. Anything still using it fails: (1) config/profiles/igor.yaml twm/ne/milieu URLs -> Igor cant connect to those subsystems when started (ticketed T-uu-config-profile-db-creds; Igor currently DOWN so no live impact yet); (2) skill psql commands in ~/.claude/skills (day-close-audit, stale context-load, audit-day) -> fail until skills-rework (Friday). THIS session env still has OLD UU_HOME_DB_URL (set at launch) — new sessions get the new password from the profile.

## Session state (pre-compact 16:59)
In-flight: NONE (T-uu-rotate-db-password closed + verified; tree clean, pushed 4fa8bf9c)
Env-vars phase progress today: eliminate-igor-home + skills-scripts-miss, env-file-for-noninteractive, dburl-name-unify + sweep-db-connection-string (96-file sweep), rotate-db-password — ALL closed. Architecture captured: ground_loop + nanny intention-points (waker/recoverer, shim-launch env-inheritance, hot-swap; Nanny = platform-independent scheduler).
NEXT (env-vars, near-done): T-uu-rename-role-and-db (rename igor role + Igor-wild-0001 DB; set password AFTER rename — md5 hashes username) OR T-uu-config-profile-db-creds (so Igor can start cleanly — config/profiles/igor.yaml still on dead choose_a_password). Akien to pick.
WATCH: choose_a_password now DEAD — config/profiles/igor.yaml + ~/.claude skill psql cmds will fail until fixed (skills-rework Friday). THIS session env has OLD UU_HOME_DB_URL (launch-time); post-compact is same process so still old — new shells get new pw from profile.
- T-uu-config-profile-db-creds CLOSED (proven, 8814233b): igor.yaml state_refs now bare #fragment; identity.compose_state_uri() resolves against UU_HOME_DB_URL at connect time — last live hardcoded DB credential out of source, no longer rides announce manifests.

## SESSION HANDOFF (pre-kill savestate — 2026-06-24 ~18:05)

ALL running claude sessions have STALE igor creds (started before the DB rename). Both are
named akiendelllinux_cc_RESCUE: PID 3163181 (13:31) and PID 3798552 (17:29 — the session
writing this). Plan: kill BOTH, relaunch ONE canonical session (cc_0, NOT cc_RESCUE) that
sources ~/.unseen_university/uu_bash_profile.sh -> new identity unseen_university@unseen_university.

DONE TODAY (durable in git, pushed to origin/main @ 71bad6a2):
- T-uu-config-profile-db-creds CLOSED proven (f8a90948): igor.yaml state_refs -> bare #fragment;
  identity.compose_state_uri() resolves against UU_HOME_DB_URL at connect time.
- T-uu-rename-role-and-db CLOSED proven (71bad6a2): LIVE rename igor/Igor-wild-0001 ->
  unseen_university (role+DB) via sudo-relay; password reset (same rotated string
  AThpareHandWhereNeeded1!); pg_hba LAN line 133 -> unseen_university/scram + reload.
  Config: bootstrap UU_DB_USER=unseen_university + new UU_HOME_DB_NAME=unseen_university;
  processor composes DB name from UU_HOME_DB_NAME (fallback IGOR_NAME). IGOR_NAME stays
  Igor-wild-0001 (tenant identity / on-disk instance folder — KEPT). uu.env regenerated.
  ground_loop+scraps+web_server(8082) on new identity; proxy 8078 -> 200 e2e. All schemas intact.

OWED on the rename (NOT yet done — fresh session pick up):
1. Doc-example string sweep ("update them too", Akien): ~17 files have docstring/comment examples
   `postgresql://igor:<password>@.../Igor-wild-0001` -> update to unseen_university:.../unseen_university.
   CRITICAL nuance: KEEP every `Igor-wild-0001` that is the instance-folder/tenant identity
   (IGOR_INSTANCE_ID, os.getenv("IGOR_INSTANCE_ID","Igor-wild-0001"), ~/.unseen_university/Igor-wild-0001).
   Only change DB-CONNECTION-STRING contexts. Exclude tests/, devlab/runtime/memory/, *.template/*.example,
   audit_check_hardcoded_instance.py (the detector). Delegate to a cheaper agent with these exclusion rules.
   (T-uu-sweep-instance-name + T-uu-sweep-hostname still OPEN — the instance-folder routing-to-resolver work.)
2. Decision-record update: D-uu-config-identity-layer-2026-06-22 said role -> akien; it LANDED as
   unseen_university (more portable; akien refs in code are vault credential-OWNERSHIP, orthogonal to PG role),
   and the DB rename happened NOW (decision had marked it deferrable). Amend the narrative.

ALSO: this session's + the other RESCUE session's MCP servers (librarian/queue/auditor PIDs 3163xxx, 3798xxx)
held stale creds — they die with the kill. Fresh session gets clean MCP env.

## In-flight
NONE (rename closed). Fresh-session TODO = the 2 OWED items above.

## Session 3 (post-compact 2026-06-24 ~20:30) — owed-items cleared
DONE (pushed origin/main @ a8530877):
- Doc-example sweep CLOSED shipped-unproven (T-uu-sweep-doc-example-db-strings, df3620c2):
  18 non-test source files' illustrative DB-string examples igor:->unseen_university: /
  /Igor-wild-0001->/unseen_university (conn-string context ONLY). Placeholders + every
  instance-identity Igor-wild-0001 left intact. Positive grep zero + negative check clean.
- Decision amend (append-only) D-uu-config-identity-layer-2026-06-22: role+DB landed
  substrate-owned unseen_university (NOT akien/${IGOR_NAME}); rename done now not deferred.
- FILED T-uu-sweep-skill-psql-creds (advisor catch — the orphaned 5th drift axis): executable
  skill psql still on dead igor:choose_a_password; owned by neither the closed conn-string
  sweep nor content-merge. Gated behind T-skills-content-merge-survivors -> Friday skills-rework,
  env-based fix (NEVER write the live pw into source).
NEXT (env-vars phase tail, then Friday): T-uu-sweep-instance-name + T-uu-sweep-hostname
(instance-folder routing-to-resolver, still OPEN) -> then skills cluster D-skills-two-products
(Friday presentation) -> then launchers.
