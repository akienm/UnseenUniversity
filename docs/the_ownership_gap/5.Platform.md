# Chapter 5: The Platform — Unseen University

---

## Why a Platform

The skills and workflow described in the previous chapters require infrastructure to run on. Not elaborate infrastructure — but specific infrastructure. A place to store state. A way for components to communicate. A mechanism for devices to be added to and removed from the system without rewriting everything around them.

The Unseen University (UU) is that infrastructure. It is not an AI system. It is the platform for building agents on, and substrate on which AI systems run. The distinction matters: an AI system makes decisions, processes information, and produces outputs. A platform provides the services those systems depend on — storage, messaging, logging, discovery, launch.

Building the AI on top of a platform rather than directly on infrastructure means the AI system can change without the platform changing, and the platform can improve without requiring changes to the AI. The two concerns are separated, and separation is the basis of maintainability.

---

## The Rack Model

UU organizes its components as rack-mounted devices. A device is any process that performs a defined function within the system: the AI reasoning engine (Igor), the knowledge management system (Librarian), the deterministic script runner (Scraps), the web server, the queue manager.

Each device has:
- A name and a rack slot
- A config file at `~/.UnseenUniversity/<DeviceName>/config.cfg`
- A log directory at `~/.UnseenUniversity/logs/<DeviceName>/`
- A health check that the rack can poll
- A defined interface for the services it provides

The rack model enforces a clean separation between "what a device does" and "how it fits into the system." A device that fails can be restarted independently. A new device can be added without modifying existing devices. The rack is the integration point, not the devices themselves.

---

## Discovery Over Configuration

One of the explicit design goals of UU is to eliminate required configuration. The system should discover what it needs rather than require the user to configure it.

The primary example is instance discovery. When Igor starts, it does not require an environment variable telling it which instance to use. It looks at `~/.UnseenUniversity/Igor-*/` and:

- If it finds one instance: uses it
- If it finds more than one: asks which to start
- If it finds none: runs the fresh-install wizard

The `--id` flag is available as an explicit override, but it is not required. The common case — one instance on one machine — requires no configuration at all.

The same principle applies throughout the platform. Devices discover each other via the rack manifest rather than via hardcoded addresses. Configuration that can be derived from the filesystem is derived rather than specified. Required environment variables are eliminated where possible.

This is not just convenience. It is the difference between a system that a new user can start without a setup guide and one that requires a half-hour of configuration before it runs.

---

## Config Over Environment Variables

Each device's configuration lives in `~/.UnseenUniversity/<DeviceName>/config.cfg`. All the parameters that used to be environment variables — API keys, database URLs, model preferences, feature flags — live in a single file that the device reads at boot.

Why this matters: when something goes wrong and you need to diagnose it, the first question is "what was the system configured to do?" With environment variables, the answer requires reconstructing the shell environment at the time of the failure — which may be gone. With a config file, the answer is in a single readable file that persists.

The config file is also the right boundary for human-readable documentation of the device's operating parameters. It can be committed to a private repo, diffed across environments, and reviewed as part of a debugging session. Environment variables cannot.

---

## Config Layout

Each device's configuration is a file. The layout is flat: one global file for system-wide defaults, one per-device file for device-specific parameters. No intermediate layers.

- `~/.UnseenUniversity/uu.cfg` — global defaults: API keys, model preferences, log paths, any setting that applies across all devices.
- `~/.UnseenUniversity/<DeviceName>/config.cfg` — device-specific overrides: ports, instance names, feature flags, parameters unique to that device.

The precedence rule is simple: launch flag > device config > global config. A parameter set explicitly at launch beats a device config, which beats the global default. Most parameters never need to be set at launch — the two config files cover the common cases.

This layout has a practical benefit beyond the technical one: when diagnosing a problem, there are exactly two files to check. Not a cascade of environment variables, not a `.env` file and a config file and a launch script, not values spread across three locations that may contradict. Two files, clear precedence.

---

## The Communication Layer

Devices in the rack communicate through structured channels rather than direct calls. A channel is a named message queue that any device can write to and any device can subscribe to. The shared channel is where Igor, Librarian, and the Claude Code session exchange messages. Device-specific channels handle point-to-point communication.

The channel model has two advantages over direct calls. First, it is asynchronous: a device that is slow or temporarily unavailable does not block the device sending the message. Second, it is observable: every message through the channel is logged, which means the communication history between devices is always available for debugging.

The inbox is a specific channel pattern: a queue of notifications addressed to the Claude Code session, written by Igor and other devices. At the start of every session, the inbox is checked and any unread notifications are surfaced. High-urgency notifications (like a scope guard block or a safe-mode trip) surface immediately. This is the mechanism by which the human-AI collaboration stays synchronized even when the human has been away.

---

## Compiled Inference in the Platform

The platform itself is an application of the compiled inference principle.

The early versions of the system had logic spread across many components — Igor contained web server logic, logging utilities, channel management, and queue handling alongside its cognition code. This was the blank-canvas problem at the architectural level: every concern mixed with every other concern.

The rack model separates those concerns. Igor contains cognition. Librarian contains knowledge management. The web server contains HTTP handling. Each device does one thing. When something needs to change in one area, only the relevant device changes.

The Scraps device is the most direct application: it holds the deterministic scripts that have been compiled out of Igor's inference pipeline. When a pattern in Igor's reasoning was stable enough to encode as a script — an IMAP lookup, a standard query format, a known data transformation — that pattern moved from "something Igor reasons about" to "something Igor calls." The reasoning was done once and compiled. The cost of future executions dropped to near zero.

---

## Starting From Zero

One of the design requirements of UU is that it should be possible to clone the repository, run one command, and have a working system. No manual configuration, no required setup guide, no "you need to set these environment variables first."

The launcher (`./igor`, `./superclaude`, `./uu`) detects the platform, bootstraps the virtual environment if needed, runs the fresh-install wizard if no instance exists, and launches the appropriate components. Everything about how this works is hidden from the user. The only question the system asks on a clean install is: what do you want to call this instance?

This is a statement of values. A system that requires an expert to set up is a system that only works for experts. A system that works from a clean clone is a system that can be shared, reproduced, and evaluated by anyone. The complexity of the setup is the platform's problem, not the user's.

---

## The Configurable Frontend

Every agent in the rack — Igor, Librarian, a new specialist agent added next year — needs a way for humans to interact with it. The wrong answer is to build a bespoke interface for each agent. The right answer is a configurable shim: one console implementation that any agent can surface via config.

The design is straightforward. The console reads a config value that names the agent it is fronting. The agent's name determines which channel it listens to and which capabilities it exposes. Adding a new agent does not require building a new UI — it requires setting a config value.

The entry points at the repo root reflect this:

- `./igor` — launches Igor with its console (command-line, primarily for debugging)
- `./superclaude` — launches a Claude Code session configured for this instance
- `./uu` — launches the Librarian with a human-facing chat surface, suitable for non-technical users

`./uu` is specifically designed as a general-purpose knowledge interface. A non-technical user who wants to ask "what do we know about this supplier?" or "what did we decide about this last month?" points at `./uu`. The Librarian answers from the memory palace, without that user needing to understand anything about the underlying system. The chat surface is the same kind of console as Claude Code — interactive, streaming, contextual. The complexity is the platform's problem.

---

## Granny Weatherwax and Nanny Ogg

Every rack needs something that decides what work happens, when, and who does it. UU has two devices for this — named for two witches from Terry Pratchett's Discworld, because the names fit the functions.

**Granny Weatherwax** is the ticket gateway and coding orchestrator. She is the single point of entry for all work that changes code: she receives ticket specifications, runs the filing-time audits, checks for duplicate or conflicting work, and routes each ticket to the right executor. Her routing is a graph traversal — ticket tags map to capability edges, capability edges map to worker nodes. A ticket tagged `[Database]` routes to Igor's database tools. A ticket tagged `[HIGH-inertia]` routes to CC for human review. The routing improves over time through the same Hebbian edge-strengthening that governs Igor's memory: routes that succeed develop stronger edges; novel situations escalate to inference and compile the result back into the graph. Granny almost never needs inference to make a routing decision. When she does, she makes sure she never needs it for that decision again.

**Nanny Ogg** is the scheduler and world-interaction dispatcher. Where Granny asks "what and who," Nanny asks "when and where." She owns all time-based and event-based triggers in the system: the periodic audit that runs every Friday, the consequence check that matures in fourteen days, the alignment review that fires after five consecutive cycles without human contact, the gate condition that clears when a dependency ticket closes. She also dispatches the tickets that reach outside the codebase: add an event to the calendar, post to a social platform, adjust the thermostat, run a system administration task, spin up a new specialized agent. Nanny knows what agents exist and what they can do. She is the "when condition is true, execute action" engine for the whole system.

Together, Granny and Nanny replace what was previously scattered across Igor's internal scheduler, the retired worker daemon, and manual oversight from CC. Work flows in through Granny; time and the world flow in through Nanny. Between them, the rack routes itself.

---

## The Inference Proxy — Archivist [Pending: T-archivist-device]

Every inference call on the rack goes through the Archivist first. Before the request reaches the LLM, the Archivist checks its knowledge graph. If the graph can answer the question, it does — no LLM is called, no token is burned. If the graph cannot answer, the LLM runs, the answer goes to the caller, and a learning payload fans out to the overnight pipeline. The next time a similar question arrives, the graph has a better chance of answering it.

This is compiled inference applied recursively to inference itself. The same principle that moves stable patterns from reasoning into habits, and from habits into scripts, is here applied to the inference layer directly. Each miss compiles. The graph grows. The hit rate rises. The per-call cost of inference falls every week.

The knowledge graph is epistemic — it records what was learned, not how to feel about it. Igor's memory graph carries emotional encoding because that is load-bearing for his cognition. The Archivist's graph does not. It is a fact store.

The Librarian stays as the knowledge retrieval and research service. The Archivist wraps the entire LLM layer and owns the overnight learning pipeline that keeps the graph current.

Historical chat logs are fed through the learning engine to pre-populate the graph before the first live inference call. The graph does not start from zero.

---

## Memory That Scales [Pending: T-memory-scope-layers, T-global-kb-git-repo]

The current memory architecture is a single shared store — `clan.memories` — accessible to all agents on the bus. That works for a single deployment. It does not work when the questions become: which patterns here are universal enough to share? Which data is private to one person? Which memory belongs to a specific agent and not the rack?

The answer is four tiers, each with a different ownership and access model:

**Global (community):** A forkable git repo of universal patterns — things that have proven useful in a local deployment and been judged general enough to benefit every UU instance. New deployments clone it at bootstrap. Locally-proven patterns are proposed upstream via `uu contrib submit` and merged through pull requests. No deployment-specific state, no personal data.

**Local (this deployment):** The current `clan.memories` — shared across all agents on this bus. This is where deployment-specific working memory lives.

**Agent (per-device):** Each agent owns a memory slice. Other agents on the bus can request read access via the channel. The owning agent is the authoritative writer for its domain. Granny owns build and routing memories. Vetinari owns task-management and project-status memories.

**Client (per-human):** A separate Postgres database per person. Akien's memory is not Leah's memory. Private data never leaks into shared memory, because they are different databases, not different rows.

Why separate tiers instead of one big table with access flags? Because access flags are a convention; separate databases are a boundary. The patterns that work universally belong in the global repo. The state that belongs to a specific deployment stays local. Personal data never leaks into shared memory not by policy, but by architecture.

---

## Agent Classes [Pending: T-agent-taxonomy-concept]

Not all agents on the rack are alike. Some are bounded tools; others are long-lived experts; others are general-purpose reasoners. The distinction matters for routing, capability dispatch, and memory access.

Three classes:

**Utility agents** are bounded, composable, and stateless. They do one thing. They do not own memory. They are called by other agents as services. Google Secretary is a utility agent: it accepts a structured request, performs an action against Google Workspace (calendar, email, tasks), and returns a result. It has no opinion about what action to perform; that judgment lives in the caller.

**Specialized agents** are domain experts. Each owns a memory slice and is the authoritative source for knowledge in its domain. Granny knows about builds and routing. Nanny knows about scheduling and world-interaction. Librarian knows about knowledge retrieval. They are long-lived and improve over time, but they are not general-purpose reasoners.

**General-purpose agents** have broad reasoning capability and full memory access across all tiers. Igor and CC are both in this class. They are the agents that do not have a fixed domain — they are invoked when the question is "figure out what to do and do it."

The taxonomy is labeled in the device registry manifest. Routing decisions, capability dispatch, and memory access requests read the label rather than hardcoding agent-specific logic. A new general-purpose agent added to the rack gets the right access model automatically because the class is declared, not inferred.

---

*Version 2026.06.01.current.MT — Latest: github.com/akienm/UnseenUniversity/tree/main/lab/docs/the_ownership_gap/*
