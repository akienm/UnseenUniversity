# Runtime
*.db
.env
datacenter_logs/
# Local bootstrap copy must never be committed (only the .example template is tracked)
bin/uu_bash_profile.sh

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Dev
.venv/
venv/
test_env/
.pytest_cache/
.mypy_cache/
.coverage
coverage.xml
*.cover

# ticket_store flock artifact (runtime, never tracked)
devlab/runtime/memory/tickets/.queue.lock

# generated labeling page (regenerable via devlab/claudecode/stuck_corpus_label_page.py;
# the durable artifact is labels.json, which IS tracked)
devlab/runtime/memory/eval_slices/**/label_page.html
